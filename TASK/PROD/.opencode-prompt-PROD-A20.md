# PROD-A20 — Config: PAYMENT_PROVIDER + DISBURSEMENT_PROVIDER

You are executing **exactly one task**: `PROD-A20` in monorepo `/var/www/fersaku-new`.

## Read first

1. `TASK/PROD/02-PHASE-A-FOUNDATION-STAGING.md` (PROD-A20 only)
2. `TASK/PROD/01-PROVIDER-ARCHITECTURE.md` §5 config composition
3. `TASK/PROD/10-SECRETS-AND-ENV-MATRIX.md` (names only)
4. `TASK/PROD/00-DECISIONS-AND-NON-GOALS.md` + ADR-0008
5. `backend/internal/config/config.go` + `config_test.go` (full)
6. `backend/internal/app/app.go` payment wiring (~lines 188–220 and readiness uses of xenditKind / UseFakeXendit)
7. `backend/.env.example`
8. Existing tests that assert XENDIT_MODE=fake forbidden on staging/production

## STRICT CONSTRAINTS

- **Only PROD-A20.** Do NOT implement Duitku HTTP adapter (that's B10). Do NOT unwire Xendit QRIS yet (B40). Do NOT change UI.
- **No secrets** in any file. Never paste `/var/www/pg.txt` values.
- Keep code **scalable, modular, clean**; follow existing config package style.
- Preserve backward compatibility: legacy `XENDIT_MODE=fake|live` must still work during transition.
- Fake providers still work on `APP_ENV=local|test`. Staging/production **fail closed**.
- Optional drill escape hatch: `ALLOW_FAKE_PROVIDERS=1` may allow fake on staging only for dry drills (document in comments + `.env.example`). Production always forbids fake even with the flag (strictest best practice) OR document if you allow only staging.
- Prefer: production always rejects fake; staging rejects fake unless `ALLOW_FAKE_PROVIDERS=1`.
- Duitku adapter does not exist yet — composition may still use **Xendit fake/real for payment QRIS** until B10, BUT selection must be driven by the new provider fields:
  - If `PAYMENT_PROVIDER=fake` → use existing xendit.Fake as payment (temporary until Duitku lands)
  - If `PAYMENT_PROVIDER=duitku` → for now, return a clear boot error like `app: PAYMENT_PROVIDER=duitku requires Duitku adapter (PROD-B10 not landed)` **OR** if you can leave a typed stub that fails CreateQRIS with ErrNotImplemented — boot error is cleaner so local default stays `fake`.
  - Best practice for A20: default local `PAYMENT_PROVIDER=fake`, `DISBURSEMENT_PROVIDER=fake`; when payment=duitku before B10, fail closed at composition with explicit message (not silent Xendit).
  - Disbursement: fake → existing Fake for disbursement methods; xendit → real Xendit. Today Fake/Real may be one adapter for both — keep that structure but gate with UseFakePayment / UseFakeDisbursement helpers.
- Update unit tests thoroughly.
- Evidence under `TASK/PROD/evidence/PROD-A20/`
- Update board A20 → done when acceptance met.
- Do not claim B* tasks.

## Implementation requirements

### 1. `backend/internal/config/config.go`

Add types/fields (names exact):

```go
// PaymentProvider selects QRIS payment adapter.
// Values: duitku | fake
PaymentProvider string

// DisbursementProvider selects withdrawal adapter.
// Values: xendit | fake
DisbursementProvider string

// Duitku* fields (strings; empty ok when payment=fake)
DuitkuMerchantCode
DuitkuAPIKey
DuitkuEnv            // sandbox|production
DuitkuBaseURL
DuitkuCallbackURL
DuitkuReturnURL
DuitkuQRISPaymentMethod  // default SP
DuitkuAccountScope       // default duitku-primary

// Xendit already has secret/webhook/scope; also:
XenditEnv      // sandbox|production optional
XenditBaseURL  // default https://api.xendit.co when empty

// AllowFakeProviders drill flag (staging only)
AllowFakeProviders bool // from ALLOW_FAKE_PROVIDERS=1
```

**Legacy mapping on Load (critical):**

1. Read `PAYMENT_PROVIDER` and `DISBURSEMENT_PROVIDER` if set.
2. If either empty, map from `XENDIT_MODE`:
   - `XENDIT_MODE=fake` (or empty default local) → payment=fake, disbursement=fake
   - `XENDIT_MODE=live` → payment should default carefully: historically live meant real Xendit for both. For transition: disbursement=xendit, and payment= if PAYMENT_PROVIDER unset then **fake is wrong** — use payment=`xendit` temporary?  
   
**Cleaner transition (follow TASK docs):**

- Preferred env: `PAYMENT_PROVIDER=duitku|fake`, `DISBURSEMENT_PROVIDER=xendit|fake`
- Keep `XENDIT_MODE` as legacy alias:
  - If `DISBURSEMENT_PROVIDER` empty and `XENDIT_MODE=fake` → disbursement=fake
  - If `DISBURSEMENT_PROVIDER` empty and `XENDIT_MODE=live` → disbursement=xendit
  - If `PAYMENT_PROVIDER` empty and `XENDIT_MODE=fake` → payment=fake
  - If `PAYMENT_PROVIDER` empty and `XENDIT_MODE=live` → payment remains **xendit-legacy** only if you introduce temporary value `xendit` for payment during transition, OR keep UseFakeXendit() as OR of both fakes for old call sites.

Recommended approach (minimal breakage):

```text
type fields:
  PaymentProvider: "fake" | "duitku" | "xendit"  // "xendit" = temporary legacy QRIS until B40
  DisbursementProvider: "fake" | "xendit"

Load defaults for local:
  PAYMENT_PROVIDER default empty → derive from XENDIT_MODE (fake→fake, live→xendit)
  DISBURSEMENT_PROVIDER default empty → same

Helpers:
  UseFakePayment() bool
  UseFakeDisbursement() bool
  UseFakeXendit() bool  // DEPRECATED compat: return UseFakePayment() && UseFakeDisbursement() OR (legacy) return UseFakePayment() || keep old behavior for tests: return XenditMode==fake if still set, else UseFakePayment()&&UseFakeDisbursement()

Actually keep XenditMode field populated for compat:
  After resolving providers, set XenditMode = fake if both providers fake, else live if either real xendit path needs keys.
```

**Validation rules:**

| APP_ENV | fake payment | fake disbursement |
| local/test | allowed | allowed |
| staging | forbidden unless ALLOW_FAKE_PROVIDERS=1 | same |
| production | always forbidden | always forbidden |

When payment=duitku: require DUITKU_MERCHANT_CODE, DUITKU_API_KEY on staging/production (and on local if explicitly duitku).
When disbursement=xendit or payment=xendit (legacy): require XENDIT_SECRET_KEY + XENDIT_WEBHOOK_TOKEN on live runtime (same as today).
When payment=fake and disbursement=fake on local: no provider keys required.

Normalize provider strings to lowercase; reject unknown values.

### 2. Tests `config_test.go`

- Local allows fake payment + fake disbursement
- Production rejects PAYMENT_PROVIDER=fake
- Production rejects DISBURSEMENT_PROVIDER=fake
- Staging rejects fake without ALLOW_FAKE_PROVIDERS
- Staging allows fake when ALLOW_FAKE_PROVIDERS=1 (if you implement the flag)
- Legacy XENDIT_MODE=fake still loads as fake providers on local
- Legacy XENDIT_MODE=live maps to real disbursement (+ payment xendit legacy) and requires keys
- Keep existing tests green by adapting assertions to new fields while preserving intent (INT-180)

### 3. Composition `app.go`

- Branch payment adapter on `cfg.PaymentProvider` / `UseFakePayment()`
- Branch disbursement: for now if Fake implements both ports, you may still assign same Fake/Real to both QRIS and Disbursement interfaces when providers agree; when they diverge (payment=fake, disbursement=xendit) wire accordingly if possible with existing adapters.
- Inspect existing Fake/Real: if single struct implements both QRISProvider and DisbursementProvider, composition can:
  - payment fake + disbursement xendit: pay=Fake, disburse=Real (need separate vars if currently one `pay` used for both)
- Search all uses of `pay` / `UseFakeXendit` / `XenditFake` in app.go and fix compile.
- If payment=duitku: return clear error until B10 (do not silently use Xendit).
- Readiness checks: report payment and disbursement separately if easy; otherwise keep xendit component name but message accurate.
- Do not break compile of `./...`

### 4. `backend/.env.example`

Document (names only, no secrets):

```
# Dual-provider (ADR-0008 / PROD-A20)
PAYMENT_PROVIDER=fake          # fake | duitku | xendit (xendit = legacy QRIS until B40)
DISBURSEMENT_PROVIDER=fake     # fake | xendit
# ALLOW_FAKE_PROVIDERS=0       # staging drill only; production always rejects fake

# Duitku (when PAYMENT_PROVIDER=duitku)
DUITKU_MERCHANT_CODE=
DUITKU_API_KEY=
DUITKU_ENV=sandbox
DUITKU_BASE_URL=
DUITKU_CALLBACK_URL=
DUITKU_RETURN_URL=
DUITKU_QRIS_PAYMENT_METHOD=SP
DUITKU_ACCOUNT_SCOPE=duitku-primary

# Xendit (disbursement + legacy payment)
XENDIT_MODE=fake               # legacy alias; prefer PAYMENT_/DISBURSEMENT_PROVIDER
XENDIT_SECRET_KEY=
XENDIT_WEBHOOK_TOKEN=
XENDIT_ACCOUNT_SCOPE=xendit-primary
XENDIT_ENV=sandbox
XENDIT_BASE_URL=https://api.xendit.co
```

### 5. Evidence + board

- `TASK/PROD/evidence/PROD-A20/20260719-HHMM-opencode.md` (use actual time)
- Run: `export PATH="$HOME/.local/go/bin:$PATH" && cd backend && go test ./internal/config/ ./internal/app/ -count=1`
- Paste pass summary in evidence (no secrets)
- Board: PROD-A20 → done + evidence path

## Acceptance

- [ ] `go test` config package green
- [ ] `go test` app package green (or whole internal if needed)
- [ ] `.env.example` lists names only
- [ ] Composition selects adapters by config (fake stubs until B/C land; duitku fails closed until B10)
- [ ] Staging/production reject fake (with optional staging drill flag)
- [ ] Evidence + board updated

## After completing

Print:
- Files changed
- Test commands + results
- How legacy XENDIT_MODE maps
- Whether payment=duitku boot-errors until B10
- Evidence path
