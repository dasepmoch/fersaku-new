# KEY-32 — Cookie / HTTPS / CSRF on real domain

| Field | Value |
|-------|--------|
| Date | 2026-07-19 |
| Agent | @opencode |
| Status | **done** |
| Domains | `https://fersaku.net` · `https://api.fersaku.net` |
| Depends | KEY-30/31 (contract); public tunnel live |

---

## 1. HTTPS / security headers

| Check | Result |
|-------|--------|
| `https://fersaku.net/` | **200**, TLS verify OK |
| `https://api.fersaku.net/health/live` | **200** |
| `https://api.fersaku.net/v1/status` | **200** |
| HSTS on FE | `max-age=63072000; includeSubDomains; preload` |
| `X-Frame-Options` | `DENY` |
| `X-Content-Type-Options` | `nosniff` |
| CSP present | yes (`default-src 'self'` …) |

## 2. Session cookie (login)

### 2.1 API host `POST /v1/auth/login`

| Attribute | Value |
|-----------|--------|
| Cookie name | `fersaku_session` |
| HttpOnly | **true** |
| Secure | **true** |
| SameSite | **Lax** |
| MFA required | **false** |
| Surface | SELLER |
| csrfToken in body | present (not logged) |

### 2.2 FE same-origin `POST https://fersaku.net/v1/auth/login`

| Attribute | Value |
|-----------|--------|
| Set-Cookie | `fersaku_session` HttpOnly + Secure + SameSite=Lax |
| Body | 200 · surface SELLER · csrf present |

## 3. CSRF mutation after login

| Step | Result |
|------|--------|
| Logout with **bad** CSRF (`X-CSRF-Token: bad`) via FE | **403** |
| Logout with **valid** CSRF from login body via FE | **200** `Signed out` |

(E10 residual “browser Secure cookie capture” closed for public HTTPS with redacted attribute proof.)

## 4. Private route after logout

| Check | Result |
|-------|--------|
| Unauthenticated `GET https://fersaku.net/v1/auth/session` | **401** |

## 5. Acceptance

- [x] Real domain TLS + HSTS  
- [x] Session cookie HttpOnly + Secure + SameSite  
- [x] CSRF rejects invalid token  
- [x] Valid CSRF logout succeeds on FE same-origin  
- [x] No secret/cookie values in evidence  

## 6. Residuals

| Item | Note |
|------|------|
| Hard refresh full browser headed on public | Optional polish; API attributes proven |
| Staging dedicated host | KEY-22 |

## Secrets check

- [x] cookie values not recorded (lengths/flags only)
