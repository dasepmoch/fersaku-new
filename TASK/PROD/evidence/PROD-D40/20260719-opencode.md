# PROD-D40 evidence — Admin money surfaces sanity

| Field | Value |
| ----- | ----- |
| Task | PROD-D40 |
| Date | 2026-07-19 |
| Agent | @nikki/opencode |
| Status | done |

## Summary

Catalogued admin payments / withdrawals / orders routes and API clients. Privileged write commands require audit reason (and permissions). No code changes.

## Route checklist (read paths → API)

| FE route | Client | API |
| -------- | ------ | --- |
| `/admin/payments` | `listPayments` / `listPaymentsPage` | `GET /v1/admin/payments` |
| payment detail | `getPayment` | `GET /v1/admin/payments/{id}` |
| payment mismatches | `listPaymentMismatches` | `GET /v1/admin/payment-mismatches` |
| `/admin/withdrawals` | `listWithdrawals` / `listWithdrawalsPage` | `GET /v1/admin/withdrawals` |
| `/admin/withdrawals/[id]` | `getWithdrawal` | `GET /v1/admin/withdrawals/{id}` |
| `/admin/orders` | `listAdminOrders` / `listAdminOrdersPage` | `GET /v1/admin/orders` |
| `/admin/orders/[orderId]` | `getAdminOrder` | `GET /v1/admin/orders/{id}` |

Domain gate: `shouldUseMockFixtures("adminRead")` / `getDomainSource("adminRead") === "api"`.

Nav: `features/admin/components/admin-shell.tsx` — Orders, Payments, Withdrawals.

## Privileged commands (reason required)

| Command | Client | Reason rule |
| ------- | ------ | ----------- |
| Withdrawal review approve/hold/reject | `reviewAdminWithdrawal` | `reason.length >= 12`; `auditReason`; optional MFA |
| Order delivery resend | `resendAdminOrderDelivery` | `reason` required; `auditReason` |
| Payment provider lookup | `providerLookup` (order-payment-commands) | `reason` required |
| Force fulfill / revoke | `fulfillment-commands.ts` | reason + permissions |

UI: `features/admin/ui/dialogs.tsx` — reason ≥ 12 chars for privileged confirm.

## Tests

```text
npm run test:run -- --reporter=dot adm-310-admin-withdrawals
# included in 59-pass batch
```

## Acceptance

- [x] Checklist of money admin routes + API clients
- [x] Privileged commands note reason (+ permission server-side)
- [x] Evidence attached

## Non-goals

- Live admin session browser walkthrough (no MFA program constraint noted; not executed here)
- UI redesign
