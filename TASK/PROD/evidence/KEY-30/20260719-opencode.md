# KEY-30 — FE production env contract (force-api live)

| Field | Value |
|-------|--------|
| Date | 2026-07-19 |
| Agent | @opencode |
| Status | **done** (contract + code gates verified; deploy inject = ops) |
| Depends | KEY-22 (staging deploy later); code gates independent |

---

## 1. Mandatory production FE env

| Variable | Production value | Notes |
|----------|------------------|-------|
| `NEXT_PUBLIC_DATA_SOURCE` | **`api`** | Never `mock` on live |
| `NEXT_PUBLIC_APP_STAGE` | **`live`** | Fail-closed with mock |
| `API_INTERNAL_URL` | internal API origin | Required when stage=live |
| `NEXT_PUBLIC_API_URL` | **empty** | Same-origin `/v1` only |

Authority: `TASK/PROD/10-SECRETS-AND-ENV-MATRIX.md` §3; `frontend/shared/config/env.ts` `assertSafePublicEnvironment`.

## 2. Code fail-closed (verified)

| Gate | Behavior |
|------|----------|
| `assertSafePublicEnvironment` | live + dataSource≠api → **throw** |
| `createBootstrapDomainSourceSnapshot` | live + bootstrap mock → config error / rewrite residual mock → disabled |
| `assertProductionDomainSources` | live snapshot must not contain domain `mock` |
| `shouldUseMockFixtures` | only true when domain source is mock |

**Unit:** `vitest tests/unit/env.test.ts` → **15/15 pass**.

## 3. Demo host FE (presence)

| Var | Status |
|-----|--------|
| `NEXT_PUBLIC_DATA_SOURCE` | SET (api) |
| `NEXT_PUBLIC_APP_STAGE` | SET |
| `API_INTERNAL_URL` | SET |
| `NEXT_PUBLIC_API_URL` | absent (correct empty) |

## 4. Deploy checklist (ops)

```text
[ ] Build with NEXT_PUBLIC_DATA_SOURCE=api and NEXT_PUBLIC_APP_STAGE=live baked or runtime
[ ] API_INTERNAL_URL points at private API (not public internet if possible)
[ ] NEXT_PUBLIC_API_URL unset
[ ] Boot fails if mis-set live+mock
[ ] Browser network: only same-origin /v1 for API (no provider secrets)
```

## 5. Acceptance

- [x] Contract documented  
- [x] Live+mock rejected by code + unit tests  
- [x] Demo FE already on api mode  
- [x] Residual: production deploy inject by ops (KEY-33 edge)

## Secrets check

- [x] no secrets
