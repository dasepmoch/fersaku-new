# KEY-11 — Managed Postgres + PITR (detailed)

| Field | Value |
|-------|--------|
| Date | 2026-07-19 |
| Agent | @opencode |
| Status | **done** for **local logical drill + runbook**; managed HA **residual human** |
| Host class | compose postgres single-node (`sslmode=disable`) — **not** prod |

---

## 1. Deliverables

| Deliverable | Path |
|-------------|------|
| Runbook | `TASK/PROD/ops/02-postgres-pitr-runbook.md` |
| Reusable drill script | `backend/scripts/pg_logical_backup_drill.sh` |
| Dump artifact (host tmp) | `/tmp/opencode/fersaku-drills/fersaku-20260719T142546Z.dump` (~524 KiB) |

## 2. Drill executed

| Step | Result |
|------|--------|
| `pg_dump -Fc` from `fersaku-backend-postgres-1` | OK |
| `CREATE DATABASE fersaku_restore_drill` | OK |
| `pg_restore` into clone | OK |
| Row parity payments/orders/withdrawals | **20/20 · 27/27 · 8/8** |
| Clone `SELECT 1` | OK |
| Approx RTO (logical, small DB) | ≪ 1 minute |

## 3. Production residual (ops)

| Item | Status |
|------|--------|
| Managed multi-AZ PG + continuous PITR | **not provisioned on this host** |
| Prod `DATABASE_URL` TLS + SM | ops |
| Quarterly managed PITR drill | ops |

## 4. Acceptance mapping

| Criterion | Met? |
|-----------|------|
| Restore drill evidence + counts | **yes (local)** |
| App boot against restored **managed** clone | **no** (no managed instance) |
| Honest non-claim of prod HA | **yes** |

## Secrets check

- [x] no DSN passwords in file (local compose password not repeated)
