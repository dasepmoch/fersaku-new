# PROD-F20 — Checkout-order cell

| Field | Value |
| ----- | ----- |
| Task | PROD-F20 |
| Date | 2026-07-19 |
| Agent | @nikki/opencode |
| Status | **done** for automated path; ~~live PAID residual deferred (B50)~~ **SUPERSEDED:** B50 CLOSED same day — see KEY-01 |

## Automated proof (DATABASE_URL set)

```bash
export DATABASE_URL=postgres://fersaku:fersaku_local@localhost:5433/fersaku?sslmode=disable
export GOTOOLCHAIN=auto
go test -tags=integration -count=1 -timeout 400s ./test/integration/ -run 'TestCheckout_|TestCallback'
# PASS ok
```

### Checkout integration

| Test | Result |
| ---- | ------ |
| `TestCheckout_ClientPriceIgnoredFee100kAndIdempotent` | PASS |
| `TestCheckout_ExpireTimeoutStaysUnknownNoStockRelease` | PASS |
| `TestCheckout_SimulateDisabledInProductionEnv` | PASS |
| `TestCheckout_ProdRouterOmitsSimulate` | PASS |
| `TestCheckout_CodeStockReserveHeldOnExpireTimeout` | PASS |

### Callback / paid path integration (Xendit-shaped fake provider in suite)

| Test | Result |
| ---- | ------ |
| `TestCallback_InvalidToken_RejectionOnly` | PASS |
| `TestCallback_DuplicatePaid_SingleEffect` | PASS (idempotent single effect) |
| `TestCallback_LatePaidAfterExpire` | PASS |
| `TestCallback_CrossPaymentMode_NoCollision` | PASS |
| `TestCallback_MissingToken_Rejection` | PASS |

## Duitku unit path (current code)

| Suite | Result |
| ---- | ------ |
| `HandleDuitkuIngress_*` (invalid/missing/accept+duplicate/form) | PASS |
| `CheckoutService` payment provider identity / CreateQRIS scope | PASS |
| `internal/adapters/duitku` (CreateQRIS map, sig fixtures, status map, no secret leak in Name) | PASS |

## Honest residual (do not invent PAID)

| Criterion | Status |
| --------- | ------ |
| Automated checkout + paid callback single-effect | **pass** (integration) |
| Duitku signature/idempotency unit | **pass** |
| Full **sandbox QRIS pay → PAID → ledger** on deployed API | **pass** — PROD-B50 CLOSED same day; KEY-01 supersedes earlier deferred note |
| Live compose `/v1/webhooks/duitku` | **mounted** after rebuild (empty body → 401; B50/E2E-06) |

## Secrets check

- [x] no secrets; no live payment ids invented as PAID
