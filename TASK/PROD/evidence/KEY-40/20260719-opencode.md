# KEY-40 — Duitku callback + public sandbox paid (Playwright headed)

| Field | Value |
|-------|--------|
| Date | 2026-07-19 |
| Agent | @opencode |
| Status | **done** |
| Base | `https://fersaku.net` + `https://api.fersaku.net` |
| Mode | Playwright **headed** + signed public webhook |

> Note: This host is the **demo/tunnel** environment (not a separate managed staging). Treat as **public sandbox parity** for KEY-40 accept; dedicated staging remains KEY-22 when provisioned.

---

## 1. Headed browser (public FE)

| Step | Result | Notes |
|------|--------|-------|
| Open home | pass | `https://fersaku.net/` |
| Storefront `/@seed-store-a` | pass | seed store loads |
| Checkout page | pass | seed product checkout |
| Create intent (UI) | pass | POST **201** · provider **DUITKU** · **PENDING** |
| Seller login public | pass | → `/dashboard` |

Intent: `pi_01KXXA7M07A4K7D6YCB5H7JWA7` · order `ORD-YCB5H7JWA6` · amount 50000

Screenshots: `headed-*.png` in this folder.

## 2. Public Duitku webhook (signed)

| Step | Result |
|------|--------|
| `POST https://api.fersaku.net/v1/webhooks/duitku` resultCode=00 | **200 OK** |
| Intent status | **PAID** |
| Settlements | **1** |
| Replay callback | 200 · settlements still **1** |

## 3. Dashboard callback expectation

Documented URL (KEY-10): `https://api.fersaku.net/v1/webhooks/duitku`  
Verified mounted (empty body → 401 earlier; signed pay → 200).

Human still owns Duitku **dashboard** UI screenshot that the live merchant points here (ops).

## 4. Acceptance

- [x] Public FE checkout creates DUITKU intent  
- [x] Public API signed callback → PAID + settlement×1 + replay-safe  
- [x] Headed screenshots  
- [x] No secrets in evidence  

## 5. Residuals

| Item | Task |
|------|------|
| Dedicated staging hostname/env | KEY-22 |
| Dashboard console screenshot | human ops |
| LIVE keys | KEY-62 only |

## Secrets check

- [x] no api keys / signatures / cookies
