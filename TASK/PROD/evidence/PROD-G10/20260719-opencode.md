# PROD-G10 — Production secrets inventory

| Field | Value |
| ----- | ----- |
| Task | PROD-G10 |
| Date | 2026-07-19 |
| Agent | @nikki/opencode |
| Status | **done** (inventory prepared; secret-manager locations TBD human) |

**Rules honored:** values never copied from `/var/www/pg.txt` or process env. Presence = key name found with non-empty assignment only where checked.

## Webhook URL expectations (names / public paths only)

| Provider | Env / dashboard field | Expected production HTTPS URL |
| -------- | --------------------- | ----------------------------- |
| Duitku payment callback | `DUITKU_CALLBACK_URL` | `https://api.fersaku.net/v1/webhooks/duitku` |
| Xendit disbursement callback | dashboard / `XENDIT_DISBURSEMENT_CALLBACK_URL` (doc only; route fixed in app) | `https://api.fersaku.net/v1/webhooks/xendit/disbursement` |

Optional mode-scoped Duitku paths (same host): `/v1/webhooks/duitku/sandbox`, `/v1/webhooks/duitku/live`. Legacy Xendit payment `/v1/webhooks/xendit` is not primary after dual-provider cutover.

## Inventory (presence yes/no only)

| Secret name | Purpose | Current host source | Target production storage | Present on host? (yes/no only) | Rotation note |
| ----------- | ------- | ------------------- | ------------------------- | ------------------------------ | ------------- |
| `APP_ENV` | Fail-closed tier | compose / process | secret manager or deploy env | no (not in `pg.txt`; local compose) | N/A non-secret |
| `HTTP_ADDR` | Listen address | compose default | deploy config | no | N/A |
| `DATABASE_URL` | Postgres DSN | local compose | managed PG secret + TLS | no | rotate credentials on leak / staff offboard |
| `REDIS_URL` | Redis DSN | local compose | managed Redis secret + TLS | no | rotate on leak |
| `SESSION_SECRET` | Session HMAC | local placeholder / unset on host | secret manager ≥32 random | no | compromise → rotate + force revoke-all |
| `CSRF_SECRET` | CSRF HMAC | local placeholder / unset on host | secret manager ≥32 random | no | same as session |
| `KYC_ENCRYPTION_KEY` | KYC field crypto | unset on host | secret manager (required prod) | no | rotate only with re-encrypt plan |
| `STOCK_ENCRYPTION_KEY` | Stock/credential crypto | unset; may fall back KYC | secret manager (required prod) | no | rotate with re-encrypt plan |
| `LOG_LEVEL` | Logging | compose / default | deploy config | no | N/A |
| `PAYMENT_PROVIDER` | Dual-provider: payment driver | `.env.example` default `fake` | secret manager / deploy env (`duitku` prod) | no | N/A config |
| `DISBURSEMENT_PROVIDER` | Dual-provider: payout driver | `.env.example` default `fake` | secret manager / deploy env (`xendit` prod) | no | N/A config |
| `DUITKU_MERCHANT_CODE` | Duitku merchant id | `/var/www/pg.txt` | secret manager | yes | env sandbox→prod; merchant change |
| `DUITKU_API_KEY` | Duitku API auth | `/var/www/pg.txt` | secret manager | yes | leak suspicion; sandbox→prod |
| `DUITKU_ENV` | Duitku sandbox\|production | `/var/www/pg.txt` | secret manager / deploy env | yes | N/A config |
| `DUITKU_BASE_URL` | Duitku API base | `/var/www/pg.txt` | secret manager / deploy env | yes | N/A config |
| `DUITKU_CALLBACK_URL` | Public payment webhook | `/var/www/pg.txt` | secret manager + Duitku dashboard | yes | update when ingress host changes |
| `DUITKU_RETURN_URL` | Buyer return (non-authoritative) | `/var/www/pg.txt` | secret manager / deploy env | yes | N/A |
| `DUITKU_QRIS_PAYMENT_METHOD` | QRIS method code | `/var/www/pg.txt` | secret manager / deploy env | yes | N/A config |
| `DUITKU_ACCOUNT_SCOPE` | Ledger/account scope | `.env.example` only | secret manager / deploy env | no | N/A config |
| `XENDIT_SECRET_KEY` | Xendit API auth (disbursement) | `/var/www/pg.txt` | secret manager | yes | leak; staff offboarding |
| `XENDIT_WEBHOOK_TOKEN` | Disbursement callback auth | `/var/www/pg.txt` | secret manager + Xendit dashboard | yes | leak; staff offboarding |
| `XENDIT_ENV` | Xendit sandbox\|production | `/var/www/pg.txt` | secret manager / deploy env | yes | N/A config |
| `XENDIT_BASE_URL` | Xendit API base | `/var/www/pg.txt` | secret manager / deploy env | yes | N/A config |
| `XENDIT_ACCOUNT_SCOPE` | Disbursement account scope | `.env.example` only | secret manager / deploy env | no | N/A config |
| `XENDIT_MODE` | Legacy provider alias | `.env.example` only | prefer `DISBURSEMENT_PROVIDER` | no | deprecate after migration |
| `XENDIT_DISBURSEMENT_CALLBACK_URL` | Doc / dashboard reference | `/var/www/pg.txt` | Xendit dashboard (route fixed in app) | yes | update when ingress host changes |
| `R2_ENDPOINT` | Object storage endpoint | local MinIO default only | prod R2 endpoint secret | no | N/A endpoint |
| `R2_BUCKET_PUBLIC` | Public bucket name | local default only | deploy config | no | N/A |
| `R2_BUCKET_PRIVATE` | Private bucket name | local default only | deploy config | no | N/A |
| `R2_ACCESS_KEY_ID` | R2 access key | local MinIO only | secret manager | no | rotate on leak / offboard |
| `R2_SECRET_ACCESS_KEY` | R2 secret key | local MinIO only | secret manager | no | rotate on leak / offboard |
| `R2_REGION` | R2 region | local default | deploy config | no | N/A |
| `MAIL_SMTP_HOST` | SMTP host | local Mailpit only | secret manager / deploy | no | N/A |
| `MAIL_SMTP_PORT` | SMTP port | local only | deploy config | no | N/A |
| `MAIL_FROM` | From address | local only | deploy config | no | N/A |
| `MAIL_SMTP_USER` | SMTP user | unset on host | secret manager | no | rotate on leak / offboard |
| `MAIL_SMTP_PASSWORD` | SMTP password | unset on host | secret manager | no | rotate on leak / offboard |
| `NEXT_PUBLIC_DATA_SOURCE` | FE data mode | `frontend/.env.local` | deploy FE env (`api` prod) | yes | N/A public |
| `NEXT_PUBLIC_APP_STAGE` | FE stage flag | `frontend/.env.local` | deploy FE env (`live` prod) | yes | N/A public |
| `API_INTERNAL_URL` | FE server rewrite target | `frontend/.env.local` | internal service URL only | yes | N/A internal |
| `NEXT_PUBLIC_API_URL` | Browser API base | empty expected | empty (same-origin `/v1`) | no (correct empty) | must stay empty prod |

Notes:

- `backend/.env` **absent** on this host; dual-provider secrets for sandbox tools come from `/var/www/pg.txt` only.
- Browser must never receive provider secrets (`DUITKU_*`, `XENDIT_*`, encryption keys, SMTP passwords).
- Human ops: fill **Target production storage** with concrete secret-manager paths/ARNs when provisioned.

## Secrets hygiene

- [x] no secret values in this file
- [x] no values from `/var/www/pg.txt` copied
- [x] inventory only yes/no presence
