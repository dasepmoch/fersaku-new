# PROD-B50 — Sandbox manual payment proof

| Field | Value |
| ----- | ----- |
| Task | PROD-B50 |
| Date | 2026-07-19 |
| Agent | @nikki/opencode |
| Status | **partial / deferred for full PAID** |

## What was proven (no secrets)

### Duitku sandbox CreateQRIS (live sandbox HTTP)

| Field | Value |
| ----- | ----- |
| merchantOrderId | `prod-b50-1784442904` |
| provider reference | `DS3290626NRNJUSEUCW00GIM` |
| domain status on create | `PENDING` |
| amount | Rp 10.000 |
| QR string | present (prefix `00020101021226610016ID.CO.SHOPEE.WWW…`) |
| QR image URL | set (true) |
| expires | `2026-07-19T14:05:04+07:00` |
| base URL | `https://sandbox.duitku.com` (from host secret names) |
| payment method | `SP` |

Secrets loaded only from host `/var/www/pg.txt` into process env for one-shot create; **not** written to git, evidence, or logs.

### Status inquiry

`GetPayment` against sandbox returned `INVALID_RESPONSE` / invalid JSON on this run — adapter status path needs follow-up hardening (not blocking create). Full paid settlement still requires:

1. Human (or sandbox simulator) pays the QRIS, **or**
2. Simulated callback via `HandleDuitkuIngress` against a real intent in DB (integration with `DATABASE_URL` + rebuilt API image with B10–B40 code)

### PAID + single ledger credit

**Not completed in this session.** Running compose API image is ~56m old (pre–PROD dual-provider rebuild). Full loop:

`intent create (API) → Duitku callback → PAID → ledger credit`

needs:

- rebuild/redeploy `fersaku-api:local` with B10–B40 code + `PAYMENT_PROVIDER=duitku` env
- either real sandbox pay or crafted signed callback
- evidence of single ledger row

## Acceptance mapping

| Criterion | Result |
| --------- | ------ |
| Sandbox create with host secrets | **pass** (ids above) |
| PAID + single ledger credit | **deferred** — see residual |

## Residual / next

- PROD-F40 canary or owner-assisted QRIS pay after API redeploy with dual-provider env
- Fix status inquiry JSON mapping if Duitku status payload differs from assumed shape
- Do not mark full money E2E green until ledger evidence exists

## Secrets check

- [x] no keys in this file
- [x] no keys in commit
- [x] one-shot cmd tools removed from tree after run
