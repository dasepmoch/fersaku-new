# PROD-B50 — Sandbox payment proof (CLOSED after host redeploy)

| Field | Value |
| ----- | ----- |
| Task | PROD-B50 |
| Date | 2026-07-19 |
| Agent | @nikki |
| Status | **done** |

## Runtime prep (this host)

1. Rebuilt `fersaku-api:local` + `fersaku-worker:local` with dual-provider code
2. Fixed compose env_file order: `.env.example` then `.env` (later wins)
3. Host-local gitignored `backend/.env` with `PAYMENT_PROVIDER=duitku`, `DISBURSEMENT_PROVIDER=xendit` + sandbox keys from `/var/www/pg.txt` (not committed)
4. Migration **000033** allows `provider IN ('XENDIT','DUITKU')` on intents/settlements/rejections
5. Boot log: `payment_provider=duitku`, `disbursement_provider=xendit`

## E2E money path (sandbox Duitku)

| Step | Result |
| ---- | ------ |
| `POST /v1/checkout/intents` | **201** |
| Intent | `pi_01KXWMGFPFRMV77PCVP9NM2VEN` |
| Order | `ord_01KXWMGFPFRMV77PCVP9NM2VEM` |
| Amount / fee / net | Rp **50.000** / fee **2.200** / net **47.800** |
| Provider on create | **DUITKU** · scope **duitku-primary** · mode SANDBOX |
| Provider reference | `DS3290626K8UJ32EEEZAJT7I` |
| QR | present (sandbox QRIS string + image URL) |
| `POST /v1/webhooks/duitku` signed paid (`resultCode=00`) | **200 OK** |
| Intent after callback | **PAID** |
| Order payment_status | **PAID** |
| Settlements | **1** row (`POSTED`, provider DUITKU, journal `PAYMENT_CAPTURE:pi_…`) |
| Delivery grants | **1** |
| Ledger journals | **2** POSTED for intent (capture path) · **6** ledger_entries |
| Replay callback | **200 OK**, settlements still **1** (no double credit) |

## Merchant balance

Seed merchant `01HQ0SEED00000000000000021` (SANDBOX) shows available balance including this net credit (lot STOREFRONT remaining 47800 observed).

## Withdrawal path smoke

- Seller login seed persona OK (no MFA)
- `GET /v1/stores/{store}/withdrawals` → **200**
- Quote endpoint rejects invalid field names and returns proper business errors (e.g. insufficient balance when wallet mode/allocation does not cover request)
- **No live bank disbursement** executed (C30 / G40 policy)

## Secrets check

- [x] no keys in this file
- [x] `backend/.env` gitignored
- [x] callback signature computed in-process only; not logged

## Residual

- Live money canary still **G40 blocked** until human `GO LIVE CANARY`
- Production secret manager + HA still human (G10/G20)
