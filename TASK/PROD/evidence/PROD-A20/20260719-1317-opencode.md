# PROD-A20 evidence — Config: PAYMENT_PROVIDER + DISBURSEMENT_PROVIDER

| Field | Value |
| ----- | ----- |
| Task | PROD-A20 |
| Date | 2026-07-19 |
| Agent | opencode |
| Status | done |

## Summary

Implemented dual-provider config composition (ADR-0008):

- `PAYMENT_PROVIDER=fake|duitku|xendit` (xendit = temporary legacy QRIS until B40)
- `DISBURSEMENT_PROVIDER=fake|xendit`
- Duitku env fields loaded (names only; adapter not wired — B10)
- Legacy `XENDIT_MODE=fake|live` maps into the new provider fields when they are unset
- Fail-closed: production always rejects fake; staging rejects fake unless `ALLOW_FAKE_PROVIDERS=1`
- Composition (`wireMoneyProviders`) selects adapters per provider; `PAYMENT_PROVIDER=duitku` boot-errors until PROD-B10

## Files changed

- `backend/internal/config/config.go`
- `backend/internal/config/config_test.go`
- `backend/internal/app/app.go`
- `backend/internal/app/composition_test.go`
- `backend/.env.example`
- `TASK/PROD/09-EXECUTION-STATUS.md` (board)

## Legacy mapping

| Input | Result |
| ----- | ------ |
| `XENDIT_MODE=fake` (or empty defaults), providers unset | payment=fake, disbursement=fake |
| `XENDIT_MODE=live`, providers unset | payment=xendit, disbursement=xendit (requires Xendit keys) |
| Explicit `PAYMENT_PROVIDER` / `DISBURSEMENT_PROVIDER` | wins over `XENDIT_MODE` |
| After resolve | `XenditMode=fake` only if **both** providers fake; else `live` |
| `UseFakeXendit()` | deprecated compat: `UseFakePayment() && UseFakeDisbursement()` |

## Duitku until B10

`PAYMENT_PROVIDER=duitku` loads config (requires `DUITKU_MERCHANT_CODE` + `DUITKU_API_KEY`) but composition returns:

`app: PAYMENT_PROVIDER=duitku requires Duitku adapter (PROD-B10 not landed)`

Local default remains `PAYMENT_PROVIDER=fake` / `DISBURSEMENT_PROVIDER=fake`.

## Tests

```bash
export PATH="$HOME/.local/go/bin:$PATH"
cd backend && go test ./internal/config/ ./internal/app/ -count=1
```

```
ok  	github.com/dasepmoch/fersaku-new/backend/internal/config	0.004s
ok  	github.com/dasepmoch/fersaku-new/backend/internal/app	0.003s
```

## Acceptance

- [x] `go test` config package green
- [x] `go test` app package green
- [x] `.env.example` lists names only (no secrets)
- [x] Composition selects adapters by config; duitku fails closed until B10
- [x] Staging/production reject fake (staging drill flag supported; production never)
- [x] Evidence + board updated

## Non-goals (not done)

- Duitku HTTP adapter (PROD-B10)
- Unwire Xendit QRIS primary (PROD-B40)
- UI changes
