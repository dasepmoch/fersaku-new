# KEY-31 — API production env fail-closed

| Field | Value |
|-------|--------|
| Date | 2026-07-19 |
| Agent | @opencode |
| Status | **done** (contract + unit verified; prod inject = ops) |
| Depends | KEY-10, KEY-21 |

---

## 1. Mandatory production API env (names)

| Variable | Production |
|----------|------------|
| `APP_ENV` | `production` |
| `PAYMENT_PROVIDER` | `duitku` (**never** `fake`) |
| `DISBURSEMENT_PROVIDER` | `xendit` (**never** `fake`) |
| `DUITKU_ENV` | `sandbox` until KEY-62 → then `production` |
| `XENDIT_ENV` | `sandbox` until KEY-62 → then `production` |
| `MAIL_MODE` | `smtp` (KEY-14) |
| `KYC_ENCRYPTION_KEY` | required (KEY-21) |
| `SESSION_SECRET` / `CSRF_SECRET` | ≥32, non-placeholder |
| `DATABASE_URL` | managed PG + TLS (no `sslmode=disable`) |
| `REDIS_URL` | managed Redis + TLS |
| `R2_*` | real R2 (no MinIO host) |

## 2. Unit verification

```text
go test ./internal/config/ -run Production -v
```

| Test | Result |
|------|--------|
| Rejects fake Xendit / payment / disbursement | PASS |
| Rejects fake even with ALLOW flag | PASS |
| Requires session secret; rejects local placeholder | PASS |
| Rejects non-TLS Redis | PASS |
| Rejects local MinIO R2 endpoint | PASS |
| Rejects noop mail | PASS |
| Accepts hardened config | PASS |

## 3. Runtime smoke (demo host — not production)

| Check | Result |
|-------|--------|
| `/health/live` | 200 (prior E2E) |
| `/health/ready` | 200 |
| `POST /v1/webhooks/duitku` `{}` | 401 (mounted) |
| `PAYMENT_PROVIDER` | duitku |
| `DISBURSEMENT_PROVIDER` | xendit |

## 4. Deploy checklist (ops)

```text
[ ] Image immutable; secrets from SM only (KEY-10)
[ ] APP_ENV=production on prod process only
[ ] Fake providers impossible (config tests + review)
[ ] After deploy: health + duitku webhook mount (401 empty body)
[ ] Key-swap (DUITKU_ENV/XENDIT_ENV + live keys) only at KEY-62
```

## 5. Acceptance

- [x] Fail-closed matrix documented  
- [x] Production unit tests green  
- [x] Webhook mount expectation documented  
- [x] Residual: real prod inject + KEY-11..14 platform  

## Secrets check

- [x] no secrets
