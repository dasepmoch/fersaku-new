# PROD-E20 — Webhook security matrix

| Field | Value |
| ----- | ----- |
| Task | PROD-E20 |
| Date | 2026-07-19 |
| Agent | @nikki/opencode |
| Status | **done** |

## Automated matrix (all PASS)

### Duitku payment callback (unit + handler)

| Case | Test | Result |
| ---- | ---- | ------ |
| Invalid signature | `TestHandleDuitkuIngress_InvalidSignatureRejects` | PASS → 401 `RejectInvalidSignature` |
| Missing signature | `TestHandleDuitkuIngress_MissingSignatureRejects` | PASS → 401 `RejectMissingSignature` |
| Merchant mismatch | `TestHandleDuitkuIngress_MerchantMismatchRejects` | PASS → 401 |
| Accept + duplicate idempotent | `TestHandleDuitkuIngress_AcceptAndDuplicate` | PASS |
| Form-encoded body | `TestHandleDuitkuIngress_FormEncoded` | PASS |
| Handler bad sig | `TestDuitkuWebhook_BadSignature_401` | PASS |
| Handler good sig | `TestDuitkuWebhook_GoodSignature_200OK` | PASS |
| Sandbox path mode | `TestDuitkuWebhook_SandboxPathMode` | PASS |

### Xendit payment callback security

| Case | Test | Result |
| ---- | ---- | ------ |
| Oversize body | `TestHandleIngress_OversizeRejects` | PASS |
| Invalid token | `TestHandleIngress_InvalidTokenRejects` | PASS |
| Missing token | `TestHandleIngress_MissingTokenRejects` | PASS |

### Xendit disbursement webhook (handler)

| Case | Test | Result |
| ---- | ---- | ------ |
| Missing token | `TestDisbursementWebhook_MissingToken` | PASS |
| Invalid token | `TestDisbursementWebhook_InvalidToken` | PASS |
| Empty configured token fail-closed | `TestDisbursementWebhook_EmptyConfiguredToken_FailClosed` | PASS |
| Oversize body | `TestDisbursementWebhook_OversizeBody` | PASS |
| Empty body | `TestDisbursementWebhook_EmptyBody` | PASS |
| Malformed JSON | `TestDisbursementWebhook_MalformedJSON` | PASS |
| Missing ref | `TestDisbursementWebhook_MissingRef` | PASS |
| Happy completed | `TestDisbursementWebhook_HappyCompleted` | PASS |
| Failed / pending / unknown map | `TestDisbursementWebhook_FailedStatusMapped`, `…PendingAndUnknownMapped` | PASS |
| Replay idempotent | `TestDisbursementWebhook_ReplayIdempotentAck` | PASS |
| Not found quarantine 200 | `TestDisbursementWebhook_NotFoundQuarantine200` | PASS |
| Apply error 5xx | `TestDisbursementWebhook_ApplyError5xx` | PASS |
| Bearer token | `TestDisbursementWebhook_BearerTokenAccepted` | PASS |
| Nil applier | `TestDisbursementWebhook_NilApplier` | PASS |

### Integration callbacks (Postgres)

| Case | Result |
| ---- | ------ |
| `TestCallback_InvalidToken_RejectionOnly` | PASS |
| `TestCallback_MissingToken_Rejection` | PASS |
| `TestCallback_DuplicatePaid_SingleEffect` | PASS |
| `TestCallback_LatePaidAfterExpire` | PASS |
| `TestCallback_CrossPaymentMode_NoCollision` | PASS |

Commands:

```bash
export GOTOOLCHAIN=auto
go test ./internal/application/ -count=1 -timeout 120s -run 'HandleDuitku|HandleIngress'
go test ./internal/adapters/http/handlers/ -count=1 -timeout 120s -run 'DisbursementWebhook|DuitkuWebhook'
export DATABASE_URL=postgres://fersaku:fersaku_local@localhost:5433/fersaku?sslmode=disable
go test -tags=integration -count=1 -timeout 400s ./test/integration/ -run 'TestCallback'
```

## Live local HTTP matrix (compose API :18080)

| Probe | HTTP | Note |
| ----- | ---- | ---- |
| POST `/v1/webhooks/xendit/disbursement` no token | 401 | `missing_token` |
| POST disbursement oversize (~270KB) | 413 | `oversize` |
| POST `/v1/webhooks/xendit` no token | 401 | fail-closed |
| GET/PUT/POST `/v1/webhooks/duitku` | **404** | **Running image `fersaku-api:local` predates Duitku routes** (source has routes; needs rebuild — same residual as B50) |

Unit/handler tests prove Duitku matrix against current code; live route 404 is deploy lag, not missing tests.

## Raw payload logging

| Check | Result |
| ----- | ------ |
| `callback_service.go` comment/policy | “never log token, signature, api key, or raw body” |
| `handlers/callbacks.go` / `handlers/withdrawals.go` Log of body | **no** Log/slog of raw body found |
| Digest only | `payments.DigestBody` (SHA-256) for storage identity |

## Admin replay

Inbound provider IDs rejected on outbound admin retry path: covered by webhook integration suite (prior C10/webhooks). Permission-gated admin ops remain.

## Secrets check

- [x] no tokens/keys/payloads in this file
