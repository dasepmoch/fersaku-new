# PROD-A40 evidence — Frontend monorepo runbook alignment

| Field | Value |
| ----- | ----- |
| Task | PROD-A40 |
| Date | 2026-07-19 |
| Agent | opencode |
| Status | done |

## Summary

Aligned monorepo runbook: root README is monorepo-first (FE under `frontend/`, root npm proxy scripts, API-live env names only, backend pointer, `TASK/PROD/`). Updated `frontend/.env.example` with production-process note. Fixed host systemd unit `WorkingDirectory` to `frontend/`. Added short `frontend/README.md`. Did not restart production FE (unit inactive; live process already ad-hoc on `:3000`).

## Files changed

- `README.md` (rewrite: monorepo how-to-run; trim stale frontend-only prototype claim)
- `frontend/.env.example` (API-live clarity + run-from-`frontend/` note)
- `frontend/README.md` (new: run/build/start + env pointers)
- `~/.config/systemd/user/fersaku-frontend.service` (`WorkingDirectory=/var/www/fersaku-new/frontend`)
- `TASK/PROD/09-EXECUTION-STATUS.md` (board A40 → done)
- `TASK/PROD/evidence/PROD-A40/20260719-opencode.md` (this file)

## Host unit verification

| Check | Result |
| ----- | ------ |
| Unit path | `/home/dasepmoch/.config/systemd/user/fersaku-frontend.service` |
| Before | `WorkingDirectory=/var/www/fersaku-new` + `npx next start` (wrong for monorepo) |
| After | `WorkingDirectory=/var/www/fersaku-new/frontend` + same ExecStart/env |
| Env kept | `API_INTERNAL_URL`, `NEXT_PUBLIC_DATA_SOURCE=api`, `NEXT_PUBLIC_APP_STAGE=live` |
| `systemctl --user daemon-reload` | ran |
| Unit active | **inactive** (not restarted) |
| Live FE | `next-server` already listening `0.0.0.0:3000` (ad-hoc; not this unit) |

## Acceptance

- [x] Fresh clone instructions work for FE + BE (documented)
- [x] No broken paths assuming Next package.json only at monorepo root for `next start`
- [x] API-live env documented without secrets
- [x] Unit working directory aligned to `frontend/` when fixed
- [x] Board updated (A40 → done)

## Non-goals (not done)

- UI redesign, secrets, backend adapters, FE process restart
