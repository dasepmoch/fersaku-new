# PROD-F50 — Mock mode regression

| Field | Value |
| ----- | ----- |
| Task | PROD-F50 |
| Date | 2026-07-19 |
| Agent | @nikki/opencode |
| Status | **done** for critical mock paths; full unit suite residual noted |

## Critical dual-mode paths (PASS)

```bash
cd frontend
export PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH"
npm run test:run -- --reporter=dot \
  tests/unit/domain-source.test.ts \
  tests/unit/env.test.ts \
  tests/unit/csrf.test.ts \
  tests/unit/aut-100-seller-auth.test.ts \
  tests/unit/aut-120-auth-ceremony.test.ts \
  tests/unit/chk-100-checkout-quote.test.ts \
  tests/unit/chk-110-checkout-intent.test.ts \
  tests/unit/chk-120-checkout-poll.test.ts \
  tests/unit/chk-130-order-result.test.ts \
  tests/unit/domain-flags.test.ts
# Test Files  10 passed | Tests  148 passed
```

Also: `domain-source` + `env.test` → 28/28 earlier.

## Full `tests/unit` snapshot

| Metric | Value |
| ------ | ----- |
| Passed files | 88 |
| Failed files | 2 |
| Passed tests | 1152 |
| Failed tests | 2 |

### Pre-existing / non-blocking failures (not introduced this phase)

| Test | Symptom |
| ---- | ------- |
| `buy-130-buyer-sessions.test.ts` | relative time string expected `Sekarang` vs `44 jam lalu` (clock-sensitive) |
| `int-170-error-mock-observability.test.ts` | architecture grep false positive on catalog mock helper comment/pattern |

Neither blocks DATA_SOURCE=mock checkout/auth dual-mode acceptance for PROD-F50.

## Playwright mock e2e

Not re-run full Playwright smoke this session (no UI freeze break; vitest critical paths used). Optional follow-up: `npm run test:e2e:smoke` with mock config when browser deps available.

## Secrets check

- [x] no secrets
