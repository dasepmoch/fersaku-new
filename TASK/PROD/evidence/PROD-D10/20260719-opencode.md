# PROD-D10 evidence — FE env + domain source for staging

| Field | Value |
| ----- | ----- |
| Task | PROD-D10 |
| Date | 2026-07-19 |
| Agent | @nikki/opencode |
| Status | done |

## Summary

Confirmed API-live FE env docs, live-stage mock rejection (domain-source), and systemd `WorkingDirectory=frontend/`. No code changes required (A40 already aligned).

## Acceptance

| Check | Result |
| ----- | ------ |
| `frontend/.env.example` API-live docs | Pass — `NEXT_PUBLIC_DATA_SOURCE=api`, `NEXT_PUBLIC_APP_STAGE=live`, `API_INTERNAL_URL`, same-origin `/v1`, run from `frontend/` |
| Live stage rejects bootstrap mock | Pass — `evaluateDomainSources` throws when `stage=live` + `bootstrap=mock` without overrides; residual mock → `disabled` |
| domain-source unit tests | **13/13 passed** (`vitest run domain-source`) |
| env unit tests | **15/15 passed** (`vitest run env.test`) |
| systemd WorkingDirectory | Pass — `~/.config/systemd/user/fersaku-frontend.service` → `/var/www/fersaku-new/frontend` |
| Unit env (process) | `NEXT_PUBLIC_DATA_SOURCE=api`, `NEXT_PUBLIC_APP_STAGE=live`, `API_INTERNAL_URL=http://127.0.0.1:18080` |

## Runbook snippet (API-live FE)

```bash
# From monorepo: work in frontend/
cd /var/www/fersaku-new/frontend

# Build-time public env (also restated in unit):
# NEXT_PUBLIC_DATA_SOURCE=api
# NEXT_PUBLIC_APP_STAGE=live
# Leave NEXT_PUBLIC_API_URL unset (same-origin /v1 rewrite)

# Server-only SSR/rewrite target:
# API_INTERNAL_URL=http://127.0.0.1:18080   # host → compose :8080

npm run build
npm run start   # or: systemctl --user start fersaku-frontend.service
# WorkingDirectory MUST be frontend/ (not monorepo root)
```

Live fail-closed: domain-source never serves mock fixtures when stage is live; mock bootstrap without overrides is a config error.

## Commands

```text
export PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH"
cd /var/www/fersaku-new/frontend
npm run test:run -- --reporter=dot domain-source   # 13 passed
npm run test:run -- --reporter=dot env.test        # 15 passed
```

## Non-goals

- UI redesign, secrets, FE process restart
