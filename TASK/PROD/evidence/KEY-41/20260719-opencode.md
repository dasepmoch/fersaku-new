# KEY-41 — Xendit disbursement webhook + sandbox

| Field | Value |
|-------|--------|
| Date | 2026-07-19 |
| Agent | @opencode |
| Status | **done** (webhook security + mount + UI; **no live bank push**) |
| Route | `POST /v1/webhooks/xendit/disbursement` |

---

## 1. Non-goals (honest)

- Did **not** create a real Xendit sandbox bank payout (needs verified destination + owner risk; KEY-42 optional).
- Did **not** claim LIVE disbursement.

## 2. Unit / handler matrix

```text
go test ./internal/adapters/http/handlers/ ./internal/adapters/xendit/ ./internal/application/ \
  -count=1 -run 'Disburse|Withdraw|Webhook|Xendit'
# ok (handlers, xendit, application)
```

Covers token fail-closed, COMPLETED/FAILED mapping, oversize, malformed, happy path (injectable applier).

## 3. Live route probes (local + public)

| Probe | Local `:18080` | Public `api.fersaku.net` |
|-------|----------------|---------------------------|
| POST disbursement, **no** token | **401** | **401** |
| POST disbursement, **bad** token | **401** | **401** |
| POST disbursement, **valid** token, unknown external_id | **200** `{"ok":true,"quarantined":true}` | **200** same |
| POST body missing ref (valid token) | **400** `missing_ref` | **400** |

Token used only in-process from container env; **not** written to evidence.

Payment webhook `/v1/webhooks/xendit` also fail-closed 401 without/with bad token (legacy; not primary payment path).

## 4. Headed UI (public FE Playwright)

| Step | Result |
|------|--------|
| Seller login | pass → `/dashboard` |
| `/dashboard/withdrawals` | pass (Xendit/saldo chrome) |
| `/dashboard/withdrawals/new` | pass (min Rp50k / bank form) |
| Admin login + `/admin/withdrawals` | pass |

Screenshots: `headed-*.png`.

## 5. Dashboard expectation (human residual)

| Field | Expected |
|-------|----------|
| Xendit disbursement callback URL | `https://api.fersaku.net/v1/webhooks/xendit/disbursement` |
| Callback token | matches `XENDIT_WEBHOOK_TOKEN` in SM |

Ops should confirm dashboard UI points here (screenshot no secrets).

## 6. Acceptance

- [x] Route mounted public + local  
- [x] Token fail-closed (401)  
- [x] Valid token accepted (quarantine for unknown ref — safe)  
- [x] Unit/handler matrix green  
- [x] Headed seller/admin withdrawal surfaces  
- [x] No bank push; no secrets in evidence  

## 7. Residuals

| Item | Task |
|------|------|
| Real sandbox bank COMPLETED/FAILED against known WD | KEY-42 optional |
| LIVE disbursement | KEY-62 only |

## Secrets check

- [x] no webhook token / secret key / bank numbers
