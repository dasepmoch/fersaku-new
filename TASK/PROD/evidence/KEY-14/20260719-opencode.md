# KEY-14 — Production SMTP (detailed)

| Field | Value |
|-------|--------|
| Date | 2026-07-19 |
| Agent | @opencode |
| Status | **done** for **fail-closed unit + Mailpit inventory + runbook**; real SMTP deliver **residual human** |

---

## 1. Deliverables

| Deliverable | Path |
|-------------|------|
| Runbook | `TASK/PROD/ops/05-smtp-mail-runbook.md` |

## 2. Code fail-closed

```text
go test ./internal/config/ -run 'StagingRejectsCaptureMail|ProductionRejectsNoopMail' -v
→ PASS
```

Staging/production require `MAIL_MODE=smtp` + host + from.

## 3. Local Mailpit

| Check | Result |
|-------|--------|
| `http://127.0.0.1:8025/api/v1/info` | up (v1.22.3) |
| Messages | 0 |
| Compose SMTP host | `mailpit:1025` |
| API `MAIL_MODE` | UNSET (local capture default path) |

## 4. Production residual

| Item | Status |
|------|--------|
| Real SMTP provider + DKIM/SPF | **ops** |
| Magic-link email to real inbox < 2 min | **ops** |
| Mailpit absent from prod deploy | **ops** |

## Secrets check

- [x] no SMTP passwords
