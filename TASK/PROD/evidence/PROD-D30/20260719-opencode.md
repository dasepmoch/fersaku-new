# PROD-D30 evidence — Seller withdrawal API path smoke

| Field | Value |
| ----- | ----- |
| Task | PROD-D30 |
| Date | 2026-07-19 |
| Agent | @nikki/opencode |
| Status | done |

## Summary

Traced seller withdrawal quote → confirm → list/detail to API clients under `sellerFinance` domain. Errors surface on existing form error chrome. No UI redesign; no code changes.

## Trace

| Feature | API client | Route |
| ------- | ---------- | ----- |
| Quote | `requestSellerWithdrawalQuote` | `POST /v1/stores/{id}/withdrawal-quotes` |
| Confirm | `createSellerWithdrawal` | `POST /v1/stores/{id}/withdrawals/` (`quoteId` only; MFA proof) |
| List | `listSellerWithdrawals` | `GET /v1/stores/{id}/withdrawals/` |
| Detail | `getSellerWithdrawal` | `GET /v1/stores/{id}/withdrawals/{id}` |
| Lock | `getSellerWithdrawalLock` | `GET /v1/stores/{id}/withdrawals/lock` |

Sources: `features/finance/api.ts`, UI `features/seller/screens/finance/withdrawal-form.tsx` + `withdrawals.tsx`.

Mock gated by `shouldUseMockFixtures("sellerFinance")`; live uses mappers (`mapWithdrawalQuoteDto`, `mapWithdrawalDto`, …).

## Error mapping (existing UI)

- Form: `error` state string → existing red banner in `withdrawal-form.tsx` (no new panels)
- Domain disabled: `DomainDisabledError` / `assertSellerFinanceEnabled`
- Quote freshness: `isSellerWithdrawalQuoteFresh` client UX; server still authoritative
- Lock: `getSellerWithdrawalLock` server-authoritative; `isSellerWithdrawalLockActive` gates submit chrome only

## Tests

```text
npm run test:run -- --reporter=dot sel-410-seller-withdrawal
# included in 59-pass batch with chk-110/120 + adm-310
```

## Acceptance

- [x] Quote → confirm → list/detail API path wired
- [x] 4xx-class errors use existing form error surface
- [x] Bank/lock server-authoritative
- [x] Smoke evidence (code + unit tests; no live bank push)

## Non-goals

- Live Xendit disbursement (C30 integration-only already done)
