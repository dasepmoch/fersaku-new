# PROD-D20 evidence — Checkout API path smoke (no mock paid)

| Field | Value |
| ----- | ----- |
| Task | PROD-D20 |
| Date | 2026-07-19 |
| Agent | @nikki/opencode |
| Status | done |

## Summary

Verified checkout API path: create intent → map QR fields → poll-only PAID. No client-side mark-paid under api domain. Mappers already accept Duitku-shaped `qrString` / `qrImageUrl`. No code changes.

## Path (api domain)

| Step | Implementation |
| ---- | -------------- |
| Create | `createCheckoutIntent` → `POST /v1/checkout/intents` (api-only; mock throws) |
| QR display | `mapCheckoutIntentDto` → `qrString` / `qrImageUrl` → `CheckoutQrisStep` |
| Status | `getCheckoutIntent` GET poll (`poll.ts`); only `PAID` advances success |
| Mock paid | `simulateCheckoutPayment` **mock-only**; throws if `!shouldUseMockFixtures("checkout")` |
| UI gate | `onPay` → `payApi` when `apiCheckout`, else `payMock` |

## Grep: simulate / mark paid

| Symbol | api-mode behavior |
| ------ | ----------------- |
| `simulateCheckoutPayment` | Throws on api domain |
| `payMock` / `setStep("paid")` timer | Only when `!apiCheckout` |
| `payApi` | Sets poll intent; never fakes paid |
| `isCheckoutIntentPaid` | `status === "PAID"` only |

No client route calls `/v1/checkout/simulate-payment` in product code (harness-only under `tests/e2e/api/helpers/callback.ts`).

## QR mapper (Duitku-shaped)

```ts
// features/commerce/checkout/mappers.ts
if (dto.qrString !== undefined) intent.qrString = dto.qrString;
if (dto.qrImageUrl !== undefined) intent.qrImageUrl = dto.qrImageUrl;
```

Schema: `shared/api/schemas.ts` — both nullable optional strings.

## Tests

```text
npm run test:run -- --reporter=dot chk-110 chk-120
# Test Files  2 passed | Tests  (subset of 59 with sel-410/adm-310)
# chk-110: simulate mock-only / throws on api; intent + QR mapping
# chk-120: poll abort does not mark paid; only PAID success
```

Targeted suite with D30/D40: **59 tests passed** (chk-110, chk-120, sel-410, adm-310).

## Acceptance

- [x] Cannot reach success without server PAID (api path)
- [x] QR fields mapped from API DTO
- [x] No client mark-paid under api mode
- [x] Evidence attached

## Non-goals

- Live sandbox PAID proof (B50 deferred); Playwright full e2e not run (unit smoke only)
