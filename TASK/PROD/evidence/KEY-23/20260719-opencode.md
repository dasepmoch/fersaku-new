# KEY-23 — Kill demo seed personas from public surfaces

| Field | Value |
|-------|--------|
| Date | 2026-07-19 |
| Agent | @opencode |
| Status | **done** |
| Depends | KEY-20 |

---

## 1. Problem

Public marketing linked to **prototype demo storefront** `/@asep-ai-tools` and footer “Toko demo” even when FE runs `DATA_SOURCE=api`. Seed personas (`seed.fersaku.test`) are nonprod-only; production must not depend on them for marketing CTAs.

Featured products already use API when `publicCatalog=api` (`listFeaturedProducts` → `/v1/public/products/featured`); mock fixtures only via `shouldUseMockFixtures`.

## 2. Changes

| File | Change |
|------|--------|
| `frontend/app/page.tsx` | Hero secondary CTA “Lihat toko demo” only when `getDomainSource("publicCatalog") === "mock"`; hidden in api/live |
| `frontend/components/footer.tsx` | “Toko demo” link only when publicCatalog is mock |

**Unchanged (correct):**
- Catalog adapters still mock-gated via domain-source.
- E2E helpers may use seed emails (test-only under `tests/e2e`).
- Backend seed refused on production (KEY-20).

## 3. Acceptance

- [x] Api mode: no public CTA to `/@asep-ai-tools` on home/footer  
- [x] Mock mode: demo links retained (prototype)  
- [x] Featured path remains API for api mode  
- [x] UI freeze: same button classes; secondary CTA omitted (not redesigned) when api  

## 4. Residuals

| Item | Note |
|------|------|
| Seed rows may still exist in nonprod DB | OK; KEY-20 blocks seed binary on production |
| Featured API may return empty on prod until real merchants | Empty marketing grid OK |

## Secrets check

- [x] no secrets
