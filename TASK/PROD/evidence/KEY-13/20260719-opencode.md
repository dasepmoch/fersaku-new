# KEY-13 — R2 production / no MinIO (detailed)

| Field | Value |
|-------|--------|
| Date | 2026-07-19 |
| Agent | @opencode |
| Status | **done** for **fail-closed unit + local MinIO inventory + runbook**; real R2 **residual human** |

---

## 1. Deliverables

| Deliverable | Path |
|-------------|------|
| Runbook | `TASK/PROD/ops/04-r2-object-storage-runbook.md` |

## 2. Code fail-closed

```text
go test ./internal/config/ -run ProductionRejectsLocalMinIO -v
→ PASS
```

Production rejects local/MinIO-style `R2_ENDPOINT`.

## 3. Local MinIO (demo only)

| Bucket | Present |
|--------|---------|
| `fersaku-private` | yes |
| `fersaku-public` | yes |

Endpoint in API container: `http://minio:9000` — **forbidden** when `APP_ENV=production`.

## 4. Production residual

| Item | Status |
|------|--------|
| Cloudflare R2 buckets + API tokens in SM | **ops** |
| Staging upload/download smoke | **ops** |
| KYC encrypt stream with KEY-21 keys | **ops** |

## Secrets check

- [x] no R2 keys (local minioadmin not copied as a secret recommendation)
