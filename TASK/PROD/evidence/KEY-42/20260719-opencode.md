# KEY-42 — C30b sandbox bank disbursement proof

| Field | Value |
|-------|--------|
| Date | 2026-07-19 |
| Agent | @opencode |
| Status | **done** |
| Mode | **Xendit SANDBOX only** — no LIVE bank, no customer funds |
| Store | seed store A (nonprod) |

---

## 1. Scope

Prove end-to-end:

1. Create bank account (encrypt with current key)  
2. Quote Rp50.000  
3. Create withdrawal → Xendit create disbursement (sandbox)  
4. Inbound disbursement webhook → **COMPLETED**  
5. Replay webhook idempotent  

---

## 2. Learning / residual from first attempt

| Attempt | Result |
|---------|--------|
| Quote + WD using **seed** bank `…141` | WD created **PROCESSING** but log: `Decrypt bank failed` — seed ciphertext not decryptable with current `STOCK`/`KYC` keys |
| Fix | Create **new** bank via API (re-encrypt with live keys) → verify → make primary |

This validates KEY-21: encryption keys must be stable; seed bank rows are not portable across key material.

---

## 3. Successful chain

| Step | Result | IDs (non-secret) |
|------|--------|------------------|
| Create bank | 201 → verify VERIFIED | `bank_01KXXB1DYGA9134X7FKYA3RB7H` · masked `****7890` · BCA |
| Quote | 201 ACTIVE | `wq_01KXXB1DZNCJ0YACH0K7SENMG1` · debit 50000 · net 46000 · fees 4000 |
| Create withdrawal | 201 PROCESSING | `wd_01KXXB1E02KDH9H74V4ZNDT5NQ` · mode **SANDBOX** |
| Xendit create | HTTP 200 (logs) | provider ref set (value not copied to prose beyond redacted id in final.json) |
| Webhook COMPLETED (local + public) | **200** `{"ok":true}` | status → **COMPLETED** |
| Replay webhook | 200 | status still **COMPLETED** |

Amounts:

- Debited: **50000** IDR  
- Net disbursement: **46000** IDR  
- Platform fee: 1500 · provider fee quoted: 2500  

## 4. API log evidence (redacted)

```text
xendit response POST /v2/… status=400 (probe path)
xendit response POST /disbursements status=200 latency~193ms
POST /v1/stores/.../withdrawals status=201
webhook apply → COMPLETED
```

## 5. Acceptance

- [x] Sandbox withdrawal created against Xendit  
- [x] Provider reference stored  
- [x] Webhook COMPLETED applied  
- [x] Replay safe  
- [x] No LIVE env / no real customer bank  
- [x] No secrets / full account numbers in evidence (masked only)  

## 6. Residuals

| Item | Note |
|------|------|
| Seed bank decrypt failure | Expected if keys rotated after seed; use API-created banks |
| LIVE disbursement | KEY-62 only |
| Human dashboard confirm | KEY-41 residual |

## Secrets check

- [x] no Xendit secret/token  
- [x] no full account number (only `****7890`)  
- [x] provider ref only as opaque id in `final.json`
