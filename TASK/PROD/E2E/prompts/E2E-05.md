# OpenCode prompt — E2E-05

Follow `TASK/PROD/E2E/prompts/OPENCODE-RUN-ONE-CASE.md` with:

- CASE_ID = `E2E-05`
- CASE_FILE = `E2E-05-CHECKOUT-QRIS-PENDING.md`

Checkout QRIS pending UI + POST /v1/checkout/intents provider=DUITKU.
Real browser unless case says shell-only. Update STATUS + evidence. One case only.
