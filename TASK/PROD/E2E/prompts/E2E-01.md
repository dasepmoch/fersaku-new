# OpenCode prompt — E2E-01 Public landing + storefront

Follow `TASK/PROD/E2E/prompts/OPENCODE-RUN-ONE-CASE.md` with:

- CASE_ID = `E2E-01`
- CASE_FILE = `E2E-01-PUBLIC-LANDING-STOREFRONT.md`

Use real browser (Playwright preferred). Discover storefront URL for slug `seed-store-a` if not obvious.
Screenshots required. Update STATUS + evidence.
