# E2E-06 evidence

| Field | Value |
|-------|--------|
| Date | 2026-07-19 |
| Agent | @nikki |
| Result | **pass** |

## Steps

| Step | Result | Notes |
|------|--------|-------|
| Intent from E2E-05: `pi_01KXX55HRX2F37ERPRRHVNVPH1` | pass | PENDING · DUITKU |
| Build Duitku callback signature (MD5) | pass | merchantCode + amount + merchantOrderId + apiKey |
| `POST /v1/webhooks/duitku` (resultCode=00) | pass | **200 OK** |
| Intent status after callback | pass | **PAID** |
| Settlements | pass | **1** row POSTED |
| Browser redirect to success page | pass | `/orders/ORD-PRRHVNVPH0/success?total=50000` |
| UI followed server PAID (no client mock) | pass | FE polling detected PAID → auto-redirected |

## Full E2E chain proven

```
Browser checkout → POST /v1/checkout/intents (201, DUITKU)
→ Duitku sandbox QRIS create (provider ref)
→ Signed webhook callback (200 OK)
→ Intent PAID → settlement POSTED
→ FE polling detects PAID → redirect to /orders/{id}/success
```

## Secrets check

- [x] no keys/cookies
