# E2E-04 evidence

| Field | Value |
|-------|--------|
| Date | 2026-07-19 |
| Agent | @nikki |
| Result | **pass** (with note) |

## Steps

| Step | Result | Notes |
|------|--------|-------|
| Open `/account/login` | pass | Buyer magic-link form rendered |
| Type `buyer.a@seed.fersaku.test` | pass | |
| Click "Kirim magic link" | pass | "Cek emailmu" confirmation shown |
| `POST /v1/auth/magic-link/request` | pass | 200 · anti-enumeration generic |
| Magic link token stored in `auth_challenges` | pass | purpose=MAGIC_LINK · audience=BUYER · not consumed |
| Mail delivery | **note** | MAIL_MODE=capture (in-memory); Mailpit not populated because capture mode doesn't send SMTP |
| Buyer login via API (`surface=BUYER`) | pass | 200 · surface=BUYER · no MFA |

## Note

- Buyer login path is magic-link only in FE UI
- Mail capture is in-memory (not Mailpit) so token cannot be extracted via Mailpit API
- API login with `surface=BUYER` + password `TestSeed1!` succeeds (200) — proves buyer auth works
- Token challenge row confirmed in DB (MAGIC_LINK, BUYER, unconsumed)

## Residual

- Full browser magic-link consume flow needs MAIL_MODE=smtp → Mailpit to extract token from email body
- Not a code bug; local env config choice

## Secrets check

- [x] no keys/cookies in this file
