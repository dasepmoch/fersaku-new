# PROD-A40 — Frontend monorepo runbook alignment

You are executing **exactly one task**: `PROD-A40` in `/var/www/fersaku-new`.

## Read

- `TASK/PROD/02-PHASE-A-FOUNDATION-STAGING.md` (PROD-A40)
- Root `README.md` (currently has monorepo header then long stale “frontend-only prototype” section)
- Root `package.json` (proxies to `frontend/`)
- `frontend/package.json`, `frontend/.env.example`
- Host unit (read + may fix): `~/.config/systemd/user/fersaku-frontend.service`
  - Today: `WorkingDirectory=/var/www/fersaku-new` + `npx next start` — **wrong** after monorepo move; Next lives under `frontend/`. Prefer:
    - `WorkingDirectory=/var/www/fersaku-new/frontend`
    - `ExecStart=.../npx next start -H 0.0.0.0 -p 3000`
    - Keep env: `API_INTERNAL_URL`, `NEXT_PUBLIC_DATA_SOURCE=api`, `NEXT_PUBLIC_APP_STAGE=live` for live host
  - Do **not** restart production FE unless needed for verification; if you change the unit file, run `systemctl --user daemon-reload` only. Mention that live process may already be ad-hoc on :3000.

## STRICT

- No UI redesign. No secrets. No backend adapter work.
- Docs + env example + unit path alignment only.
- Only PROD-A40.

## Do

1. **Root README.md** — rewrite the top “how to run” so a fresh clone works:
   - Layout: `frontend/`, `backend/`, `TASK/`, `docs/`
   - FE: `cd frontend && npm install && npm run dev|build|start` **or** root `npm run dev|build|start` (prefix scripts)
   - API-live env (names only): `NEXT_PUBLIC_DATA_SOURCE=api`, `NEXT_PUBLIC_APP_STAGE=live`, `API_INTERNAL_URL=http://127.0.0.1:18080` (server-only), same-origin `/v1` (leave `NEXT_PUBLIC_API_URL` empty)
   - Backend: `cd backend` + point to `backend/README.md` / compose
   - Point production readiness to `TASK/PROD/`
   - You may keep a shorter “product surfaces” summary; **trim or reframe** the huge outdated “frontend-only / mock only” claim so it doesn’t contradict monorepo + API mode
2. **frontend/.env.example** — ensure API-live block is clear (already mostly good); add one-line note that production process must run from `frontend/` after `npm run build`
3. **Fix systemd unit** path to `frontend/` if still root (file under `~/.config/systemd/user/fersaku-frontend.service`)
4. Optional: short `frontend/README.md` (new) with run/build/start + env pointers if root README stays long — only if helpful
5. Evidence `TASK/PROD/evidence/PROD-A40/20260719-opencode.md` with checklist results
6. Board A40 → done

## Acceptance

- [ ] Fresh clone instructions work for FE + BE (documented)
- [ ] No broken paths assuming Next package.json only at monorepo root for `next start`
- [ ] API-live env documented without secrets
- [ ] Unit working directory aligned to `frontend/` when fixed

Print files changed + evidence path.
