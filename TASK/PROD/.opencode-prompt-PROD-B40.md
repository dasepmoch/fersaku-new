# PROD-B40 — Unwire Xendit as payment (QRIS) primary

You are executing **exactly one task**: `PROD-B40` in `/var/www/fersaku-new`.

## Read

- `TASK/PROD/03` B40
- `backend/internal/app/app.go` wireMoneyProviders + paymentIntentIdentity
- Xendit payment webhook handler (keep mounted for late events / option B optional)
- Docs that say “no Duitku” / Xendit-only payment: `backend/docs/launch/e2e-acceptance.md`, `secrets-matrix.md`, security scan scripts if any
- ADR-0008 already accepted

## STRICT

- Prefer option **A**: keep Xendit QRIS code, unreferenced when `PAYMENT_PROVIDER=duitku`.
- Disbursement Xendit **must** keep working.
- Do not delete Xendit QRIS adapter code.
- No secrets. No UI redesign.
- Only B40.

## Do

1. Confirm composition:
   - `payment=duitku` → Duitku QRIS only (already B10/B30)
   - `payment=xendit` → legacy Xendit QRIS allowed (local/transition)
   - `payment=fake` → xendit.Fake for local
   - disbursement independent
2. Optionally add readiness/log message distinguishing payment vs disbursement providers.
3. Docs hygiene:
   - Update any launch/security docs that forbid Duitku payment to match ADR-0008
   - Note Xendit payment webhook remains for historical/late events but is not primary create path when payment=duitku
4. If a security_scan greps forbidding Duitku package: invert so `adapters/duitku` is expected; do not ban Duitku.
5. Unit test: payment=duitku does not call Xendit CreateQRIS (composition already selects Duitku — add assert on wireMoneyProviders kind strings).
6. Evidence `TASK/PROD/evidence/PROD-B40/20260719-opencode.md` stating option A
7. Board B40 → done

## Acceptance

- [ ] No production path creates Xendit QRIS when PAYMENT_PROVIDER=duitku
- [ ] Disbursement still Xendit-capable
- [ ] Docs/ADR-0008 consistency
- [ ] Evidence

Print files changed + evidence path.
