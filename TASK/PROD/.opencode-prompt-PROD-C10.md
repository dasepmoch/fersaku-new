# PROD-C10 — Harden Xendit disbursement adapter + webhook

You are executing **exactly one task**: `PROD-C10` in `/var/www/fersaku-new`.

## Read

- `TASK/PROD/04-PHASE-C-XENDIT-DISBURSEMENT.md` (C10)
- `backend/internal/adapters/xendit/client.go` (Quote/Create/Get disbursement)
- `backend/internal/adapters/http/handlers/withdrawals.go` (`DisbursementWebhook`)
- `backend/internal/adapters/http/handlers/disbursement_webhook_test.go`
- `backend/internal/application/withdrawal_service.go` (create + webhook apply)
- `backend/test/integration/withdrawals_test.go`
- Config fail-closed for empty webhook token on staging/prod (already A20 — verify)

## STRICT

- Only C10. No FE UI. No secrets. Disbursement stays Xendit (ADR-0008).
- Payment Duitku path must not break.
- `export PATH="$HOME/.local/go/bin:$PATH"`

## Do

1. Audit real Xendit client: quote/create/get disbursement paths, error classes, idempotency header.
2. Harden webhook if gaps: bounded body, constant-time token, empty token reject on live, replay safety.
3. Ensure status mapping COMPLETED/FAILED/PENDING/UNKNOWN documented + tested.
4. Add/strengthen unit tests for adapter + handler (happy path can use fake service).
5. Integration tests: if DATABASE_URL available run withdrawals integration; else unit only and note honestly.
6. Evidence `TASK/PROD/evidence/PROD-C10/20260719-opencode.md`
7. Board C10 → done

## Acceptance

- [ ] Happy + negative webhook coverage (unit and/or integration)
- [ ] Fail-closed empty token on staging/production (config or handler)
- [ ] Real client paths verified/tested with httptest
- [ ] Evidence

Print files changed, gaps fixed, test results, evidence path.
