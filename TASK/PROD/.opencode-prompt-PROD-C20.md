# PROD-C20 — Withdrawal application service audit

You are executing **exactly one task**: `PROD-C20` in `/var/www/fersaku-new`.

## Read

- `TASK/PROD/04` C20
- `backend/internal/application/withdrawal_service.go` (full flow)
- Fee policy ADR-0003 / domain fees
- Existing tests: domain fees, withdrawals integration, withdrawal_service tests
- Composition: Disburse port from wireMoneyProviders

## STRICT

- Only C20. No UI. No secrets. Prefer fix gaps over docs-only if real bugs found.
- Document sequence in evidence. Keep modular/clean.

## Do

1. Trace seller create withdrawal → quote TTL → create disbursement → webhook COMPLETED/FAILED.
2. Confirm fee: platform 3% + provider fee; min Rp50.000.
3. Confirm ledger holds/releases match states.
4. Admin review: permission + reason + idempotency (no MFA).
5. Fix any path that forces fake provider when config says xendit.
6. Ensure tests for min amount, insufficient balance, expired quote exist (add if missing).
7. Evidence with sequence diagram (text/mermaid).
8. Board C20 → done

## Acceptance

- [ ] Sequence diagram in evidence
- [ ] Tests for min amount, insufficient balance, expired quote
- [ ] No double disbursement on retry (idempotency)
- [ ] Evidence path set

```bash
export PATH="$HOME/.local/go/bin:$PATH"
cd backend
go test ./internal/application/ ./internal/domain/platform/ ./internal/domain/ledger/ ./internal/adapters/http/handlers/ -count=1
```

Print files changed, any bugs fixed, test results, evidence path.
