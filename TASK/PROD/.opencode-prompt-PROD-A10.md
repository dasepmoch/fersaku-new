# PROD-A10 — ADR-0008 dual provider (payment Duitku / disbursement Xendit)

You are executing **exactly one task**: `PROD-A10` in monorepo `/var/www/fersaku-new`.

## Read first (in order)

1. `TASK/PROD/README.md`
2. `TASK/PROD/00-DECISIONS-AND-NON-GOALS.md`
3. `TASK/PROD/01-PROVIDER-ARCHITECTURE.md`
4. `TASK/PROD/02-PHASE-A-FOUNDATION-STAGING.md` (section PROD-A10 only)
5. `TASK/PROD/11-AGENT-RUNBOOK.md`
6. `backend/docs/adr/ADR-0002-one-xendit-account.md` (to be partially superseded for payment ingress)
7. `backend/docs/DECISIONS.md`
8. Existing ADR style: `backend/docs/adr/ADR-0001-modular-monolith.md` and `ADR-0007-production-runtime-topology.md` for tone/structure

## STRICT CONSTRAINTS

- Implement **only PROD-A10**. Do NOT implement adapters, config code, Duitku HTTP, FE, or other PROD-* tasks.
- **No UI changes.** No secrets. Never paste values from `/var/www/pg.txt`.
- Do **not** rewrite/delete ADR-0002 body; ADR-0008 supersedes the **payment half** only and must state that clearly.
- Match existing ADR markdown style (table fields, Context, Decision, Consequences, References).
- English for ADR/docs (repo convention). Evidence can be English.
- No unrelated refactors. No git push unless asked (you may commit if needed; prefer leave files ready — commit is OK for this monorepo).

## Do (acceptance from phase file)

1. Create `backend/docs/adr/ADR-0008-duitku-payment-xendit-disbursement.md`
   - Status: **Accepted**
   - Date: **2026-07-19**
   - Task: PROD-A10 / PROD program
   - Explicitly supersedes ADR-0002 **for QRIS payment create/callback only**
   - Keep disbursement on **Xendit**
   - Money path: buyer pay QRIS → Duitku; seller withdraw → Xendit
   - Ports stay: `ports.QRISProvider` (Duitku impl later), `ports.DisbursementProvider` (Xendit)
   - Config direction: `PAYMENT_PROVIDER=duitku|fake`, `DISBURSEMENT_PROVIDER=xendit|fake`
   - Fail-closed on staging/production for fake providers
   - Explicit **non-goals**: no Duitku payout, no Xendit QRIS as primary after cutover, no multi-provider failover UI, no refund/dispute console in this program
   - Canonical identities as in TASK/PROD/01
   - Note auth policy: no MFA (password / magic-link only) only if relevant; do not reopen ADR-0004
2. Update `backend/docs/DECISIONS.md` index:
   - Add ADR-0008 row Accepted 2026-07-19
   - On ADR-0002 row, note payment half superseded by ADR-0008 (keep historical title)
   - Update footer: next work is PROD program / PROD-A20 not BE-002
3. Optional light touch only if needed for honesty: one-line cross-link in ADR-0002 header/status note that payment ingress is superseded by ADR-0008 — prefer minimal edit.
4. Evidence under `TASK/PROD/evidence/PROD-A10/20260719-1312-opencode.md` using template from `TASK/PROD/11-AGENT-RUNBOOK.md`
   - Record answers to Q1–Q4 from `00` §6 with defaults if owner silent:
     - Q1: use `DUITKU_QRIS_PAYMENT_METHOD` env (default SP from product matrix)
     - Q2: platform single merchant code
     - Q3: `api.fersaku.net` sandbox first
     - Q4: keep Xendit QRIS code unwired; delete only after canary stable
   - Secrets check checkboxes
5. Update `TASK/PROD/09-EXECUTION-STATUS.md`: PROD-A10 → `done`, owner `@nikki/opencode`, evidence path filled. Do not claim other tasks.

## Done criteria

- [ ] ADR-0008 file exists
- [ ] Linked from DECISIONS.md
- [ ] Explicit non-goals present
- [ ] Evidence file present with Q1–Q4
- [ ] Board A10 done
- [ ] No secrets in any file

## After completing

Print summary:
- Files created/changed
- Whether ADR-0002 was lightly annotated
- Evidence path
- Confirmation no adapters/config code changed
