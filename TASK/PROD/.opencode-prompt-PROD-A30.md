# PROD-A30 — Staging topology doc + tunnel checklist

You are executing **exactly one task**: `PROD-A30` in `/var/www/fersaku-new`.

## Read

- `TASK/PROD/02-PHASE-A-FOUNDATION-STAGING.md` (PROD-A30)
- `TASK/PROD/01-PROVIDER-ARCHITECTURE.md` HTTP surface
- `backend/docs/launch/topology.md` (append dual-provider section; do not destroy BE-630 HA content)
- Host tunnel config (read-only reference; do not modify credentials): `~/.cloudflared/fersaku.yml`
- Live host facts (already verified by orchestrator — use these, re-check if you want):
  - Tunnel YAML: `fersaku.net`/`www` → `http://localhost:3000`; `api.fersaku.net` → `http://localhost:18080`
  - Also temp hosts under `dasepmoch.dev`
  - API `/health/live` on `:18080` returns 200
  - FE process may be ad-hoc on `:3000` (user unit `fersaku-frontend.service` currently inactive) — document both unit + manual start
  - FE app lives in monorepo `frontend/` (not old root-only Next)
  - Callbacks must hit public HTTPS `https://api.fersaku.net/v1/webhooks/...`

## STRICT

- Docs/evidence only. No secrets. No code adapters.
- Do not commit tunnel credentials or token files.
- Only PROD-A30.

## Do

1. Create `TASK/PROD/evidence/PROD-A30/topology.md` with:
   - Text topology diagram for **this host staging/demo path** (Cloudflare tunnel → FE/API)
   - Host table: public hostname → local service → port → notes
   - Callback URLs (no secrets):
     - Duitku: `https://api.fersaku.net/v1/webhooks/duitku` (+ sandbox/live variants if listed in PROD/01)
     - Xendit disbursement: `https://api.fersaku.net/v1/webhooks/xendit/disbursement`
     - Legacy Xendit payment (no new traffic after cutover): `https://api.fersaku.net/v1/webhooks/xendit`
   - Checklist: tunnel ingress hosts correct after monorepo move; FE from `frontend/`; API compose host map 18080→8080
   - Note production HA still follows `backend/docs/launch/topology.md` (not tunnel-as-HA)
2. Amend `backend/docs/launch/topology.md` with a short **§ Dual-provider callbacks (PROD-A30)** section linking Duitku + Xendit disbursement routes and pointing to PROD evidence for host tunnel table.
3. Evidence summary file `TASK/PROD/evidence/PROD-A30/20260719-opencode.md`
4. Board: A30 → done

## Acceptance

- [ ] Topology diagram text + host table
- [ ] Callback URLs listed (no secrets)
- [ ] Board updated

Print files changed + evidence path when done.
