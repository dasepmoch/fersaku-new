# PROD Phase E + F — Security + Quality (agent-executable portions)

Execute remaining tasks in `/var/www/fersaku-new`. Mark human-blocked items honestly as `blocked`/`deferred` with residual notes — **never invent** live canary or full money PAID without proof.

## Phase E

### E10 Cookie/CSRF/HTTPS
- Curl/browser smoke against https://fersaku.net and https://api.fersaku.net if reachable
- Document Secure cookie, CSRF after refresh, login without MFA
- If live session cookies hard without credentials, use unit tests + local evidence + partial remote curl headers

### E20 Webhook security matrix
- Run existing Duitku + Xendit disbursement webhook tests
- Table in evidence: missing/invalid sig/token, oversize, wrong method
- Confirm no raw payload logging (grep Log calls)

### E30 Rate limit
- Review limiter code for login/checkout/webhook
- Session storm regression if exists
- Document Retry-After

### E40 Secret scan
```bash
bash backend/scripts/security_scan.sh
# grep evidence for secret-like patterns without printing secrets
```
Ensure pg.txt values not in TASK/PROD/evidence

Board each E* done with evidence.

## Phase F

### F10 Auth/session cell
- Run auth-related unit/integration tests; checklist no MFA
- If full browser login needs credentials, use automated tests + document residual

### F20 Checkout-order cell
- Prefer automated callback integration if DATABASE_URL set:
  `export DATABASE_URL=postgres://fersaku:fersaku_local@localhost:5433/fersaku?sslmode=disable`
- Prove Duitku path unit + any integration
- **Honest residual:** full PAID via live sandbox pay may still be deferred (B50)

### F30 Seller-finance cell
- Withdrawal unit tests + isolation notes
- Balance path documentation

### F40 Sandbox canary
- Write canary-sandbox.md adapted for Duitku+Xendit
- Execute **what is possible** without owner live money (dry-run rollback, health checks)
- Do not claim live money canary

### F50 Mock regression
- Run frontend mock e2e smoke or vitest critical paths with DATA_SOURCE=mock

## Rules
- UI freeze
- No secrets in evidence
- Update `09-EXECUTION-STATUS.md` for every task
- PATH: go + node from user local

Print final board summary for E+F.
