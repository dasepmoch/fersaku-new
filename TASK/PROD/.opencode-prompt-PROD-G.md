# PROD Phase G — Ops / owner-sign (agent prep only)

Execute agent-safe portions of Phase G in `/var/www/fersaku-new`.

## Hard rules

- **Do NOT execute G40 live canary** — no real money. Mark G40 `blocked` awaiting human “GO LIVE CANARY”.
- No secrets in any file. Values from `/var/www/pg.txt` never copied.
- Honest residuals only.

## G10 — Production secrets inventory

Create `TASK/PROD/evidence/PROD-G10/20260719-opencode.md` with table:

| Secret name | Purpose | Current host source | Target production storage | Present on host? (yes/no only) | Rotation note |
|-------------|---------|---------------------|---------------------------|--------------------------------|---------------|

Include all names from `TASK/PROD/10-SECRETS-AND-ENV-MATRIX.md` + dual-provider. Values redacted — only yes/no presence.

Confirm webhook URL expectations (names only): Duitku → `https://api.fersaku.net/v1/webhooks/duitku`, Xendit disbursement → `.../xendit/disbursement`.

Board G10 → done (inventory prepared; human fills secret manager locations if unknown).

## G20 — Infrastructure HA checklist

Create evidence checklist linking `backend/docs/launch/topology.md` + readiness docs. Each row: item, status (verified on this host demo / unknown for managed prod / N/A tunnel), owner (human ops), date.

Honest: this host is tunnel demo, not production HA multi-replica.

Board G20 → done as **checklist prepared** or `blocked` if you prefer human-only — prefer done with “demo host verified; managed prod TBD human”.

## G30 — Residual risk sign-off

1. Update `backend/docs/security/residual-risks.md` if exists for dual-provider Duitku payment + Xendit payout; remove any MFA-required language.
2. Evidence with residual table and sign-off row: **deferred until human signature** with date.
3. Board G30 → done (doc ready) or deferred for signatures.

## G40 — Live canary

**Status: blocked** — agent must not run. Evidence note waiting for explicit human GO.

## G50 — Program close

1. Finalize `09` board: accurate statuses for all tasks including residuals B50/F20/G40.
2. Ensure root README points to `TASK/PROD/` for production.
3. Evidence summarizing program state: what is code-complete vs owner-gated.

## After all

Print final board table A–G summary.
