# QLT-210 — Backend integration/database tests (parent framework)

- Status: **[x] complete** (parent/framework only)
- Owner: @nikki/opencode
- UI impact: **none**
- Depends on: QLT-100 `[x]` (CI skeleton); co-evolves with domain migrations
- Scope: Continuous harness + required CI + migrate/race foundation samples + co-evolution rule. **Not** full domain matrix (§3.7 cells remain `[ ]`).

## 1. Deliverables

| Item | Path |
| --- | --- |
| Co-evolution rule doc | `docs/QLT-210-INTEGRATION-COEVOLUTION.md` |
| Foundation: empty DB migrate | `TestMigrateUpFromZero` in `backend/test/integration/foundation_test.go` |
| Foundation: upgrade previous → head | `TestMigrateUpgradeFromSupportedPrevious` |
| Foundation: concurrent first-writer | `TestConcurrentIdempotencyFirstWriterWins` (real `WaitGroup` × 20) |
| Foundation: atomic multi-table rollback | `TestAtomicCommitRollbackOnOutboxFailure` |
| Local full suite (required env) | `backend/Makefile` → `make test-integration` |
| Local parent smoke | `make test-integration-foundation` |
| CI required job | `.github/workflows/backend-ci.yml` → `backend-integration` |
| Non-empty + parent marker assert | `scripts/ci-assert-suite.mjs` → `backend-integration` + `qlt-210-integration` |
| Zero-test package guard | `scripts/ci-assert-go-tests.sh` |
| Existing domain concurrent samples | inventory, coupons, withdrawals, audit, identity, domains, callbacks (WaitGroup) |

## 2. Parent vs cells

| Level | Claimed by this task |
| --- | --- |
| Parent `QLT-210` | Harness, CI gate, non-empty guards, migrate empty/upgrade, concurrent + atomic foundation, co-evolution doc |
| §3.7 `QLT-210 DB/integration` cells | **Remain `[ ]`** — domain canary still requires cell evidence |
| `07` coverage bullets (inventory/KYC/…) | Partial suites exist; full claim is per-domain cell, not parent |

Parent must never wait on descendant cells. Domain tasks extend `backend/test/integration/` in the same PR as BE schema/handler changes.

## 3. CI / no silent skip

- `DATABASE_URL` missing under `CI` or `QLT_REQUIRE_INTEGRATION=1` → `t.Fatal` (not Skip).
- Job: Postgres service → migrate up → full `go test -tags=integration` via `ci-assert-go-tests.sh`.
- Subset step requires ≥5 PASS including explicit:
 - `TestMigrateUpFromZero`
 - `TestMigrateUpgradeFromSupportedPrevious`
 - `TestConcurrentIdempotencyFirstWriterWins`
- Suite assert `qlt-210-integration` requires foundation markers, co-evolution doc, Makefile `QLT_REQUIRE_INTEGRATION`, ≥3 domain WaitGroup files.

## 4. Tests run (2026-07-17)

```text
node scripts/ci-assert-suite.mjs backend-integration
# → OK files=30

node scripts/ci-assert-suite.mjs qlt-210-integration
# → OK concurrent domain files=7 suite=30

cd backend && make test-integration-foundation
# → ok (TestMigrate*|TestConcurrentIdempotency|TestAtomicCommit|TestPoolPing)

go test -tags=integration -count=1 -timeout 10m \
 -run 'TestSecurity_|TestConcurrentIdempotency|TestMigrate|TestAtomicCommit' \
 ./test/integration/ -v
# → 14 PASS including:
# TestMigrateUpFromZero
# TestMigrateUpgradeFromSupportedPrevious
# TestConcurrentIdempotencyFirstWriterWins
# TestAtomicCommitRollbackOnOutboxFailure
# TestSecurity_* (10)
```

Disposable Postgres: compose `localhost:5433` / credentials `fersaku:fersaku_local` (ephemeral local only; no production secrets).

## 5. Checklist mapping (07 §QLT-210)

- [x] Tagged integration required CI and repeatable
- [x] Race/concurrency uses actual concurrent transactions (foundation + domain samples)
- [x] Migrations apply from empty DB
- [x] Upgrade supported previous schema (goto v1 → up; down 1 → up)
- [x] Co-evolution documented
- [ ] Full domain coverage matrix bullets — **capability cells** (§3.7)
- [ ] App-role immutable ledger/audit production separation drill — ops / domain cells
- [ ] Backup/PITR/restore rehearsal — outside unit CI (pre-live)

## 6. Capability matrix

§3.7 `QLT-210 DB/integration` cells remain `[ ]` for all capabilities including `foundation-pilot`. Parent `[x]` only means harness ready; domain canary still requires cell evidence.

## 7. Files touched

```text
backend/test/integration/foundation_test.go
backend/Makefile
docs/QLT-210-INTEGRATION-COEVOLUTION.md
scripts/ci-assert-suite.mjs
.github/workflows/backend-ci.yml
TASK/07-TESTING-ROLLOUT-DOD.md
TASK/09-EXECUTION-STATUS.md
TASK/evidence/QLT-210/20260717-qlt210-nikki.md
```

## 8. Follow-up

- Claim/fill `foundation-pilot` (and other) QLT-210 cells with domain-depth evidence before canary.
- Expand migration upgrade sample to multi-version “N-1 supported” pin when release policy freezes a previous head.
- Keep domain WaitGroup races when adding last-slot / overspend / first-writer invariants.
