# QLT-310 — Performance and “smooth” behavior budget (parent framework)

- Status: **[x] complete** (parent/framework only)
- Owner: @nikki/opencode
- UI impact: **none**
- Depends on: INT-160 `[x]`; capability cells co-evolve
- Scope: Continuous performance/smoothness categories + required non-empty samples (bundle budget, query policy, checkout poll no-overlap, timeouts) + CI suite `qlt-310-performance` + co-evolution rule. **Not** full FE/BE/UX bullets or §3.7 cells. **No invented load-test results.**

## 1. Deliverables

| Item | Path |
| --- | --- |
| Co-evolution rule doc | `docs/QLT-310-PERFORMANCE-COEVOLUTION.md` |
| Parent framework asserts | `tests/unit/qlt-310-parent-framework.test.ts` |
| Bundle budget sample | `scripts/check-bundle-budget.mjs` (+ `frontend-build` / `npm run check:bundle`) |
| Query / keepPrevious / exact invalidation | `shared/query/query-policy.ts`, `tests/unit/int-160-query-mutation.test.ts` |
| Mutation pending / no auto-retry | `shared/query/mutation-policy.ts` |
| Checkout poll no-overlap | `features/commerce/checkout/poll.ts`, `tests/unit/chk-120-checkout-poll.test.ts` |
| HTTP timeouts | `shared/api/http-client.ts`, `tests/unit/http-client.test.ts` |
| CI suite id | `scripts/ci-assert-suite.mjs` → `qlt-310-performance` |
| npm assert | `package.json` → `ci:assert:performance` |
| Frontend CI wire | `.github/workflows/ci.yml` → `frontend-static` |
| Branch protection note | `docs/CI-BRANCH-PROTECTION.md` |

## 2. Parent vs cells

| Level | Claimed by this task |
| --- | --- |
| Parent `QLT-310` | Category registration, non-empty samples, parent unit assert, `qlt-310-performance` CI guard, co-evolution doc |
| §3.7 `QLT-310 performance` cells | **Remain `[ ]`** — domain canary still requires cell evidence / measured SLOs |
| `07` FE interaction / BE budget / UX smoothness bullets | Partial samples exist; full claim is per-domain cell, not parent |

Parent must never wait on descendant cells. Domain tasks expand budgets and smoothness guards in the same PR as performance-sensitive FE/BE changes.

## 3. Categories (registered)

1. FE interaction guards  
2. BE budget categories  
3. UX smoothness policy  

## 4. Tests run (2026-07-17)

```text
node scripts/ci-assert-suite.mjs qlt-310-performance
# → OK qlt-310 parent harness + 3 categories + samples=10 + bundle/query/poll/timeout + co-evolution

./node_modules/.bin/vitest run tests/unit/qlt-310-parent-framework.test.ts
# → Test Files  1 passed (1)
# → Tests  9 passed (9)
```

No production secrets used. No load-test results invented; domain SLOs co-evolve via §3.7 cells.

## 5. Checklist mapping (07 §QLT-310)

- [x] Parent: categories registered + samples + CI + co-evolution
- [ ] Full FE interaction target bullets — **capability cells**
- [ ] Full BE budget/load bullets — **capability cells** (measured; not invented)
- [ ] Full UX smoothness bullets — **capability cells**

## 6. Capability matrix

§3.7 `QLT-310 performance` cells remain `[ ]` for all capabilities including `foundation-pilot`. Parent `[x]` only means harness ready; domain canary still requires cell evidence.

## 7. Files touched

```text
docs/QLT-310-PERFORMANCE-COEVOLUTION.md
tests/unit/qlt-310-parent-framework.test.ts
scripts/ci-assert-suite.mjs
package.json
.github/workflows/ci.yml
docs/CI-BRANCH-PROTECTION.md
TASK/07-TESTING-ROLLOUT-DOD.md
TASK/09-EXECUTION-STATUS.md
TASK/evidence/QLT-310/20260717-qlt310-nikki.md
```
