# SEL-250 — Seller order list/detail read model and delivery actions

**Status:** `[x]` completed  
**Date:** 2026-07-17  
**Agent:** @nikki/opencode  
**Depends on:** SEL-100, INT-190 (`[x]`)

## Scope delivered

### Backend
- `GET /v1/stores/{storeId}/orders` — NumberedPageList (page/pageSize/totalCount/pageCount), filters: status/source/q/from/to, stable `created_at DESC, id DESC`, max pageSize 50
- `GET /v1/stores/{storeId}/orders/{orderId}` — detail aggregate (items, grants meta, payment summary, derived timeline); no access token hash/secrets
- Store membership + platform admin; foreign store/order → safe `RESOURCE_NOT_FOUND`
- Dedicated sqlc queries in `seller_orders.sql` (list projection with primary product + delivery status)
- OpenAPI paths + schemas; router inventory updated
- Delivery resend/retry/revoke remain existing BE-235 commands (unchanged)

### Frontend
- Schemas: `sellerOrder*DtoSchema` / list+detail envelopes in `shared/api/schemas.ts`
- Mappers: `features/orders/mappers.ts` → existing `SellerOrder` view
- API: `listSellerOrders` / `getSellerOrder` / `resendSellerOrderDelivery` (resend only)
- Hooks: store-scoped query keys with filters; resend mutation invalidates list/detail/overview (no optimistic success)
- SSR: `features/orders/server.ts` via `serverApiRequest` + cookie forward + no-store; foreign → `notFound()`
- Screen: keep filter/table/detail geometry; server fee/net; existing resend control only (no retry/revoke buttons)
- Mock path preserved for prototype

## Key files

| Area | Path |
| --- | --- |
| SQL | `backend/internal/adapters/postgres/queries/seller_orders.sql` |
| Service | `backend/internal/application/seller_order_service.go` |
| Repo | `backend/internal/adapters/postgres/seller_order_repo.go` |
| Handler | `backend/internal/adapters/http/handlers/seller_orders.go` |
| Presenter | `WriteNumberedList` in `presenters/envelope.go` |
| OpenAPI | `backend/api/openapi.yaml` (`listStoreOrders`, `getStoreOrder`) |
| FE api/mappers | `features/orders/*` |
| Screen | `features/seller/screens/orders.tsx` |
| SSR page | `app/dashboard/(workspace)/orders/[orderId]/page.tsx` |
| Tests | `tests/unit/sel-250-seller-orders.test.ts` |

## Endpoints

| Method | Path | Notes |
| --- | --- | --- |
| GET | `/v1/stores/{storeId}/orders` | NumberedPageList; seller.store.read |
| GET | `/v1/stores/{storeId}/orders/{orderId}` | Detail; foreign → 404 |
| POST | `/v1/stores/{storeId}/orders/{orderId}/delivery/resend` | Existing control wired |
| POST | `.../delivery/retry`, `.../revoke` | Backend exists; UI out-of-scope |

## Tests / verification

```text
npx vitest run tests/unit/sel-250-seller-orders.test.ts tests/unit/api.test.ts
# Test Files  2 passed (2) · Tests  19 passed

go build ./internal/...
go test ./internal/application/ ./internal/adapters/http/presenters/ -count=1
npx tsc --noEmit -p tsconfig.json  # clean for SEL-250 surfaces
npm run api:generate               # OpenAPI types regenerated
```

### Unit coverage
- Store-scoped list path + filter mapping (status tab → payment_status, q)
- Foreign detail → null (safe 404)
- Mock path no network
- Fee/net from server snapshot only
- No delivery secrets in view JSON
- Query keys include store id + filters

## AC mapping

| AC | Result |
| --- | --- |
| Store-scoped via SEL-100 | `useSellerStoreId` + path `storeId` |
| Foreign order → safe 404 | BE membership + FE null/`notFound()` |
| NumberedPageList / TablePagination | Server meta → page controls (list); overview keeps client page on bounded items |
| Resend only | Existing button wired; no retry/revoke UI |
| No UI redesign | Same cards/table/timeline geometry |
| No secret in reads | Grant DTO omits token hash; mapper assert |

## Out of scope (explicit)
- Retry/revoke UI buttons (no characterized control)
- Export button wiring
- Invoice/review admin deep links beyond existing detail fields
- Full cross-stack E2E (QLT-220)
