# INT-180 — Production runtime adapters, callback security, truthful readiness

**Status:** engineering readiness complete (owner secrets still required for live traffic)  
**Date:** 2026-07-17  
**Actor:** @nikki/opencode  
**Depends on:** INT-000 [x], INT-030 [x]

## Objective met

Close the gap between “routes exist” and “staging/production composition cannot run with fake/noop authority.”

## What is REAL (code path)

| Capability | Adapter | Composition rule |
| --- | --- | --- |
| Xendit QRIS + disbursement | `adapters/xendit.Real` HTTP client | `XENDIT_MODE=live` → Real; `fake` only local/test |
| Xendit fake | `adapters/xendit.Fake` | local/test only; **boot fail** staging/production |
| Mail SMTP | `adapters/mail.SMTP` | required on staging/production (`MAIL_MODE=smtp`) |
| Mail capture | `adapters/mail.Capture` | local/test default; **forbidden** live |
| Redis client | `adapters/redis.Client` (go-redis) | required staging/production |
| Rate limiter | Redis fixed-window token bucket | live; process-local token bucket local/test only |
| Object store | `adapters/r2.Client` | production requires configured R2 (existing) |
| Payment callback ingress | existing BE-330 + tests | token CT-compare, 256KiB bound, quarantine, dedupe |
| Disbursement callback | hardened `DisbursementWebhook` | same bound + CT token; no mutation on invalid |

## What is still OWNER-SECRET dependent

| Item | Owner action |
| --- | --- |
| `XENDIT_SECRET_KEY` / `XENDIT_WEBHOOK_TOKEN` | Secret manager + Xendit dashboard match |
| `MAIL_SMTP_*` credentials | Provider SMTP |
| `REDIS_URL` (`rediss://` prod) | Managed Redis TLS |
| `R2_*` | Cloudflare R2 |
| Live canary money path | After secrets + canary procedure |

## Explicit non-claims (not fake-as-ready)

| Item | Disposition |
| --- | --- |
| Malware scanner | **Not** production-ready; `LocalScanPass` only local/test. Object READY must not be claimed live without INT-185/SEL-230 scanner. |
| DNS/edge custom domain | Still fake adapters; **do not roll out** custom domain until SEL-310 + real adapters. |
| HA job registry / lifecycle inventory | INT-185 |
| Live provider sandbox contract with real money | Owner + staging secrets |

## Composition / config fail-closed (INT-180)

- `APP_ENV=staging|production` + `XENDIT_MODE=fake` → config error
- staging/production + `MAIL_MODE=capture|noop` → config error
- production requires SMTP host/from, Redis TLS, R2, session secrets (existing + mail)
- `NewRuntime` refuses noop redis / fake payment / capture mail on live runtime
- `/health/ready` fails if live checks detect fake/noop
- Admin `platformComponentHealth` reports **DOWN** for fake/noop on live (never OK)

## Callback security

| Check | Payment | Disbursement |
| --- | --- | --- |
| Max body 256KiB | yes | yes |
| Constant-time token | yes (`ConstantTimeTokenEqual`) | yes (same) |
| Invalid → no business mutation | rejection table | 401 + no service call |
| Oversize | 413 + quarantine | 413 |
| Dedupe 80× single effect | integration suite | business applyLookup idempotent |

## Tests run (green)

```text
go test ./internal/config/ ./internal/adapters/xendit/ ./internal/app/ \
  ./internal/application/ ./internal/adapters/http/handlers/ \
  ./internal/adapters/http/ ./internal/... -count=1
```

Key new/extended tests:

- `config`: staging rejects fake xendit; live rejects capture/noop mail
- `xendit`: Real create QRIS, timeout classification, rate limit, disbursement idempotency header, no secret in Name()
- `app`: platform health DOWN for fake/noop on live; wireMailer policy; redis limiter fail-closed
- `application`: ingress oversize/invalid/missing token → rejection quarantine
- `handlers`: disbursement missing/invalid token, oversize, malformed

Existing integration callback suite (token/dedupe/cross-mode) remains authoritative for paid path when DB available.

## Files touched (primary)

```text
backend/internal/config/config.go
backend/internal/config/config_test.go
backend/internal/app/app.go
backend/internal/app/composition_test.go
backend/internal/adapters/xendit/client.go
backend/internal/adapters/xendit/client_test.go
backend/internal/adapters/xendit/real_stub.go
backend/internal/adapters/xendit/fake.go
backend/internal/adapters/mail/smtp.go
backend/internal/adapters/mail/noop.go
backend/internal/adapters/mail/capture.go
backend/internal/adapters/redis/client.go
backend/internal/adapters/redis/limiter.go
backend/internal/adapters/redis/noop.go
backend/internal/adapters/http/router.go
backend/internal/adapters/http/handlers/withdrawals.go
backend/internal/adapters/http/handlers/disbursement_webhook_test.go
backend/internal/application/callback_service.go
backend/internal/application/callback_security_test.go
backend/go.mod (github.com/redis/go-redis/v9)
```

## Acceptance mapping

| Criterion | Result |
| --- | --- |
| No fake/noop authority on live composition path | **PASS** (config + NewRuntime) |
| Readiness honest | **PASS** (checks + admin component health) |
| Callback dedupe/signature tests | **PASS** (unit + existing integration design) |
| go test relevant packages | **PASS** |

## Residual for G4 / launch

Owner must fill secrets and run staging with `XENDIT_MODE=live` + real SMTP/Redis/R2 before any live money canary. Scanner and custom-domain remain non-launch unless separate tasks complete.
