# SEL-100 — Merchant/current-store bootstrap and route readiness

**Status:** `[x]` completed  
**Actor:** @nikki/opencode  
**When:** 2026-07-17 13:30 WIB  
**Depends on:** INT-120, INT-140, INT-150, INT-160 (`[x]`)

## 1. Objective delivered

- Complete seller bootstrap schema/mapper: merchant, memberships, stores, capabilities, `canonicalStoreId`, `currentStoreId`, **onboarding state**
- Server-authoritative onboarding gate: incomplete / no-store → `/dashboard/onboarding`
- Keep store-switch control as-is (no invented dropdown; canonical single-store launch)
- API-mode never uses `DEMO_STORE_ID` as store authority; mock fixtures only
- Query keys remain store-scoped; prior-store cache cleared on switch
- Foreign/tampered store preference rejected client-side; BE continues 404 via StoreAccessGuard

## 2. Backend (bootstrap completeness)

| Change | Path |
| --- | --- |
| `Merchant.OnboardingState` on authz domain | `backend/internal/domain/authz/types.go` |
| Map onboarding_state from SQL rows | `backend/internal/adapters/postgres/authz_repo.go` |
| Bootstrap fields `OnboardingState`, `OnboardingCompleted` | `backend/internal/application/authz_service.go` |
| JSON: `onboardingState`, `onboardingCompleted` | `backend/internal/adapters/http/handlers/authz.go` |
| OpenAPI description | `backend/api/openapi.yaml` |
| Unit: incomplete default + COMPLETE when set | `backend/internal/application/authz_service_test.go` |

**Completion rule:** `onboardingCompleted = (onboardingState == "COMPLETE" && currentStoreId != "")`. Empty state → `NOT_STARTED` / not completed.

## 3. Frontend

| Module | Role |
| --- | --- |
| `shared/api/schemas.ts` | Zod: `onboardingState`, `onboardingCompleted` |
| `shared/seller/bootstrap-api.ts` | Fetch + mock; `selectCurrentStoreId`, `needsSellerOnboarding`, `isAllowedSellerStoreId` |
| `shared/seller/current-store.tsx` | Provider statuses include `needs_onboarding`; switch rejects foreign ids |
| `shared/seller/workspace-gate.tsx` | API-mode redirect to `/dashboard/onboarding` |
| `app/dashboard/(workspace)/layout.tsx` | Wraps frame with `SellerWorkspaceGate` |
| `shared/seller/store-cache.ts` | Unchanged: clear prior store query keys |

**Store switch UI:** `dashboard-shell.tsx` still has the existing non-functional switch button only — **no dropdown invented** (UI-080 not required).

**API mode:** `useSellerStoreId()` returns `""` until ready; hooks already `enabled: Boolean(storeId)`.  
**Mock mode:** demo bootstrap + gate skipped so prototype screens stay intact.

## 4. Tests

```text
# FE
npm run test:run -- tests/unit/sel-100-seller-bootstrap.test.ts tests/unit/int-150-current-store.test.ts
# expected: pass

# BE
go test ./internal/application/ ./internal/domain/authz/ -count=1
# expected: ok
```

Coverage:

- Bootstrap schema + onboarding fields
- Preferred / foreign / empty store selection
- Tampered preference rejected (`isAllowedSellerStoreId`)
- Unfinished onboarding / no-store → `needsSellerOnboarding`
- Cache clear prior store only
- API domain source ≠ DEMO as authority

## 5. Acceptance

| Criterion | Evidence |
| --- | --- |
| Schema/mapper merchant+stores+onboarding | schemas + bootstrap-api + BE handler |
| Bootstrap before seller queries | CurrentStoreProvider + empty storeId disables hooks |
| Onboarding redirect server state | SellerWorkspaceGate + `onboardingCompleted` |
| No DEMO in API path | useSellerStoreId mock-only DEMO; architecture gate |
| Foreign store safe | FE ignore + BE StoreAccessGuard NOT_FOUND (INT-150) |
| No store-switch dropdown | dashboard-shell unchanged button-only |
| No UI redesign | gate returns null / existing shell |

## 6. Out of scope

- SEL-110 onboarding create/resume/complete wiring
- Multi-store switch menu (UI-080)
- Finer OWNER vs STAFF capability migration (documented INT-150)

## 7. Consumer notes

```ts
import {
  useSellerStoreId,
  useSellerStoreReady,
  useCurrentStore,
  needsSellerOnboarding,
} from "@/shared/seller";

const storeId = useSellerStoreId(); // "" until ready (API)
const { status, bootstrap, onboardingCompleted } = useCurrentStore();
// status: loading | ready | needs_onboarding | no_membership | error
```
