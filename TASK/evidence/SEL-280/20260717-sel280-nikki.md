# SEL-280 — Coupon lifecycle dan checkout redemption

**Status:** `[x]` completed  
**Date:** 2026-07-17  
**Agent:** @nikki/opencode  
**Depends on:** SEL-100, CHK-100 (`[x]`)  
**Branch:** main  

## 1. Objective delivered

Wire store-scoped coupon list/detail/create/patch/activate/pause/archive on `/dashboard/coupons` and `/dashboard/coupons/new` when `sellerOperations` is `api`. No UI redesign. Foreign store/coupon → safe 404. Integer IDR/bps only. Status only via explicit activate/pause/archive (no status patch). Checkout coupon UI remains **DISABLED/OUT-OF-SCOPE** (CHK-100); server quote/reserve path unchanged.

## 2. Files

| Path | Role |
| --- | --- |
| `shared/api/schemas.ts` | `couponDtoSchema`, list/create/patch envelopes + requests |
| `shared/query/query-keys.ts` | `seller.coupons(storeId)`, `seller.coupon(storeId, couponId)` |
| `features/seller/coupons/contracts.ts` | View models + command inputs |
| `features/seller/coupons/mappers.ts` | DTO → list labels; create/patch body builders |
| `features/seller/coupons/api.ts` | List/detail/create/patch/activate/pause/archive adapters |
| `features/seller/coupons/hooks.ts` | Queries + mutations + invalidate list/detail |
| `features/seller/coupons/mock.ts` | Snapshot-identical demo fixtures |
| `features/seller/screens/coupons.tsx` | Wire existing list/form controls (no redesign) |
| `features/seller/ui/pieces.tsx` | Optional controlled `Select` value/onChange (additive) |
| `tests/unit/sel-280-seller-coupons.test.ts` | Unit coverage |
| `TASK/04-SELLER-WORKSPACE.md` | Checklist `[x]` |
| `TASK/06-ENDPOINT-CONTRACT-MATRIX.md` | Coupon rows → M |
| `TASK/09-EXECUTION-STATUS.md` | `[x]` claim released |

## 3. Endpoints

| Method | Path | Notes |
| --- | --- | --- |
| GET | `/v1/stores/{storeId}/coupons` | Full list SuccessEnvelope; membership; foreign → 404 |
| POST | `/v1/stores/{storeId}/coupons` | Create draft; Idempotency-Key; integer money |
| GET | `/v1/stores/{storeId}/coupons/{couponId}` | Detail; foreign → null (safe 404) |
| PATCH | `/v1/stores/{storeId}/coupons/{couponId}` | `expectedVersion` concurrency |
| POST | `.../activate` | DRAFT\|PAUSED → ACTIVE (idempotent) |
| POST | `.../pause` | ACTIVE → PAUSED (idempotent) |
| POST | `.../archive` | → ARCHIVED (idempotent) |

Checkout: `POST /v1/checkout/quote` / `apply-coupon` / `coupon-reservations` already BE; FE coupon input remains OUT-OF-SCOPE.

## 4. Behavior

| Surface | Transport | Notes |
| --- | --- | --- |
| List (api) | GET coupons | Maps code/discount/usage/ends/status; client TablePagination |
| Metrics | derived from list | activeCount / usageCount sum; total discount `—` (no invented money) |
| Create+activate | POST create then activate | Existing “Simpan & aktifkan kupon” CTA |
| Patch | PATCH expectedVersion | No status in body |
| Transitions | POST activate/pause/archive | Explicit only |
| Mock | fixtures | demo 12 rows; no network |
| Invalidation | list + detail keys | |

### Pagination

- Snapshot has `TablePagination` (NumberedPageList profile).
- BE list has no page meta yet → FE loads full store list and pages client-side (same geometry as pre-wire mock).
- Query key remains store-scoped for tenant cache isolation.

### Checkout redemption

- UI never decrements usage as authority.
- Reservation/redemption remains server-atomic (existing BE + CHK-100 quote path).

## 5. Tests / verification

```text
npx vitest run tests/unit/sel-280-seller-coupons.test.ts
# Test Files  1 passed · Tests  19 passed
```

### Unit coverage

- Schemas: list envelope, create integer-only, patch requires expectedVersion
- Mappers: PERCENT/FIXED_IDR labels, lifecycle status chips, metrics
- Query keys store-scoped
- Mock path no network
- API list path + mapping
- Foreign store list rethrows 404; foreign detail → null
- Create idempotency + body normalization
- activate/pause/archive paths
- Stale patch 409 rethrow

## 6. AC mapping

| AC | Result |
| --- | --- |
| Store-scoped via SEL-100 | `useSellerStoreId` + path storeId |
| Foreign coupon → safe 404 | list rethrow / detail null |
| Explicit status transitions | activate/pause/archive only |
| Integer IDR/bps | moneyIdrSchema + trunc mappers |
| No checkout redesign | coupon UI still OUT-OF-SCOPE |
| UI list/form unchanged | same table/form/MiniStat geometry |
| Mock path | demo fixtures preserved |

## 7. Out of scope / follow-ups

- Checkout coupon input (UI-080)
- Server NumberedPageList meta for large coupon sets
- Finance “total diskon” aggregate (shows `—` in API mode)
- Row action menu (MoreHorizontal remains inert icon as snapshot)
