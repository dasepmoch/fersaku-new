# BUY-140 — Notification center and profile shell authority

**Status:** `[x]` completed  
**Actor:** @nikki/opencode  
**When:** 2026-07-17 16:32 WIB  
**Depends on:** INT-120 (`[x]`), INT-160 (`[x]`)  
**Branch:** main  

## 1. Objective delivered

Shared canonical notification list/mark-read adapters + profile shell identity authority for buyer/seller/admin shells. Wire existing `NotificationCenter` / `ProfileMenu` when domain is `api`. Session-bound; no cross-surface leakage. No UI redesign.

## 2. Files

| Path | Role |
| --- | --- |
| `shared/api/schemas.ts` | `notificationDataDto` + list/unread/mark-read/read-all envelopes |
| `shared/query/query-keys.ts` | `notifications.list/unreadCount(surface, subject)` |
| `shared/notifications/contracts.ts` | Shell view model (`NotificationItemView`) |
| `shared/notifications/mappers.ts` | DTO → view; CTA surface allowlist; relative time; surface filter |
| `shared/notifications/api.ts` | Surface-alias list/unread/mark-read/read-all; mock path |
| `shared/notifications/hooks.ts` | Subject/surface-bound queries + mutations |
| `shared/notifications/mock.ts` | Frozen fixtures from `account-controls-data` |
| `shared/notifications/index.ts` | Public exports |
| `shared/ui/notification-center.tsx` | Wire list/unread/mark-read/read-all (JSX/style preserved) |
| `shared/ui/profile-menu.tsx` | API mode: session claims only (no hardcoded persona) |
| `tests/unit/buy-140-notifications.test.ts` | list, mark-read, mock path, surface isolation, CTA reject |
| `tests/unit/query-keys.test.ts` | notification key isolation |
| `TASK/03-PUBLIC-AUTH-CHECKOUT-BUYER.md` | BUY-140 checklist `[x]` |
| `TASK/09-EXECUTION-STATUS.md` | `[x]` claim released |

## 3. Behavior

| Surface | Transport | Domain gate |
| --- | --- | --- |
| Buyer list/unread/read | `/v1/buyer/notifications*` | `buyer === "api"` |
| Seller list/unread/read | `/v1/seller/notifications*` | `sellerOperations === "api"` |
| Admin list/unread/read | `/v1/admin/notifications*` | `adminRead === "api"` |
| Mock | fixtures from `account-controls-data` | domain mock |
| CTA `ctaPath` | `sanitizeReturnToForSurface` | cross-surface / external → surface home |
| Mark one / read-all | POST after server success | cache patch + invalidate |
| Profile menu | session provider claims | API mode never uses mock persona |
| Cache clear | root key `notifications` | already in INT-120 `private-cache` |

### Bounded list

- First page only: `limit=20` (`NOTIFICATION_INBOX_LIST_LIMIT`).
- No cursor UI on notification center (UI-080 for expansion).

### Surface isolation

- Query keys include surface + `${subjectId}:${sessionId}`.
- Mapper drops rows whose BE `surface` ≠ shell surface.
- CTA must stay under surface prefix (`/account`, `/dashboard`, `/admin`).

## 4. Tests / verification

```text
npm run test:run -- tests/unit/buy-140-notifications.test.ts
# 10 passed

npx eslint shared/notifications shared/ui/notification-center.tsx \
  shared/ui/profile-menu.tsx shared/api/schemas.ts \
  shared/query/query-keys.ts tests/unit/buy-140-notifications.test.ts \
  --max-warnings=0
# pass

npx tsc --noEmit
# pass
```

Coverage:

- Schema parse: list / unread / mark-read / read-all
- CTA allowlist rejects external / javascript / cross-surface
- Cross-surface row filter (buyer cannot keep seller rows)
- Query key surface + subject isolation; private-cache root
- Mock path fixtures per surface
- API list path + limit; mark-read POST; unread/read-all admin alias
- Domain gate buyer vs seller

## 5. UI freeze

- Existing notification panel geometry/copy/classes preserved.
- Badge still `size-1.5` dot; “Tandai semua dibaca” / prefs footer unchanged.
- Profile menu links/logout chrome unchanged; identity source only.

## 6. Out of scope

- SEL-420 / ADM-230 surface acceptance binding beyond shared primitive
- Notification preferences page (BUY-120 / SEL-340)
- Cursor pagination UI (UI-080)
- New notification types / redesign

## 7. Consumer notes

```ts
import {
  listNotifications,
  markNotificationRead,
  markAllNotificationsRead,
  useNotifications,
  useUnreadNotificationCount,
  useMarkNotificationReadMutation,
  useMarkAllNotificationsReadMutation,
  isNotificationApiDomain,
} from "@/shared/notifications";
```

- Shells already pass `surface="buyer"|"seller"|"admin"`.
- SEL-420 / ADM-230 must not invent separate adapters; reuse this module.
