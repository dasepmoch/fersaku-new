# BUY-120 — Buyer profile and notification preferences

**Status:** `[x]` completed  
**Actor:** @nikki/opencode  
**When:** 2026-07-17 16:17 WIB  
**Depends on:** AUT-110 (`[x]`); INT-175 avatar active → no (DISABLED disposition)  
**Branch:** main  

## 1. Objective delivered

Wire session-bound buyer profile GET/PATCH and notification preferences on `/account/profile` when `buyer` domain is `api`. Map BE `ProfileData` + closed-schema notification prefs to existing `BuyerProfile` view. Avatar/personal media remains **DISABLED/OUT-OF-SCOPE** (INT-175). Email-change CTA remains **DISABLED** until AUT-120. No UI redesign.

## 2. Files

| Path | Role |
| --- | --- |
| `shared/api/schemas.ts` | `buyerProfileDto` + patch request; notification prefs envelope/patch |
| `shared/query/query-keys.ts` | `buyer.profile(subject)` + `notificationPreferences(subject)` |
| `features/buyer/data/contracts.ts` | `BuyerProfile` view (revision, toggles, initials); patch inputs |
| `features/buyer/data/mappers.ts` | ProfileData → view; prefs → marketing/receipt toggles; initials/locale |
| `features/buyer/data/api.ts` | GET profile+prefs; PATCH profile; PATCH marketing prefs; mock path |
| `features/buyer/data/hooks.ts` | subject-bound query; patch mutations; invalidate after success |
| `features/buyer/data/mock.ts` | Demo profile with revision/toggles/initials |
| `features/buyer/data/index.ts` | Public exports |
| `features/buyer/screens/pieces.tsx` | Controlled `ProfileField` (value/onChange/readOnly) |
| `features/buyer/screens/buyer-profile.tsx` | Wire existing controls; draft-on-edit; disabled email CTA |
| `tests/unit/buy-120-buyer-profile.test.ts` | get/patch, mock, 409, avatar disposition |
| `tests/unit/query-keys.test.ts` | profile subject isolation |
| `TASK/03-PUBLIC-AUTH-CHECKOUT-BUYER.md` | BUY-120 checklist `[x]` |
| `TASK/09-EXECUTION-STATUS.md` | `[x]` claim released |

## 3. Behavior

| Surface | Transport | Notes |
| --- | --- | --- |
| Get (api) | `GET /v1/buyer/profile` + `GET /v1/me/notification-preferences` | Parallel; map to view |
| Patch profile | `PATCH /v1/buyer/profile` | `expectedVersion` + displayName/phone/locale/timezone; **no email**, **no avatarRef** |
| Patch prefs | `PATCH /v1/me/notification-preferences` | `MARKETING_NEWSLETTER` EMAIL only |
| Receipt toggle | — | Always on (PAYMENT_RECEIPT mandatory); control has no onChange |
| Product-update toggle | local draft only | No closed BE event at launch; not claimed as server truth |
| Mock buyer | fixtures | demo profile; no network |
| 409 conflict | rethrow | Form draft kept; no optimistic overwrite |

### Avatar disposition (INT-175)

- **DISABLED / OUT-OF-SCOPE** for launch.
- Screen uses initials circle only (`profile.initials`); title documents INT-175 deferral.
- No upload control, no `/v1/me/objects`, no store-scoped objects, no data URL / localStorage for personal media.
- Architecture gate in `tests/unit/architecture-boundaries.test.ts` (INT-175) + BUY-120 disposition tests.

### Email-change disposition

- “Mulai perubahan email” remains **disabled** (no handler/modal) until AUT-120 dual-confirm composition / UI-080.
- Profile PATCH never accepts email.

## 4. Tests / verification

```text
npm run test:run -- tests/unit/buy-120-buyer-profile.test.ts tests/unit/query-keys.test.ts tests/unit/buyer-data.test.ts
# 20 passed

npx eslint features/buyer/data features/buyer/screens/buyer-profile.tsx \
  features/buyer/screens/pieces.tsx shared/api/schemas.ts \
  shared/query/query-keys.ts tests/unit/buy-120-buyer-profile.test.ts \
  tests/unit/query-keys.test.ts --max-warnings=0
# pass

npx tsc --noEmit
# pass
```

Coverage:

- Profile + prefs schema parse; patch requires expectedVersion; no avatar/email on patch
- Map displayName/version/initials/locale; marketing vs mandatory receipt
- Subject A vs B profile query key isolation
- Mock get/patch/prefs path
- API GET parallel paths; PATCH body shape; 409 rethrow
- Avatar initials-only + disabled email CTA evidence in screen source

## 5. UI freeze

- Existing card/geometry/copy/classes preserved.
- Controlled inputs only (no new panels/error chrome).
- Email CTA disabled styling only (`disabled:opacity-60`).
- Avatar remains static initials circle (no new upload button).

## 6. Out of scope

- AUT-120 dual-confirm email change
- INT-175 personal media upload lifecycle
- BUY-140 notification center / shell authority
- Product-update preference BE event (not in closed schema)

## 7. Consumer notes

```ts
import {
  getBuyerProfile,
  patchBuyerProfile,
  patchBuyerNotificationPreferences,
  isBuyerProfileApiDomain,
  useBuyerProfile,
  usePatchBuyerProfileMutation,
  usePatchBuyerNotificationPreferencesMutation,
} from "@/features/buyer/data";
```

- Domain gate: `buyer === "api"` for live adapters; mock uses fixtures.
- Pass `expectedVersion` from `profile.revision` on save.
- Do not patch email or avatar via this surface.
