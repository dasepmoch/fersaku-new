# QLT-400 — Per-domain flags, canary, kill switch (parent framework)

- Status: **[x] complete** (parent/framework only)
- Owner: @nikki/opencode
- UI impact: **none**
- Depends on: INT-025 `[x]`, INT-170 `[x]`, QLT-320 framework `[x]`; capability cells co-evolve (activation gates)
- Scope: Continuous flags/canary/kill-switch categories + required non-empty samples (INT-025 domain-source, domain-flags precedence/kill/cache/telemetry, architecture-boundaries) + CI suite `qlt-400-flags` + co-evolution rule. **Not** full rollout stages or §3.7 QLT-400 flag cells. **No invented production canary/kill firings.** Production never falls back to mock business data when flag off.

## 1. Deliverables

| Item | Path |
| --- | --- |
| Co-evolution rule doc | `docs/QLT-400-FLAGS-COEVOLUTION.md` |
| Parent framework asserts | `tests/unit/qlt-400-parent-framework.test.ts` |
| Typed registry (INT-025) | `shared/data/domain-source.ts`, `domain-source-provider.tsx` |
| Flags / canary / kill / cache | `shared/data/domain-flags.ts` |
| INT-025 unit samples | `tests/unit/domain-source.test.ts` |
| QLT-400 unit samples | `tests/unit/domain-flags.test.ts` |
| Architecture mock/API gate | `tests/unit/architecture-boundaries.test.ts` |
| Query policy note | `shared/query/QUERY-MUTATION-POLICY.md` |
| CI suite id | `scripts/ci-assert-suite.mjs` → `qlt-400-flags` |
| npm assert | `package.json` → `ci:assert:flags` |
| Frontend CI wire | `.github/workflows/ci.yml` → `frontend-static` |
| Branch protection note | `docs/CI-BRANCH-PROTECTION.md` |

## 2. Parent vs cells

| Level | Claimed by this task |
| --- | --- |
| Parent `QLT-400` | Category registration, non-empty samples, parent unit assert, `qlt-400-flags` CI guard, co-evolution doc |
| §3.7 `QLT-400 flag` cells | **Remain `[ ]`** — domain canary/activation still requires cell evidence |
| `07` rollout stages / per-stage checks | Sequence registered; go/no-go is cell/ops, not parent |

Parent must never wait on descendant cells. Domain tasks expand canary activation in the same PR as domain cutover work.

## 3. Categories (registered)

1. Typed registry  
2. Production mock rejection  
3. Emergency kill switch  
4. Canary / allowlist  
5. Hydration parity  
6. Cache cleanup  
7. Telemetry (public-safe)  

## 4. Precedence (implemented)

```text
emergency kill (active) > canary/allowlist > DOMAIN_SOURCE_OVERRIDES > bootstrap
```

Live residual mock → `disabled` (never fixtures).

## 5. Tests run (2026-07-17)

```text
node scripts/ci-assert-suite.mjs qlt-400-flags
# → OK qlt-400 parent harness + categories + samples + domain-source/flags + kill/canary/cache + co-evolution

./node_modules/.bin/vitest run \
  tests/unit/qlt-400-parent-framework.test.ts \
  tests/unit/domain-source.test.ts \
  tests/unit/domain-flags.test.ts
# → Test Files  3 passed
```

No production secrets used. No live canary percentages or kill-switch firings invented; domain activation co-evolves via §3.7 cells.

## 6. Checklist mapping (07 §QLT-400)

- [x] Parent: registry principles + kill/canary/hydration/cache/telemetry samples + CI + co-evolution
- [ ] Full recommended rollout sequence stages — **capability cells / ops**
- [ ] Per-stage canary observation windows — **capability cells / ops** (not invented)
- [ ] §3.7 flag cells — **remain open**

## 7. Capability matrix

§3.7 `QLT-400 flag` cells remain `[ ]` for all capabilities including `foundation-pilot`. Parent `[x]` only means harness ready; domain canary still requires cell evidence.

## 8. Files touched

```text
docs/QLT-400-FLAGS-COEVOLUTION.md
shared/data/domain-flags.ts
shared/data/domain-source.ts
shared/query/QUERY-MUTATION-POLICY.md
tests/unit/domain-flags.test.ts
tests/unit/qlt-400-parent-framework.test.ts
scripts/ci-assert-suite.mjs
package.json
.github/workflows/ci.yml
docs/CI-BRANCH-PROTECTION.md
TASK/07-TESTING-ROLLOUT-DOD.md
TASK/09-EXECUTION-STATUS.md
TASK/evidence/QLT-400/20260717-qlt400-nikki.md
```
