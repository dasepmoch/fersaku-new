# ADM-110 — Permission boundary and least-privilege navigation

**Status:** `[x]` completed  
**Actor:** @nikki/opencode  
**When:** 2026-07-17 16:16 WIB  
**Depends on:** ADM-100, INT-150 (both `[x]`)  
**Branch:** main

## 1. Objective delivered

Canonical FE permission map aligned to backend `authz.AllPermissionCodes` / router middleware. API path uses session claims only (no mock session authority). Existing `AdminPermissionBoundary`, shell nav, and action chrome hide/disable without redesign. Backend remains enforcement authority.

## 2. Drift resolution (option b — FE → existing BE codes)

| Surface | Snapshot FE | Target decision | Result |
| --- | --- | --- | --- |
| Profile | `profile.read` | Authenticated admin + subject ownership | `permission: null` + `claimsAreAuthenticatedAdmin` |
| Campaigns | `campaigns.read` | DecisionPending until ADM-380 | `disposition: decision_pending` (nav hidden, boundary denies even with `*`) |
| Withdrawals | `withdrawals.read` | Combined with review | `withdrawals.review` |
| KYC | `kyc.read` | Combined with review | `kyc.review` |
| Providers | `providers.read` | Use payments read | `payments.read` |
| System | `system.read` | Emergency gate for system surface | `platform.emergency` |
| Merchant mutation | `merchants.update` | Canonical write | `merchants.write` |
| Audit export | `audit.export` | Accept audit.read | `audit.read` |

## 3. Files

| Path | Role |
| --- | --- |
| `features/admin/config/permissions.ts` | FE mirror of `AllPermissionCodes`, fail-closed helpers, action map |
| `features/admin/config/routes.ts` | Route meta + disposition + nav access helpers |
| `features/admin/config/index.ts` | Barrel |
| `features/admin/components/admin-permission-boundary.tsx` | Session claims + disposition |
| `features/admin/components/admin-console-frame.tsx` | Pass permission + disposition |
| `features/admin/components/admin-shell.tsx` | Nav filter via claims |
| `features/admin/screens/actions.tsx` | Staff invite / roles write / audit export guards |
| `features/admin/data/access.ts` | Mock role fixtures → canonical codes |
| `lib/admin-mock-data.ts` | Mock permission groups → canonical codes only |
| `shared/auth/guards.ts` / `contracts.ts` | Empty permission fail-closed |
| `tests/unit/adm-110-admin-permissions.test.ts` | Registry / route / superuser / mock path |
| `tests/unit/routes.test.ts` | Withdrawals permission expectation |

## 4. Behavior

| Layer | Behavior |
| --- | --- |
| API session | Bootstrap claims from INT-120; no `createMockSession` authority on API path |
| Mock session | `createMockClaims("admin")` still `permissions: ["*"]` for prototype; campaigns still decision_pending |
| Nav | Hide items without page permission; campaigns always hidden |
| Boundary | Existing unauthorized panel; fail-closed when unauthenticated API or missing code |
| Actions | Disable staff invite (`roles.assign`), create role (`roles.write`), audit export (`audit.read`) without new chrome |
| Unknown codes | `claimsHavePermission` denies even under `*` |

## 5. Tests

```text
npm run test:run -- tests/unit/adm-110-admin-permissions.test.ts tests/unit/routes.test.ts
# 16 passed
```

Coverage:

- Missing permission denied (route + nav)
- Superuser `*` path for active routes
- Campaigns blocked for superuser (decision_pending)
- Profile authenticated-admin gate without page code
- Mock fixtures contain only known registry codes
- Route contract: every active section has known permission or null profile gate

## 6. UI freeze

- No new permission UI chrome; existing panel copy/classes retained
- Nav link classes unchanged; only visibility filtered
- Action buttons use existing `AdminButton` disabled state

## 7. Out of scope / deferred

- BE `POST /v1/admin/actions` allowlist hardening (server)
- Credential permission split (`credentials.*`) → ADM-200
- Impersonation scope isolation → ADM-390
- Separate `withdrawals.read` / `kyc.read` / `providers.read` / `system.read` BE codes (not required; FE maps to existing)
- OpenAPI regen / role seed migration (codes already in BE registry)
- Domain action wiring beyond shell chrome (ADM-200+)

## 8. Rollout

- Active when admin uses session claims (`auth=api` + bootstrap)
- Mock domain retains prototype `*` for visual demo minus campaigns
- Production enforcement remains Go middleware on every request
