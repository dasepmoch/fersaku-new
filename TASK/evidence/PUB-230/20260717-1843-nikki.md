# PUB-230 — Static help/careers/blog/API playground disposition

**Status:** `[x]` completed (launch disposition) 
**Actor:** @nikki/opencode 
**When:** 2026-07-17 18:43 WIB 
**Depends on:** INT-025, UI-080 (`[x]`)

## 1. Product decision (authoritative)

| Surface | Disposition | Notes |
| --- | --- | --- |
| Help / blog / company / legal | **STATIC** repository content | Not forbidden mock business authority |
| Help search + category cards | **STATIC local** client filter | No help backend; filter static category index only |
| Careers role CTAs | **STATIC** `mailto:careers@fersaku.id?subject=Application:…` | Same card geometry; was no-op `<button>` |
| Blog list/article | **STATIC** `lib/content-data` + `notFound` | Article + back links real |
| `/api` marketing | **STATIC** | Docs CTA → `/docs/api` |
| `/docs/api` TOC + copy | **STATIC functional** | Real anchors; clipboard copy; no `href="#"` |
| API playground Send | **mock:** labeled timer no-network; **api/disabled:** `DISABLED` | Never fake live network; sandbox IMPLEMENT deferred |

Domain key for playground gate: `publicCatalog` (public docs surface).

Disabled title (api/disabled only): 
`API playground sandbox is out of scope for launch (PUB-230 deferred)`

## 2. Existing UI inventory (no redesign)

| Surface | Control | Launch state |
| --- | --- | --- |
| `app/(resources)/help/page.tsx` | Search input + category cards | Local filter; empty local message; support → `/contact` |
| `app/(company)/careers/page.tsx` | Role rows | `<a href="mailto:…">` same classes as prior button |
| `app/(resources)/blog/*` | Article links | Unchanged STATIC |
| `app/api/page.tsx` | Docs CTA | Unchanged STATIC |
| `app/docs/api/page.tsx` | TOC / on-page nav / CodeBlock copy | Functional anchors + copy |
| `components/api-playground.tsx` | Send / tabs / response copy | Mock timer + label; API Send disabled+title |

## 3. Architecture enforcement

| Path | Role |
| --- | --- |
| `tests/unit/architecture-boundaries.test.ts` | `PUB-230: static help/careers/docs/playground disposition + isolation` |
| `tests/unit/pub-230-static-disposition.test.ts` | Unit disposition + domain-source gate |

Gates assert:

1. Help is local-only (no `/v1/help` / `apiRequest`).
2. Careers uses `CAREERS_APPLY_MAIL` mailto template.
3. Docs has no `href="#"`; anchors include `#api-playground`, `#errors`, section ids.
4. Playground: `getDomainSource("publicCatalog")`, `playgroundSendEnabled`, early return, disabled title; timer send path; no `apiRequest` / shared api import.
5. Isolation: `features/{commerce,seller,admin,buyer,finance,auth}` must not import `api-playground`.

## 4. Contract / matrix updates

| Doc | Update |
| --- | --- |
| `TASK/03-PUBLIC-AUTH-CHECKOUT-BUYER.md` §PUB-230 | Checklist + AC checked |
| `TASK/10-ROUTE-AND-CONTROL-DISPOSITION.md` | Routes + inventory + UXE-007 |
| `TASK/evidence/UI-080/exception-register.md` | UXE-007 closed for PUB-230 surfaces |
| `TASK/09-EXECUTION-STATUS.md` | PUB-230 → `[x]` |

## 5. Explicit non-goals (this task)

- No live sandbox gateway playground (re-open + allowlist + short-lived credential)
- No help CMS/search backend
- No recruiting ATS integration
- No UI redesign / frozen marketing metric rewrite (still UI-080 if copy removed)

## 6. Tests

```text
npm run test:run -- tests/unit/architecture-boundaries.test.ts tests/unit/pub-230-static-disposition.test.ts
```

Result: **21 passed** (2026-07-17).

## 7. Rollback

Revert playground mode-gate + help/careers/docs wiring + tests + disposition/status if product re-opens sandbox playground as IMPLEMENT; then replace DISABLED with isolated sandbox allowlist only.
