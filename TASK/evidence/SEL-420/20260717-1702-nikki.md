# SEL-420 — Seller notifications/profile/logout shell

**Status:** `[x]` completed 
**Actor:** @nikki/opencode 
**When:** 2026-07-17 17:02 WIB 
**Depends on:** SEL-100 (`[x]`), BUY-140 (`[x]`), INT-120 (`[x]`) 
**Branch:** main 

## 1. Objective delivered

Seller dashboard shell binds shared BUY-140 `NotificationCenter` / `ProfileMenu` with `surface="seller"`, session/current-store mapped store chrome (no hardcoded mock identity on API path), server-session impersonation authority when present, and logout that clears private cache including notifications root. No second notification adapter. No UI redesign.

## 2. Files

| Path | Role |
| --- | --- |
| `features/seller/components/dashboard-shell.tsx` | Wire shared controls; bootstrap store chrome; session-bound impersonation prefer |
| `tests/unit/sel-420-seller-shell.test.ts` | Shell disposition, seller alias isolation, logout cache clear |
| `TASK/04-SELLER-WORKSPACE.md` | SEL-420 checklist `[x]` |
| `TASK/09-EXECUTION-STATUS.md` | `[x]` claim released |

Shared (owned by deps, reused only):

| Path | Owner |
| --- | --- |
| `shared/notifications/*` | BUY-140 |
| `shared/ui/notification-center.tsx` / `profile-menu.tsx` | BUY-140 |
| `shared/auth/private-cache.ts` / `session-store.ts` | INT-120 |
| `shared/seller/current-store.tsx` | SEL-100 / INT-150 |

## 3. Behavior

| Surface | Transport / source | Notes |
| --- | --- | --- |
| Notifications list/unread | `/v1/seller/notifications*` | Domain `sellerOperations === "api"` |
| Mark one / read-all | POST seller alias | BUY-140 hooks; cache patch + invalidate |
| Profile menu identity | Session claims | API mode never uses mock persona |
| Logout | `ProfileMenu` → `logoutSession({ surface: "seller" })` | Backend revoke + private cache (incl. `notifications`) + `/login` |
| Store switcher chrome | `useCurrentStore` bootstrap | API: name/slug from membership; mock keeps demo labels |
| Lihat toko | Bootstrap slug → `/@{slug}` | API empty slug → `/dashboard/storefront` |
| Impersonation banner | Prefer `claims.impersonation` | URL/sessionStorage only when no server impersonation |
| Mock interaction | Existing `MockInteractionBoundary` | INT-170 mode-gate; controls use `data-feedback="off"` |

### Cache clear (logout / actor change)

- Root keys: `seller`, `notifications`, `auth`, `session`, `me`, `profile`, `buyer`, `admin` (INT-120 `private-cache`).
- Store switch: `clearSellerStoreCache` (SEL-100) — prior store keys only.

### Surface isolation

- Query keys: `["notifications", "seller", subjectKey, "list"|"unread-count"]`.
- Mapper drops non-SELLER rows from list response.
- CTA allowlist keeps paths under `/dashboard`.

## 4. Tests / verification

```text
npm run test:run -- tests/unit/sel-420-seller-shell.test.ts
# expected pass

npm run test:run -- tests/unit/buy-140-notifications.test.ts tests/unit/session-int-120.test.ts
# related suite

npx tsc --noEmit
# pass
```

Coverage:

- Shell imports shared account-controls; `surface="seller"`; no seller-only adapter
- Seller list alias + cross-surface row filter
- Mark-read / read-all seller paths
- Notification keys private; logout clears notifications + seller roots
- Logout returns `/login`, clears CSRF/session

## 5. UI freeze

- Sidebar / header geometry, nav, theme toggle, notification bell, profile menu classes/copy preserved.
- Store switch control remains non-menu button (SEL-100 disposition); labels only bind bootstrap text.
- Impersonation banner chrome unchanged; data source prefers server session.

## 6. Out of scope

- ADM-230 admin shell acceptance (separate)
- Notification preferences page (SEL-340)
- Store switch multi-store menu (SEL-100 / UI-080)
- Impersonation end API (admin command) beyond local redirect when server-bound

## 7. Consumer notes

```ts
// Already in dashboard-shell header:
<NotificationCenter surface="seller" />
<ProfileMenu surface="seller" />
// ProfileMenu.logout → logoutSession({ surface: "seller" })
// Notifications: shared/notifications (BUY-140) — do not invent seller adapter
```
