# ADM-120 — Admin overview/read-model query foundation

**Status:** `[x]` completed  
**Actor:** @nikki/opencode  
**When:** 2026-07-17 16:28 WIB  
**Depends on:** ADM-110, INT-160, INT-190 (all `[x]`)  
**Branch:** main

## 1. Objective delivered

- Admin overview + shared list read adapters/schemas/mappers foundation
- Wire admin overview metrics + platform volume when `adminRead=api`
- Permission-gated hooks via ADM-110 codes (`admin.dashboard.read`, domain read codes)
- Server-authoritative money aggregates; no client recompute of money totals
- Unit tests: permission deny, mock path, money mapping

## 2. Frontend modules

| Module | Role |
| --- | --- |
| `shared/api/schemas.ts` | Zod: `adminOverview*`, `adminPlatformVolume*`, merchant/buyer/order/payment/withdrawal/review/inventory DTOs + bounded list envelopes |
| `features/admin/data/contracts.ts` | `AdminOverview`, volume series, `AdminListFilters`, `AdminBoundedList` |
| `features/admin/data/mappers.ts` | DTO → view; money clamp; list filter normalize; metric labels |
| `features/admin/data/overview.ts` | `GET /v1/admin/overview` + platform-volume + audit stream |
| `features/admin/data/merchants.ts` (+ buyers/orders/payments/withdrawals/reviews/inventory) | Schema-validated list/detail foundation for ADM-200+ |
| `features/admin/data/hooks.ts` | Claim-gated `useAppQuery` when api; mock fixtures when mock |
| `features/admin/screens/overview.tsx` | Wire KPI cards + volume bars from server; mock keeps prototype values |
| `shared/query/query-keys.ts` | `admin.overview` + bounded filter keys |

## 3. Backend contract (consumed)

| Endpoint | Permission | Use |
| --- | --- | --- |
| `GET /v1/admin/overview` | `admin.dashboard.read` | KPI aggregates + asOf from meta |
| `GET /v1/admin/overview/platform-volume` | `admin.dashboard.read` | 24 hourly gross IDR buckets |
| `GET /v1/admin/merchants` (+ buyers/orders/payments/withdrawals/reviews/inventory) | domain codes | Bounded list foundation |
| `GET /v1/admin/audit-logs` | `audit.read` | Overview stream (structural; ADM-360 deep search) |

Domain source: `adminRead`.

## 4. Behavior

| Path | Behavior |
| --- | --- |
| Mock | Fixtures only; no transport; prototype KPI copy preserved |
| API + claim | Schema parse → map money/asOf; hooks enabled |
| API without claim | Query disabled (fail-closed); no fabricated zeros for money cards |
| Partial failure | Overview/volume error → `—` values, not invented revenue |
| List foundation | `limit` clamp 50/100; filters in query keys; `keepPrevious` |

## 5. Tests

```text
npm run test:run -- tests/unit/adm-120-admin-overview-reads.test.ts tests/unit/admin-data.test.ts
# 13 passed
```

Coverage:

- Money mapping from overview/merchant/order/payment/withdrawal DTOs
- Fractional money schema rejection
- Platform volume relative height only (amountIdr server truth)
- Permission deny: missing `admin.dashboard.read` / unknown code under `*`
- Mock path never hits transport
- API overview/volume/merchant list paths + asOf from meta
- Query keys include normalized filters + bounded profile

`tsc --noEmit` clean.

## 6. Acceptance

| Criterion | Evidence |
| --- | --- |
| Schema/mapper admin read foundation | `shared/api/schemas.ts` + `mappers.ts` |
| Overview asOf / no fake money zeros | overview api + screen `—` on error |
| Permission-scoped reads | hooks `claimsHavePermission` + ADM-110 codes |
| Bounded list keys / keepPrevious | query-keys + hooks |
| No UI redesign | overview geometry/classes unchanged; mock notes preserved |
| Secrets not in list DTOs | inventory schema redacted; reveal stays POST |

## 7. Out of scope / follow-ups

- ADM-200 merchant list filter UX + NumberedPageList when BE numbered meta
- ADM-210 buyer support surface completeness
- ADM-300+ domain command/evidence wiring
- ADM-360 full audit search schema
- System health / action queue still snapshot chrome (ADM-370)
