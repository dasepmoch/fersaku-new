# SEL-210 — Product list server search/filter/sort/bounded result

**Status:** `[x]` completed 
**Actor:** @nikki/opencode 
**When:** 2026-07-17 
**Depends on:** SEL-100, INT-190 (`[x]`)

## 1. Objective delivered

- Store-scoped seller product list for `/dashboard/products` via `GET /v1/stores/{storeId}/products`
- Schema/mapper → existing `CatalogProduct` (+ optional exhaustive `status`)
- Bounded first result (`SELLER_PRODUCT_LIST_LIMIT = 50`, BoundedNoPaging)
- Existing SearchBox wired with debounce + keepPrevious; FilterButton chrome unchanged (no menu invented)
- Empty list: empty grid only — no fixture inject, no new empty copy, no layout collapse hacks
- No pagination control added (UI-080 / UXE-012 for expansion / empty composition)

## 2. Frontend modules

| Module | Role |
| --- | --- |
| `features/catalog/contracts.ts` | `ProductStatus`, `SellerProductListFilters`, `SELLER_PRODUCT_LIST_LIMIT` |
| `features/catalog/mappers.ts` | Status exhaustive map; `applySellerProductListFilters`; list status label |
| `features/catalog/api.ts` | Store-encoded list adapter; mock/API; filter + bound |
| `features/catalog/hooks.ts` | Debounced search; filter query keys; keepPrevious; list invalidation prefix |
| `shared/query/query-keys.ts` | `products(storeId, filters)` → `…/products/bounded/{filters}` |
| `features/seller/ui/pieces.tsx` | Optional `value`/`onChange` on existing SearchBox (no geometry change) |
| `features/seller/screens/products/list.tsx` | Wire search + truthful status label |

## 3. Backend contract (consumed)

| Endpoint | Use |
| --- | --- |
| `GET /v1/stores/{storeId}/products` | Seller list; includes `status`; order `created_at DESC, id DESC` |

**Note:** OpenAPI/handler currently has **no** search/status/limit query params. Adapter applies normalized search/status/type filters and hard bound **client-side** over the store-scoped response. Server-side filter/limit remains residual (not blocking list activation under BoundedNoPaging). Do not invent cursor/page UI.

Domain source: `sellerCatalog`.

## 4. Tests

```text
npm run test:run -- tests/unit/sel-210-seller-products-list.test.ts
# expected: pass

# related store-key shape (still store-scoped prefix)
npm run test:run -- tests/unit/int-150-current-store.test.ts tests/unit/sel-100-seller-bootstrap.test.ts
```

Coverage:

- Store-scoped path + encodeURIComponent
- Empty list (no fixture inject)
- Search / status / type filter mapping
- Bound cap (no local full-catalog paging)
- Status exhaustive (unknown ≠ published)
- Query keys: store + filters + `bounded`
- Mock never hits transport
- Foreign store key isolation

## 5. Acceptance

| Criterion | Evidence |
| --- | --- |
| Schema + mapper → CatalogProduct | mappers + schemas status optional |
| Store-scoped via SEL-100 | `useSellerStoreId` + path `storeId` |
| BoundedNoPaging | `SELLER_PRODUCT_LIST_LIMIT` + no TablePagination |
| Existing search/filter only | SearchBox value/onChange; FilterButton unchanged |
| Debounce + cancel + keepPrevious | `useDebouncedProductSearch` + RQ signal + keepPrevious |
| Empty: no inject / no new empty UI | `products.map` only; empty array → empty grid |
| No UI redesign | list.tsx geometry/classes preserved |
| Cross-store isolation | storeId in path + query keys |

## 6. Out of scope

- SEL-220 create/edit/publish/archive commands
- Server-side search/limit query (BE residual)
- Pagination / cursor UI (UI-080)
- Empty-state copy composition (UXE-012 / UI-080)
- Inventory list (SEL-240)

## 7. Residual / launch notes

1. **UXE-012** still open for product-owner empty composition if product wants non-empty chrome at zero rows.
2. BE unbounded list until limit/search land server-side; FE hard-caps display to launch bound.
3. FilterButton remains visual-only (no filter menu without UI-080).
