# QLT-105 — Activate all CI gates as required checks (no no-op jobs)

- Status: **[x] complete** (parent/framework)
- Owner: @nikki/opencode
- UI impact: **none**
- Scope: Harden CI so every required job runs real suites and fails on drift/empty suites

## 1. Deliverables

| Item | Path |
| --- | --- |
| Frontend + API e2e workflow | `.github/workflows/ci.yml` |
| Backend + OpenAPI + integration | `.github/workflows/backend-ci.yml` |
| Non-empty suite guards | `scripts/ci-assert-suite.mjs` |
| Go test executed-package guard | `scripts/ci-assert-go-tests.sh` |
| Branch protection checklist | `docs/CI-BRANCH-PROTECTION.md` |
| Integration no silent skip in CI | `backend/test/integration/foundation_test.go` |
| Router inventory drift fix (SEL-260/270 + KYC content) | `backend/test/contract/router_inventory.txt` |
| Regenerated OpenAPI TS (api:check clean) | `shared/api/generated/openapi.ts` |

## 2. Required job topology (active — no placeholders)

### Frontend quality (`ci.yml`)

| Job | Real work | No-op guard |
| --- | --- | --- |
| `frontend-static` | format, lint, typecheck, unit/coverage, audit; asserts unit + security-negative files | `ci-assert-suite.mjs frontend-unit` + `security-negative` |
| `frontend-build` | production build + bundle budget | needs static |
| `frontend-mock-e2e` | Playwright smoke/critical/a11y/visual (desktop+mobile) | `ci-assert-suite.mjs frontend-mock-e2e` (specs + ≥5 baselines each) |
| `cross-stack-api-e2e` | `e2e-api-stack.sh` → migrate/seed/api/worker → `test:e2e:api` (harness + INT-190) | `ci-assert-suite.mjs cross-stack-api-e2e` |

### Backend CI (`backend-ci.yml`)

| Job | Real work | No-op guard |
| --- | --- | --- |
| `backend-unit` | fmt, vet, `go test ./...`, sqlc dirty-diff | `ci-assert-go-tests.sh` |
| `backend-race` | `go test -race ./...` | `ci-assert-go-tests.sh` |
| `backend-docker` | docker build api + worker | image build fails if Dockerfile broken |
| `openapi-contract` | `api:lint`, `api:bundle`, `api:check` (codegen dirty-diff), `go test ./test/contract` | suite assert + go assert + bundle size |
| `backend-integration` | service Postgres/Redis, migrate, `go test -tags=integration`, security/idempotency subset ≥5 PASS | suite assert; `DATABASE_URL` **Fatal** under `CI`/`QLT_REQUIRE_INTEGRATION` (no skip-pass) |

**Removed:** `if: false` placeholders for `cross-stack-api-e2e` and `backend-integration`.

**No** `continue-on-error` on any P0 job.

## 3. Checklist mapping (07 §QLT-105)

- [x] OpenAPI lint/bundle/codegen dirty-diff/router coverage (openapi-contract job)
- [x] Backend tagged integration required (Postgres service + `go test -tags=integration`)
- [x] API-mode Playwright harness required (cross-stack-api-e2e; QLT-215 + INT-190)
- [x] Mock smoke/critical/a11y/visual required (frontend-mock-e2e)
- [x] Security/contract/tenant/idempotency negatives required (unit assert + integration security subset; not optional)
- [x] Branch protection documentation (`docs/CI-BRANCH-PROTECTION.md`) — operators must enable exact job names; no invented secrets

## 4. Local validation (2026-07-17)

```text
node scripts/ci-assert-suite.mjs openapi-contract
# → OK inventory(310) + generated types

node scripts/ci-assert-suite.mjs backend-integration
# → OK files=30

node scripts/ci-assert-suite.mjs frontend-unit
# → OK files=83

node scripts/ci-assert-suite.mjs frontend-mock-e2e
# → OK baselines desktop=14 mobile=14

node scripts/ci-assert-suite.mjs cross-stack-api-e2e
# → OK harness + INT-190 + seed

node scripts/ci-assert-suite.mjs security-negative
# → OK 6 unit files

python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); ..."
# → workflow yaml: OK

cd backend && go test -count=1 ./test/contract/
# → ok  (after inventory sync for customers/reviews/kyc content)

npm run api:generate && npm run api:check
# → exit 0 (generated types up to date)

../scripts/ci-assert-go-tests.sh openapi-contract -- -count=1 ./test/contract/...
# → OK
```

Full clean-checkout CI (integration Postgres + Playwright API stack) runs on GitHub Actions runners; local host may lack Docker/Postgres at agent time. Guards ensure those jobs cannot green-wash empty suites.

## 5. Intentional failure cases (must break CI)

| Drift | Failing job |
| --- | --- |
| OpenAPI lint / router inventory mismatch | `openapi-contract` |
| Dirty `shared/api/generated/openapi.ts` | `openapi-contract` (`api:check`) |
| Dirty sqlc gen | `backend-unit` |
| Empty unit/e2e/integration file tree | assert steps |
| `DATABASE_URL` missing under CI | `backend-integration` (Fatal, not Skip) |
| Cross-tenant / CSRF / idempotency fail | unit coverage + integration security subset |
| API stack/seed/harness/INT-190 fail | `cross-stack-api-e2e` |
| Visual baseline missing | `frontend-mock-e2e` assert |

## 6. Secrets policy

Only ephemeral local/compose credentials (Postgres `fersaku_local`, fake Xendit, seed personas). No production API keys, org secrets, or invented tokens required for these gates.

## 7. Files touched

```text
.github/workflows/ci.yml
.github/workflows/backend-ci.yml
scripts/ci-assert-suite.mjs
scripts/ci-assert-go-tests.sh
docs/CI-BRANCH-PROTECTION.md
backend/test/integration/foundation_test.go
backend/test/contract/router_inventory.txt
shared/api/generated/openapi.ts
package.json
.gitignore
TASK/07-TESTING-ROLLOUT-DOD.md
TASK/09-EXECUTION-STATUS.md
TASK/evidence/QLT-105/20260717-1850-nikki.md
```

## 8. Capability cells

Parent `QLT-105` = framework: all listed jobs are real required checks with no-op guards.

| Capability | QLT-105 cell | Note |
| --- | --- | --- |
| `foundation-pilot` | `[x]` | Gates cover INT-000/010 contract, integration, API harness + INT-190 |
| Other domain cells | remain `[ ]` | Domain canary still needs domain-specific QLT-200/220/etc. cells; parent does not wait on them |

## 9. Acceptance

- [x] No required job that passes because it finds no tests/files
- [x] OpenAPI lint/bundle/codegen dirty-diff/router coverage active
- [x] Security/contract/tenant/idempotency negatives required (not continue-on-error)
- [x] Mock smoke/critical/a11y/visual + API harness paths active
- [x] Branch protection docs; no invented secrets
