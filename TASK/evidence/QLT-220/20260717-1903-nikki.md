# QLT-220 — Cross-stack API-mode Playwright (parent framework)

- Status: **[x] complete** (parent/framework only)
- Owner: @nikki/opencode
- UI impact: **none**
- Depends on: QLT-215 `[x]`, INT-190 `[x]`
- Scope: Continuous cross-stack API E2E harness + mock/API project registration + real auth helpers + required CI + co-evolution rule. **Not** full domain matrix (§3.7 `QLT-220 API E2E` cells remain `[ ]`).

## 1. Deliverables

| Item | Path |
| --- | --- |
| Co-evolution rule doc | `docs/QLT-220-API-E2E-COEVOLUTION.md` |
| Parent framework asserts | `tests/e2e/api/qlt-220-parent-framework.spec.ts` |
| Real auth + ephemeral storageState | `tests/e2e/api/helpers/auth.ts` |
| Required harness sample | `tests/e2e/api/harness-health.spec.ts` |
| Required INT-190 sample | `tests/e2e/api/int-190-vertical-slice.spec.ts` |
| Mock Playwright registration | `playwright.config.ts` (`testIgnore **/api/**`, desktop/mobile) |
| API Playwright registration | `playwright.api.config.ts` (`api-desktop-chromium`, DATA_SOURCE=api) |
| Stack orchestration | `scripts/e2e-api-stack.sh` |
| CI required job | `.github/workflows/ci.yml` → `cross-stack-api-e2e` |
| Non-empty + parent markers | `scripts/ci-assert-suite.mjs` → `cross-stack-api-e2e` + `qlt-220-api-e2e` |
| npm assert | `package.json` → `ci:assert:api-e2e` |
| Branch protection name | `docs/CI-BRANCH-PROTECTION.md` |

## 2. Parent vs cells

| Level | Claimed by this task |
| --- | --- |
| Parent `QLT-220` | Distinct mock/API projects, stack+seed, real auth (ephemeral cookies), no mock-network policy, secret hygiene, required samples (health + INT-190), CI guards, co-evolution doc |
| §3.7 `QLT-220 API E2E` cells | **Remain `[ ]`** — domain canary still requires cell evidence |
| `07` public/buyer/seller/admin flow bullets | Samples only (INT-190 + parent); full claim is per-domain cell |

Parent must never wait on descendant cells. Domain tasks add `tests/e2e/api/<domain>-*.spec.ts` in the same PR as live FE↔BE paths.

## 3. Hard rules verified

| Rule | Evidence |
| --- | --- |
| Distinct mock vs API projects | mock: `desktop-chromium`/`mobile-chromium` + ignore `api/**`; API: `api-desktop-chromium` only |
| Auth via real API flow | `loginViaApi` → `POST /v1/auth/login`; optional `loginViaUiOrApi` |
| Ephemeral cookies only | `test-results/api/.auth/*.json` (gitignored); `clearEphemeralAuthState` |
| No mock simulator network in API mode | parent blocklist + `NEXT_PUBLIC_DATA_SOURCE=api`; probe on live origin |
| No secret leak in traces/annotations | `maskToken` / `sanitizeAuthSummary`; traces retain-on-failure only |
| INT-190 + harness-health required | suite assert + CI job runs full `test:e2e:api` |
| Parent `[x]` ≠ all domain bullets/cells | §3.7 QLT-220 column still `[ ]` for all capabilities |

## 4. Tests run (2026-07-17)

```text
node scripts/ci-assert-suite.mjs cross-stack-api-e2e
# → OK … specs=3

node scripts/ci-assert-suite.mjs qlt-220-api-e2e
# → OK qlt-220 parent harness + mock/API registration + auth + INT-190/health …

# Stack already up; re-seed QLT-110 then:
E2E_API_SKIP_WEBSERVER=1 E2E_API_HAS_NEXT=0 \
 PLAYWRIGHT_API_BASE_URL=http://127.0.0.1:18080 \
 APP_ENV=local API_INTERNAL_URL=http://127.0.0.1:18080 \
 MAILPIT_URL=http://127.0.0.1:8025 NEXT_PUBLIC_DATA_SOURCE=api \
 npx playwright test -c playwright.api.config.ts
# → 23 passed, 4 skipped (Next-edge-only when HAS_NEXT=0)

# With Next webServer (local reuse):
# → 27 passed (includes Next rewrite no-mock-network probe)
```

Full list log: `TASK/evidence/QLT-220/playwright-qlt220.log`

Disposable credentials only: compose `fersaku:fersaku_local`, seed personas, fake Xendit token — no production secrets.

## 5. Checklist mapping (07 §QLT-220)

- [x] Distinct mock/API Playwright registration
- [x] Isolated stack + migrate + seed + health + Next
- [x] Real auth; ephemeral storage state cleaned
- [x] No internal DB force-success outside seed/callback helpers
- [x] Trace/secret hygiene
- [x] Co-evolution documented; CI non-empty guards
- [ ] Full public/checkout/buyer/seller/admin matrix — **capability cells** (§3.7)

## 6. Capability matrix

§3.7 `QLT-220 API E2E` cells remain `[ ]` for all capabilities including `foundation-pilot`. Parent `[x]` only means harness ready; domain canary still requires cell evidence. `foundation-pilot` already has INT-190 sample coverage usable when claiming that cell later.

## 7. Files touched

```text
docs/QLT-220-API-E2E-COEVOLUTION.md
docs/CI-BRANCH-PROTECTION.md
playwright.config.ts
playwright.api.config.ts
tests/e2e/api/helpers/auth.ts
tests/e2e/api/helpers/index.ts
tests/e2e/api/qlt-220-parent-framework.spec.ts
scripts/ci-assert-suite.mjs
.github/workflows/ci.yml
package.json
TASK/07-TESTING-ROLLOUT-DOD.md
TASK/09-EXECUTION-STATUS.md
TASK/evidence/QLT-220/20260717-1903-nikki.md
TASK/evidence/QLT-220/playwright-qlt220.log
```

## 8. Follow-up

- Claim/fill `foundation-pilot` (and other) QLT-220 cells with domain-depth E2E before canary.
- Domain slices: checkout intent/callback, buyer delivery, seller auth/finance, admin RBAC — same PR as product work.
- Optional: dedicated Playwright setup project that writes ephemeral storageState once per worker (helpers already support write/clear).
