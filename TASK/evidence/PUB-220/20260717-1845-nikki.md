# PUB-220 — Public platform status

**Status:** `[x]` completed  
**Actor:** @nikki/opencode  
**When:** 2026-07-17 18:45 WIB  
**Depends on:** INT-180 **if live multi-service status** (soft — not required for informational launch); QLT-320 framework (soft)  
**Branch:** main

## 1. Product decision (authoritative)

| Surface | Disposition | Notes |
| --- | --- | --- |
| `/status` page | **Non-live informational** | Not marketed as full live multi-service operational status |
| `GET /v1/status` | **IMPLEMENT** (sanitized) | Process identity only: `service`, `version`, `appEnv`, `uptimeSeconds` |
| Multi-service health rows | **Not reported** | Existing service names kept; only “API & webhooks” may show Reachable when status DTO loads |
| Uptime % / SLA | **Forbidden** | Never invent `99.x%` |
| Incident history | **Empty STATIC** | No public incident feed on `/v1/status`; accordion shows empty informational copy |
| `/metrics`, admin health, topology | **Private** | Never called from public page |
| Outage | **Unavailable** | Neutral banner + Unavailable rows; never green “operational” default |

Full operational aggregate / incident provider remains a future dependency (status provider + INT-180 readiness truthfulness) before the page can be marketed as live.

## 2. Files

| Path | Role |
| --- | --- |
| `features/platform-status/contracts.ts` | View models + service name freeze |
| `features/platform-status/mappers.ts` | Truthful mapping; no green on unknown/unavailable |
| `features/platform-status/api.ts` | Adapter: mock / `GET /v1/status` / unavailable |
| `features/platform-status/index.ts` | Public exports |
| `app/(resources)/status/page.tsx` | Wire existing chrome to adapter |
| `shared/api/schemas.ts` | `StatusDataDto` export |
| `tests/unit/pub-220-public-status.test.ts` | Unit coverage |
| `TASK/03-PUBLIC-AUTH-CHECKOUT-BUYER.md` | Checklist `[x]` |
| `TASK/06-ENDPOINT-CONTRACT-MATRIX.md` | Public status U → M |
| `TASK/10-ROUTE-AND-CONTROL-DISPOSITION.md` | `/status` disposition |
| `TASK/09-EXECUTION-STATUS.md` | PUB-220 → `[x]` |

## 3. Behavior

| State | UI |
| --- | --- |
| Mock `publicCatalog` | Informational headline; API row Reachable (fixture); others Not reported; no incidents |
| API success | Same; detail includes sanitized service/version/env; process uptime secondary on API row only |
| API failure | Headline “Status signal unavailable”; all rows Unavailable; neutral (non-green) banner |
| Incidents | Always empty at launch |

### Cache / revalidate

- `PUBLIC_STATUS_REVALIDATE_SECONDS = 60`
- `PUBLIC_STATUS_CACHE_TAG = "public-platform-status"`
- Page: `export const revalidate = PUBLIC_STATUS_REVALIDATE_SECONDS`

### Authority boundary

- Public page must not claim dependency topology, payment rail health, or SLA uptime from process status alone
- Admin providers/system health remains ADM-370 (authenticated)
- Raw `/metrics` restricted / not a UI data source

## 4. Tests / verification

```text
npm run test:run -- tests/unit/pub-220-public-status.test.ts
# 10 passed

npx eslint features/platform-status app/(resources)/status/page.tsx tests/unit/pub-220-public-status.test.ts --max-warnings=0
# pass
```

Coverage:

- Status envelope schema accept
- Process uptime formatting (no %)
- Map DTO → informational (not “all systems operational”)
- Unavailable never green
- Mock adapter
- API path calls only `/v1/status` with schema
- Outage → unavailable, no invented uptime/incidents
- Page source forbids hardcoded operational/uptime/incident fixtures

## 5. UI freeze

- Same card/list/accordion geometry on `/status`
- Existing lucide icons; Info icon for non-ok banner (CheckCircle only if overall ok — not used at launch overall)
- No new marketing layout; copy slots filled with truthful informational strings

## 6. Explicit non-goals (this task)

- Live multi-service public status aggregate
- Public incident CMS/feed
- Binding admin shell “All systems operational” (ADM-370 / separate)
- Exposing `/health/*` or `/metrics` to the browser

## 7. Rollback

Revert `features/platform-status`, status page wiring, tests, and disposition/status docs if product re-opens full live status; then require a public aggregate endpoint before any green multi-service claim.
