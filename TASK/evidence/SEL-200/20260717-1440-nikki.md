# SEL-200 — Overview metrics, revenue/order/product summary, traffic

**Status:** `[x]` completed 
**Actor:** @nikki/opencode 
**When:** 2026-07-17 14:40 WIB 
**Depends on:** SEL-100, INT-190 (`[x]`)

## 1. Objective delivered

- Store-scoped seller overview/analytics read model for `/dashboard`
- Parallel queries: analytics overview, traffic, finance summary, revenue series, products, orders
- Consistent `asOf` from envelope `meta.timestamp` (never client-fabricated financial truth)
- Range/channel filters in query keys + server query; `keepPrevious` on refetch
- Partial failure shows existing “Gagal memuat” / “—” without fabricating zeros
- No fake export/download: row detail buttons disabled (no handler); export not invented in UI
- Mock mode preserves prototype geometry/copy notes; API mode uses server aggregates only

## 2. Frontend modules

| Module | Role |
| --- | --- |
| `shared/api/schemas.ts` | Zod: `analyticsOverview*`, `analyticsTraffic*` |
| `features/seller/analytics/contracts.ts` | View models for overview + traffic cards/table |
| `features/seller/analytics/mappers.ts` | DTO → view; date range; channel UI↔wire; empty metrics |
| `features/seller/analytics/api.ts` | `GET .../analytics/overview` + `.../traffic` |
| `features/seller/analytics/hooks.ts` | Store-scoped queries + keepPrevious |
| `features/seller/analytics/mock.ts` | Demo fixtures (prototype parity) |
| `features/seller/screens/overview.tsx` | Wire metric cards + revenue range + chart |
| `features/seller/components/traffic-analytics.tsx` | Wire filters/table/metrics; disable no-op detail |
| `features/finance/api.ts` + `hooks.ts` | Revenue `days` query param + keepPrevious |
| `shared/query/query-keys.ts` | `analyticsOverview`, `analyticsTraffic`, revenue filters |

## 3. Backend contract (consumed)

| Endpoint | Use |
| --- | --- |
| `GET /v1/stores/{storeId}/analytics/overview?from&to&timezone` | Gross, orders, conversion, channels |
| `GET /v1/stores/{storeId}/analytics/traffic?from&to&timezone&channel` | Daily rows → channel-aggregated table |
| `GET /v1/stores/{storeId}/finance/summary` | Available balance card (ledger) |
| `GET /v1/stores/{storeId}/finance/revenue?days` | Revenue chart series |

Domain sources: `sellerOperations` (analytics), `sellerFinance` (balance/revenue).

## 4. Tests

```text
npm run test:run -- tests/unit/sel-200-seller-overview-analytics.test.ts
# 10 passed
```

Coverage:

- Empty/new store metrics (zeros, no NaN)
- Mapping gross/orders/conversion from DTO
- Store-scoped query keys + range/channel isolation
- Traffic aggregation by channel
- Date range boundaries (7/30/90)
- API path encoding + query params
- Mock fixtures never hit transport
- Foreign store key isolation

## 5. Acceptance

| Criterion | Evidence |
| --- | --- |
| Bounded overview read model + asOf | mappers + api meta.timestamp |
| Cards/charts mapped without redesign | overview.tsx + traffic-analytics.tsx geometry preserved |
| Range/channel in keys + server | query-keys + api query |
| Revenue/fee/net not from UI rows | finance summary + analytics gross from API |
| Partial failure no fake zeros | “—” / “Gagal memuat” when isError && !data |
| keepPrevious / no blank shift | useAppQuery keepPrevious on analytics + revenue |
| Export | no active export control on overview; detail CTA disabled |
| Cross-store isolation | storeId in path + query keys |
| Visual baseline | mock notes preserved; layout classes unchanged |

## 6. Out of scope

- SEL-250 seller order read-model completeness (orders still use existing adapter)
- SEL-400 full finance authority
- Admin overview (ADM-120)
- Inventing traffic export button or detail drawer (UI-080)
- UI redesign / new metric chrome
