# INT-175 — User-scoped profile media contract

**Status:** `[x]` completed (launch disposition)  
**Actor:** @nikki/opencode  
**When:** 2026-07-17 12:57 WIB  
**Depends on:** INT-000, INT-120 (`[x]`); INT-180/185 N/A (capability not activated)

## 1. Product decision (authoritative)

| Choice | Decision |
| --- | --- |
| Launch scope | Personal avatar/photo upload is **not required for launch** |
| Disposition | **`DISABLED` / `OUT-OF-SCOPE`** on all buyer/seller/admin personal media controls |
| User-scoped API | **Do not mount** `/v1/me/objects...` (or equivalent PROFILE purpose upload) for launch |
| Store objects | **Forbidden** for personal media: never use `POST/GET /v1/stores/{storeId}/objects...` for buyer/admin/seller avatar |
| Re-open criteria | Explicit product re-open + full user-scoped OpenAPI contract + INT-180/185 scan/lifecycle before any IMPLEMENT |

Rationale: existing UI is either static initials (buyer) or already-disabled photo control (admin). No domain adapter calls store object endpoints for profile photo. Implementing a full me/objects lifecycle without launch need would force INT-180/185 and widen attack surface without product value.

## 2. Existing UI inventory (no redesign)

| Surface | Personal media control | Launch state |
| --- | --- | --- |
| `features/buyer/screens/buyer-profile.tsx` | Initials-only circle (“NP”); no upload button | Static display — no upload path |
| `features/admin/screens/access/profile.tsx` | “Upload new photo” `disabled` + title tooltip | Authoritative **DISABLED** (markup unchanged) |
| Seller settings (`seller-settings.tsx`) | Profile text fields only; no avatar upload control | No personal media control to enable |
| Customer/order “avatar” fields | Display initials from mock/read models | Not profile media upload |

## 3. Architecture enforcement

| Path | Role |
| --- | --- |
| `tests/unit/architecture-boundaries.test.ts` | `INT-175: forbids store-scoped objects for personal profile media` |

Gate asserts:

1. No profile/personal-media feature/app surface wires `/v1/stores/.../objects` (or `/objects/uploads`) for avatar/photo/PROFILE_ASSET.
2. Admin profile retains “Upload new photo” + `disabled` and has no store-object or `/v1/me/objects` path.
3. Buyer profile has no store-object or upload path.

Store-scoped object routes remain valid for **product/store assets** (SEL-230) outside personal profile context.

## 4. Contract / matrix updates

| Doc | Update |
| --- | --- |
| `TASK/02-FOUNDATION-TRANSPORT-AUTH.md` §INT-175 | Decision checked; deferred checklist items marked; AC for launch gate |
| `TASK/06-ENDPOINT-CONTRACT-MATRIX.md` | Personal profile media row: launch deferred, no route, store objects invalid |
| `TASK/10-ROUTE-AND-CONTROL-DISPOSITION.md` | `/account/profile`, `/admin/profile`, buyer photo disposition = DISABLED/OUT-OF-SCOPE for launch |
| `TASK/09-EXECUTION-STATUS.md` | INT-175 → `[x]` |

## 5. Explicit non-goals (this task)

- No OpenAPI `/v1/me/objects` operations
- No BE upload stub handlers for personal media
- No UI redesign, no enabling admin photo button, no buyer upload control
- No use of `PROFILE_ASSET` via store-scoped object service for avatars
- Domain wiring BUY-120 / SEL-340 / ADM-230 must keep photo **DISABLED** until product re-opens INT-175 as IMPLEMENT

## 6. Tests

```text
npm run test:run -- tests/unit/architecture-boundaries.test.ts
# Test Files  1 passed (1)
# Tests  12 passed (12)
# Duration  ~267ms
```

Admin photo button title only: authoritative OUT-OF-SCOPE reason (still `disabled`, same geometry).

## 7. Rollback

Revert evidence + architecture test + disposition/matrix/status rows if product re-opens personal media as IMPLEMENT; then replace with full me/objects PROFILE contract (separate claim).
