# ADM-370 — Providers, system health, fees, emergency controls

**Status:** `[x]` completed 
**Date:** 2026-07-17 
**Agent:** @nikki/opencode 
**Depends on:** ADM-110 (`[x]`); INT-180/185 if runtime controls active (soft); QLT-320 framework (soft/co-req) 
**Branch:** main 

## 1. Objective delivered

Wire admin providers/system when admin domain is `api`:

- Truthful **provider/component health** (never fake green/OK when BE says degraded/unknown/down)
- Exactly three **emergency switches** (`SELLER_REGISTRATION`, `QRIS_CHECKOUT`, `WITHDRAWALS`) with version/auth/reason/ticket/idempotency
- **Fee read + pure preview** (read-only fields; preview never publishes)
- Existing chrome only (Emergency switchboard, providers vault, system settings, fee preview modal)

Maintenance banner and fourth “maintenance mode” toggle: **truthful disposition** — disabled / non-authoritative on api path (not a BE switch).

## 2. Files

| Path | Role |
| --- | --- |
| `features/admin/operations/emergency/data.ts` | Switch constants, view models |
| `features/admin/operations/emergency/mappers.ts` | Health classification + DTO mappers |
| `features/admin/operations/emergency/api.ts` | System/providers/emergency/fees transport |
| `features/admin/operations/emergency/hooks.ts` | Queries/mutations + visibility-aware poll (45s) |
| `features/admin/operations/emergency/mock.ts` | Snapshot fixtures (non-live only) |
| `features/admin/operations/emergency/index.tsx` | Wire switchboard (dialog + conflict refresh) |
| `features/admin/operations/emergency.tsx` | Public exports |
| `features/admin/domains/providers/index.tsx` | Wire vault + list from truthful health |
| `features/admin/domains/providers/generic-provider.tsx` | Status kind styling |
| `features/admin/screens/system.tsx` | Fee fields + maintenance disposition from snapshot |
| `features/admin/commerce/fee-policy-preview.tsx` | Active policy + pure preview on api |
| `shared/api/schemas.ts` | ADM-370 Zod DTOs/envelopes |
| `shared/query/query-keys.ts` | `emergencyControls`, `systemFees` |
| `tests/unit/adm-370-admin-providers-system.test.ts` | Unit coverage |
| `TASK/05-ADMIN-CONSOLE.md` | FE checklist `[x]` |
| `TASK/06-ENDPOINT-CONTRACT-MATRIX.md` | Emergency U → M |
| `TASK/09-EXECUTION-STATUS.md` | `[x]` claim released |

## 3. Endpoints

| Method | Path | Permission | Notes |
| --- | --- | --- | --- |
| GET | `/v1/admin/system` | `platform.emergency` | Snapshot: emergency + componentHealth + feePolicyVersion |
| GET | `/v1/admin/system/emergency-controls` | `platform.emergency` | Three switches |
| POST | `/v1/admin/system/emergency-controls` | `platform.emergency` | `switchName`, `enabled`, `reason`, `incidentTicket?`, `expectedVersion`; Idempotency-Key + X-Audit-Reason + recent authentication + If-Match version |
| GET | `/v1/admin/providers` | `payments.read` | Xendit (and list) health; no secrets |
| GET | `/v1/admin/system/fees` | `platform.fees.preview` | Active policy read-only |
| POST | `/v1/admin/system/fees/preview` | `platform.fees.preview` | Pure calculator; never persists |

## 4. Behavior

| Surface | Transport | Notes |
| --- | --- | --- |
| Providers vault (api) | GET system ∥ GET providers | Prefer `componentHealth`; never invent healthy R2/Redis/mail if only Xendit returned |
| Status labels | Mapper | OK→Live; DEGRADED/DOWN/unknown never painted green |
| Emergency toggle (api) | POST emergency-controls | No optimistic success; 409 refreshes list and keeps reason |
| Maintenance banner | UI only | Disabled on api with clear copy |
| System fee fields | GET system/fees | Read-only; publish buttons stay disabled |
| Fee preview modal | POST fees/preview | Debounced pure calc; mock uses local launch math |
| Health poll | 45s | Paused when `document.visibilityState !== "visible"` |
| Mock | fixtures | Local seed + client audit on write |

## 5. Tests

```bash
npx vitest run tests/unit/adm-370-admin-providers-system.test.ts
```

**Result:** 25 passed.

Coverage: schemas, truthful health classification, three-switch mapping (reject fourth), system/providers compose + partial failure, emergency reason/version/idempotency/conflict, fee read+preview pure, permissions, query keys.

## 6. AC mapping

| AC | Evidence |
| --- | --- |
| Dependency degraded | `classifyHealthStatus` / overallKind unit tests + vault tone |
| Emergency version conflict | `setAdminEmergencyControl` 409 + UI refresh keeps reason |
| Missing proof/reason/permission | API `requireRecentMfa` + reason min 12 + write gate |
| Exactly three switches | `mapEmergencyControlList` + EMERGENCY_SWITCH_NAMES |
| Fee preview pure | POST preview only; publish buttons disabled |
| Existing chrome | No layout redesign |

## 7. Follow-ups (out of scope)

- QLT-320 `admin-runtime-audit` canary cell before production cutover
- INT-180/185 deeper runtime probes when fully active (FE already surfaces BE status)
- Dedicated `system.read` vs `platform.emergency` split if product expands read-only staff roles
