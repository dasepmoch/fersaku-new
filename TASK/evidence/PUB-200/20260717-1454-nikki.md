# PUB-200 — Contact form submission disposition

**Status:** `[x]` completed (launch disposition) 
**Actor:** @nikki/opencode 
**When:** 2026-07-17 14:54 WIB 
**Depends on:** INT-025, INT-100, INT-130 (`[x]`)

## 1. Product decision (authoritative)

| Choice | Decision |
| --- | --- |
| Launch scope | Public contact form submit **not required for launch** |
| Disposition | **`DISABLED` / `OUT-OF-SCOPE`** on `/contact` “Kirim pesan” in API/live |
| Mode gate | `getDomainSource("publicCatalog") === "mock"` → prototype `setSent` may remain; **`api` / `disabled` → `disabled` + authoritative title** |
| Backend contact | **Do not implement** `POST /v1/public/contact-messages` (or equivalent) for launch |
| Success UI | Never set `sent=true` / “Pesan terkirim” in API mode without durable backend acceptance |
| Re-open criteria | Explicit product re-open + OpenAPI contact op via INT-000 + UXE-010/UI-080 error/pending composition + anti-spam/mail adapter |

Rationale: matrix row is `D` (no route mounted); snapshot `ContactPage` has no field-error/pending/general-error region (`UXE-010`); active local `setSent(true)` is forbidden fake-success in API mode.

## 2. Existing UI inventory (no redesign)

| Surface | Control | Launch state |
| --- | --- | --- |
| `app/(company)/contact/page.tsx` | “Kirim pesan” button | Same geometry/copy; API/disabled → `disabled` + title; mock may stay interactive prototype |
| Success panel “Pesan terkirim.” / “Kirim pesan lain” | Existing composition | Reachable only via mock prototype path; not via API fake-success |

Disabled title (API/disabled only): 
`Contact submit is out of scope for launch (PUB-200 deferred)`

Domain key: `publicCatalog` (contact is a public marketing surface; no separate contact domain).

## 3. Architecture enforcement

| Path | Role |
| --- | --- |
| `tests/unit/architecture-boundaries.test.ts` | `PUB-200: contact submit is authoritatively disabled in API mode` |

Gate asserts:

1. `ContactPage` reads `getDomainSource("publicCatalog")` and sets `disabled={!contactSubmitEnabled}` with launch title.
2. Submit handler returns early when not mock-enabled; unconditional `onClick={() => setSent(true)}` is forbidden.
3. Page does not mount contact transport (`/v1/public/contact`, `contact-messages`, etc.).

## 4. Contract / matrix updates

| Doc | Update |
| --- | --- |
| `TASK/03-PUBLIC-AUTH-CHECKOUT-BUYER.md` §PUB-200 | Launch DISABLED checked; IMPLEMENT path deferred |
| `TASK/10-ROUTE-AND-CONTROL-DISPOSITION.md` | `/contact` + inventory + UXE-010 status |
| `TASK/06-ENDPOINT-CONTRACT-MATRIX.md` | Contact row → launch DISABLED note |
| `TASK/09-EXECUTION-STATUS.md` | PUB-200 → `[x]` |

## 5. Explicit non-goals (this task)

- No contact backend or OpenAPI operations
- No new error/pending UI (UXE-010 / UI-080)
- No fake-success or silent no-op in API mode
- No UI redesign / snapshot copy change beyond disabled affordance

## 6. Tests

```text
npm run test:run -- tests/unit/architecture-boundaries.test.ts
```

## 7. Rollback

Revert evidence + architecture test + contact mode-gate + disposition/status if product re-opens contact as IMPLEMENT; then replace with exact public contact command + approved state composition.
