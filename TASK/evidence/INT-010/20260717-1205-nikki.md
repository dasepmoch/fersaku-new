# INT-010 — Generate transport DTO and runtime schema; keep view models

- Status: **complete**
- Owner: @nikki/opencode
- Base claim: `TASK/09-EXECUTION-STATUS.md` INT-010 `[~]`
- UI impact: **none** (no product JSX/layout/style changes)
- Depends on: INT-000 `[x]`

## 1. Objective delivered

```text
generated OpenAPI types
  -> Zod runtime response schemas
  -> feature mapper (catalog example)
  -> existing feature contract/view model
```

View models in `features/**/contracts.ts` unchanged as presentation authority. Screens not rewired (domain tasks own live adapter `schema:` + mapper calls).

## 2. Artefacts

| Deliverable | Path |
| --- | --- |
| Generated TS types (openapi-typescript 7.9.1, OpenAPI 3.0.3) | `shared/api/generated/openapi.ts` |
| Transport re-exports + operation helpers | `shared/api/generated/index.ts` |
| Runtime Zod envelopes + pilot samples | `shared/api/schemas.ts` |
| Shared contracts (Cursor/Numbered/Problem) | `shared/api/contracts.ts` |
| Shared mapper helpers (`INVALID_API_CONTRACT`) | `shared/api/mappers.ts` |
| Mapper pattern doc | `shared/api/MAPPER-PATTERN.md` |
| Catalog transport aliases | `features/catalog/transport.ts` |
| Catalog DTO→view mappers (example) | `features/catalog/mappers.ts` |
| Unit tests | `tests/unit/api-schemas.test.ts` |
| Architecture: generated types not in screens | `tests/unit/architecture-boundaries.test.ts` |
| npm scripts | `api:generate`, `api:check` (+ existing `api:lint`) |

### Codegen pin

- Package: `openapi-typescript@^7.9.1` (devDependency, lockfile updated)
- Command:  
  `openapi-typescript backend/api/openapi.yaml -o shared/api/generated/openapi.ts --root-types --export-type -c /dev/null`
- `--check` used by `npm run api:check` for dirty detection

### Runtime schemas (minimum set)

- `successEnvelopeSchema` / `apiEnvelopeSchema`
- `problemEnvelopeSchema` (`{ problem: ... }` wire shape)
- `cursorListMetaSchema` / `cursorListEnvelopeSchema`
- `numberedPageListMetaSchema` / `numberedPageListEnvelopeSchema`
- `moneyIdrSchema`, `rfc3339TimestampSchema`, `fieldViolationSchema`
- Pilot samples: `catalogProduct*`, `publicStorefront*`, `feePolicy*`, `status*`, `authLogin*`, `healthStatus`

### Mapper example (no screen change)

- `mapCatalogProductDto` / `mapPublicStorefrontDto` → `features/catalog/contracts.ts`
- Exhaustive product type + storefront token enums; fail closed via `invalidApiContract`
- Money via safe integer check; mock fixture parity unit-tested

## 3. Acceptance

| Criterion | Result |
| --- | --- |
| FE can import transport types without manual `any` for pilot ops | Yes — `SchemaCatalogProduct`, `OperationSuccessJson<"listFeaturedProducts">`, etc. |
| Runtime schema validates envelopes | Yes — unit tests success/fail |
| No UI design changes | Yes — no app/components/screens edits |
| `go test` contract still passes | Yes |
| npm unit tests for new schemas pass | Yes |

## 4. Test evidence

```bash
# Contract (backend INT-000 gate still green)
make -C backend test-contract
# ok  github.com/dasepmoch/fersaku-new/backend/test/contract

# Codegen check
npm run api:check
# openapi-typescript 7.9.1 — exit 0

# Typecheck
npm run typecheck
# exit 0

# Unit (schemas + full suite)
npm run test:run -- tests/unit/api-schemas.test.ts tests/unit/http-client.test.ts tests/unit/architecture-boundaries.test.ts
# 39 passed

npm run test:run
# Test Files  18 passed (18)
# Tests  112 passed (112)
```

### Schema test coverage (INT-010)

- valid success envelope
- malformed envelope / missing required
- ProblemEnvelope nested shape
- empty CursorList + NumberedPageList
- fractional + unsafe money reject
- unknown product enum reject
- mapper unknown enum → `INVALID_API_CONTRACT` (no secret leakage in problem JSON)
- mock product/storefront parity mapping

## 5. Explicit non-goals (deferred)

| Item | Owner |
| --- | --- |
| Wire every feature `api.ts` to pass `schema:` | INT-100 + domain tasks |
| Parse HTTP errors as nested `ProblemEnvelope` in http-client | INT-100 |
| Per-domain source registry | INT-025 |
| Screen/live adapter cutover | PUB/AUT/SEL domain tasks |

## 6. Risks / notes

- Generated file is large (~14k lines); committed so CI consumers need no network codegen unless `api:check` fails.
- `api:generate` uses `-c /dev/null` so Redocly multi-api config does not require `x-openapi-ts.output` for lint-only `redocly.yaml`.
- Existing feature APIs still cast view types without schema; INT-010 provides the infrastructure and one domain mapper example without changing product UI.

## 7. Checklist (INT-010 FE)

- [x] Generator compatible with OpenAPI 3.0.3 + TS; version pinned
- [x] Artefacts under `shared/api/generated/`; transport-only exports
- [x] Scripts `api:lint`, `api:generate`, `api:check`
- [x] Reusable envelope/problem/meta schemas in `shared/api/schemas.ts`
- [x] Mapper pattern + catalog example without React
- [x] Exhaustive enum mapping + money/timestamp rules
- [x] Unit tests for schema parse success/failure + mapper
- [x] Evidence + registry `[x]`
