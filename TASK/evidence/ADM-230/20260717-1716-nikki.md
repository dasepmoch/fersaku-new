# ADM-230 — Admin profile, photo, sessions, notifications, security

**Status:** `[x]` completed 
**Actor:** @nikki/opencode 
**When:** 2026-07-17 17:16 WIB 
**Depends on:** ADM-100 (`[x]`), ADM-110 (`[x]`), AUT-120 (`[x]`), BUY-140 (`[x]`); INT-175 photo active → no (DISABLED) 
**Branch:** main 

## 1. Objective delivered

Wire session-bound admin own profile GET/PATCH, notification preferences, trusted sessions list/revoke, and auth recovery regenerate on `/admin/profile` when `auth` domain is `api`. Reuse shared BUY-140 `NotificationCenter`/`ProfileMenu` with `surface=admin` (already on admin shell). Photo remains **DISABLED** (INT-175). No UI redesign; no second notification adapter.

## 2. Files

| Path | Role |
| --- | --- |
| `shared/query/query-keys.ts` | `admin.profile/sessions/notificationPreferences(subjectKey)` |
| `features/admin/profile/contracts.ts` | AdminProfile / AdminSession / patch + revoke inputs |
| `features/admin/profile/mappers.ts` | ProfileData → view; closed prefs; sessions; initials |
| `features/admin/profile/api.ts` | GET/PATCH me profile+prefs; auth sessions revoke* |
| `features/admin/profile/hooks.ts` | subject-bound queries + mutations; current revoke → logout |
| `features/admin/profile/mock.ts` | Demo profile/sessions (snapshot continuity) |
| `features/admin/profile/index.ts` | Public exports |
| `features/admin/screens/access/profile.tsx` | Wire existing chrome; no localStorage truth on API path |
| `features/admin/components/admin-shell.tsx` | Already `NotificationCenter`/`ProfileMenu` surface=admin |
| `tests/unit/adm-230-admin-profile.test.ts` | transport, mappers, shell disposition, notif isolation |
| `TASK/05-ADMIN-CONSOLE.md` | ADM-230 checklist `[x]` |
| `TASK/09-EXECUTION-STATUS.md` | `[x]` claim released |

## 3. Behavior

| Surface | Transport | Domain |
| --- | --- | --- |
| Get profile | `GET /v1/me/profile` + `GET /v1/me/notification-preferences` | `auth` |
| Patch profile | `PATCH /v1/me/profile` (`expectedVersion`, no email/avatar/jobTitle) | `auth` |
| Patch prefs | `PATCH /v1/me/notification-preferences` (closed codes only) | `auth` |
| Sessions list | `GET /v1/auth/sessions` | `auth` |
| Revoke one | `POST /v1/auth/sessions/{id}/revoke` | `auth` |
| Revoke others | `POST /v1/auth/sessions/revoke-others` | `auth` |
| Revoke all | `POST /v1/auth/sessions/revoke-all` → logout admin surface | `auth` |
| auth recovery | `POST /v1/auth/auth/recovery-codes/regenerate` via AUT-120 | `auth` |
| Inbox shell | shared BUY-140 `listNotifications("admin")` → `/v1/admin/notifications` | `adminRead` |
| Mock | in-memory fixtures; **no** `fersaku-admin-profile-settings` localStorage | mock |
| 409 conflict | rethrow; draft kept | — |

### Notification mapping (closed schema)

| UI toggle | BE event (EMAIL) |
| --- | --- |
| QRIS API KYC reviews | `KYC_UPDATE` |
| High-value withdrawal reviews | `WITHDRAWAL_UPDATE` |
| Provider and infrastructure incidents | `SECURITY_ALERT` |
| Daily operations digest | `MARKETING_NEWSLETTER` |

### Photo disposition (INT-175)

- **DISABLED / OUT-OF-SCOPE** — upload control remains `disabled` with INT-175 title.
- Initials only from display name; no store objects endpoints.

### Security

- Security codes (unused): component memory only after regenerate (never RQ keys/localStorage).
- Revoke current/all → `logoutSession({ surface: "admin", redirect: true })` clears private cache.
- Session list `deviceLabel` sanitized; no raw auth secrets in views.

### Removed on API path

- `fersaku-admin-profile-settings` localStorage read/write
- Hardcoded “Dinda” as API truth
- Client-only session filter as authority

## 4. Tests / verification

```text
npx vitest run tests/unit/adm-230-admin-profile.test.ts
# 21 passed
```

Coverage:

- Profile/prefs/session schemas + mappers
- Query-key subject isolation under `admin` private root
- GET/PATCH me profile; prefs closed codes; sessions list/revoke*
- Shell disposition: shared NotificationCenter/ProfileMenu surface=admin
- Screen: no localStorage, photo disabled, AUT-120 recovery hook
- Admin notification alias isolation (shared BUY-140)

## 5. UI freeze

- Existing profile panels, toggles, ControlDialog, auth section, session rows unchanged in structure.
- Photo button remains disabled.
- Job title field retained for mock/local draft; not sent on PATCH (no BE field).

## 6. Out of scope

- INT-175 personal photo/media
- Dual-confirm email change UI (no chrome)
- Password change form (not on this screen; AUT-120 adapters remain for other surfaces)
- Second notification adapter
- Admin staff directory (ADM-220) / impersonation (ADM-390)

## 7. Consumer notes

- Shell already correct; SEL-420-style acceptance covers admin alias isolation.
- Prefer `features/admin/profile` for own me/* only — do not fold into `data/access.ts` (other staff).
