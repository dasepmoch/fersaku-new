# ADM-220 — Staff users, invitations, roles, permissions, assignments

**Status:** `[x]` completed  
**Actor:** @nikki/opencode  
**When:** 2026-07-17  
**Depends on:** ADM-100, ADM-110, AUT-120 (all `[x]`)  
**Branch:** main

## 1. Objective delivered

- Wire staff/users/roles/permissions when `adminRead`/`adminWrite` is `api`
- Map BE `{items}` role/permission envelopes + UserLookup array to existing chrome
- Role create/update with `expectedVersion`; system roles read-only
- Staff invite create/list (token once on create only; never in list cache)
- Role assign/remove adapters; permission gates via ADM-110 codes
- Unit tests: list, invite, role assign, permission deny, mock path
- No UI redesign

## 2. Frontend modules

| Module | Role |
| --- | --- |
| `shared/api/schemas.ts` | Role/permission/user/invite/accept DTO + envelopes |
| `features/admin/data/contracts.ts` | `AdminUserLookup`, `AdminStaffMember`, `AdminStaffInvitation`, role version/code |
| `features/admin/data/mappers.ts` | Role/permission group/user/invite mappers + role color |
| `features/admin/data/access.ts` | Full read/write adapters + mutations |
| `features/admin/data/hooks.ts` | Staff directory, users, invites, write gates |
| `features/admin/data/index.ts` | Public exports |
| `shared/query/query-keys.ts` | `role`, `user`, `userRoles`, `staffInvitations` |
| `features/admin/screens/access/users.tsx` | Staff directory from hooks |
| `features/admin/screens/access/roles.tsx` | Metrics from roles/staff data |
| `features/admin/screens/access/role-builder.tsx` | `saveAdminRole` + expectedVersion |
| `features/admin/screens/actions.tsx` | Staff invite → API/mock adapter |

## 3. Backend contract (consumed)

| Endpoint | Permission | Use |
| --- | --- | --- |
| `GET /v1/admin/roles` | `roles.read` | List `{items}` RoleDTO |
| `GET /v1/admin/roles/{id}` | `roles.read` | Detail |
| `POST /v1/admin/roles` | `roles.write` | Create custom |
| `PATCH /v1/admin/roles/{id}` | `roles.write` | Update + permissions + expectedVersion |
| `POST /v1/admin/roles/{id}/archive` | `roles.write` | Archive |
| `GET /v1/admin/permissions` | `roles.read` | Flat registry → FE groups |
| `GET /v1/admin/users` | `users.read` | UserLookup[] |
| `GET /v1/admin/users/{id}` | `users.read` | Detail |
| `GET /v1/admin/users/{id}/roles` | `roles.read` | Assignments |
| `POST /v1/admin/users/{id}/roles` | `roles.assign` | Assign |
| `DELETE /v1/admin/users/{id}/roles/{roleId}` | `roles.assign` | Remove |
| `GET/POST /v1/admin/invitations/staff` | `roles.assign` | List / create (token once) |
| `POST .../revoke` | `roles.assign` | Revoke |
| `POST /v1/invitations/staff/accept` | public | Fragment token ceremony (adapter) |

Domain sources: `adminRead` (reads), `adminWrite` (mutations), `auth` for accept mock.

## 4. Behavior

| Path | Behavior |
| --- | --- |
| Mock | Fixtures + in-memory store; no transport |
| API + claims | Schema-mapped lists; mutations with idempotency + recent MFA |
| Permission deny | Hooks fail-closed without claim |
| Invite token | Create may return `deliveryToken`; stripped from list cache |
| Resend | Not implemented (no BE route); no double-create |
| System roles | Read-only in builder |
| 409 conflict | Form selections preserved; rethrow from save |

## 5. Tests

```text
npm run test:run -- tests/unit/adm-220-admin-access.test.ts tests/unit/query-keys.test.ts
# 20 passed (16 + 4 key assertions in query-keys file subset)
npm run test:run -- tests/unit/adm-220-admin-access.test.ts tests/unit/query-keys.test.ts tests/unit/adm-110-admin-permissions.test.ts
# 32 passed
```

Coverage:

- Role DTO mapping (`isSystem`, version, color, members=0)
- Permission registry → groups
- Invite list never carries token; create keeps delivery token separate
- Permission deny matrix
- Mock path no transport (list, save, assign, invite)
- API list/invite/assign/PATCH paths
- Query key isolation
- AUT-120 fragment helpers reuse

`tsc --noEmit` clean.

## 6. Acceptance

| Criterion | Evidence |
| --- | --- |
| Staff list/invite/role assignment adapters + hooks | `access.ts` + `hooks.ts` |
| Wire access screens when admin domain is api | users/roles/role-builder/actions |
| Unit tests | `tests/unit/adm-220-admin-access.test.ts` |
| Permission-gated ADM-110 | `roles.read/write/assign`, `users.read` |
| No UI redesign | existing panels/dialogs |

## 7. Out of scope / follow-ups

- Invitation **resend** route (matrix D) — control not double-create
- Public accept UI page (adapter + fragment helpers ready; wire on approved auth shell)
- Merchant invitations (matrix secondary)
- Role member aggregate from BE (still 0 / mock fixture counts)
- Role archive UI button (adapter ready; delete control remains disabled chrome)
- Seller impersonation (ADM-390)
