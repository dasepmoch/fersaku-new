# SEL-110 — Onboarding create/resume/complete

**Status:** `[x]` completed 
**Actor:** @nikki/opencode 
**When:** 2026-07-17 14:21 WIB 
**Depends on:** SEL-100, INT-190 (`[x]`)

## 1. Objective delivered

- Wire `/dashboard/onboarding` to real API when `sellerCatalog` domain is `api`
- Resume server progress on load/refresh (no reset to welcome)
- Debounced, cancellable slug availability; stale responses ignored
- Idempotent store create (`Idempotency-Key` + backend owner idempotency)
- Completion server-authoritative; refetch merchant bootstrap then redirect
- API mode: remove only documented fake “Storefront mock telah dibuat” claim; bind theme/publish line to truthful status
- Keep store-onboarding layout/stepper/form geometry exact
- No new maintenance banner (UXE-008 / UI-080): use existing form field error surface only

## 2. Frontend modules

| Module | Role |
| --- | --- |
| `shared/api/schemas.ts` | Zod: onboarding progress + slug availability envelopes |
| `features/seller/onboarding/contracts.ts` | Domain types + completion display |
| `features/seller/onboarding/slug.ts` | Normalize/validate aligned with BE `stores.NormalizeSlug` |
| `features/seller/onboarding/mappers.ts` | DTO→domain, UI step ↔ server state, completion copy |
| `features/seller/onboarding/api.ts` | GET/POST/PATCH/complete + slug check; mock fixtures when domain mock |
| `features/seller/onboarding/mock.ts` | Prototype mock create/patch/complete |
| `features/seller/onboarding/store-onboarding.tsx` | Wire steps; resume; slug race; idempotent create/complete |
| `app/dashboard/onboarding/page.tsx` | `PrivateSurfaceShell` + `CurrentStoreProvider` (bootstrap refresh) |

**API paths:** `GET /v1/onboarding`, `POST /v1/onboarding/store`, `PATCH /v1/onboarding/store`, `POST /v1/onboarding/complete`, `GET /v1/stores/slug-availability`.

**Create flow:** identity → POST create (idempotent key) → slug PATCH → visual PATCH → product skip → POST complete → `refreshBootstrap()` → `/dashboard`.

## 3. Backend (already present; exercised by FE)

Canonical merchant+OWNER+store transaction, slug normalize/availability, resume, complete idempotency in `backend/internal/application/onboarding_service.go` + HTTP handlers (BE-200 / INT-190 slice).

## 4. Tests

```text
npm run test:run -- tests/unit/sel-110-onboarding.test.ts
# 14 passed
```

Coverage:

- Slug normalize + reserved/short rejection
- Progress schema parse + resume step mapping (each server state)
- Form field restore from progress
- Completion display: mock keeps prototype copy; API removes mock claim; draft status not “Published”
- Slug race: slower response ignored when latest slug differs
- Duplicate create: same `Idempotency-Key` reused; mock second create returns same store
- Complete: `skipProduct: true`, maps COMPLETE; GET resume stays on finished step
- PATCH does not invent COMPLETE

## 5. Acceptance

| Criterion | Evidence |
| --- | --- |
| Layout/stepper/form exact | same classes/structure; only data binding + spinner/error |
| Resume on load/refresh | GET onboarding → `uiStepFromServerState` |
| Slug debounce/cancel/stale-safe | 400ms debounce + AbortController + seq/slug match |
| Idempotent create | `createIdempotencyIntentHolder` + BE owner re-entry |
| Validation/slug conflict → existing form | `fieldError` + unavailable slug line |
| Completion server-authoritative | POST complete then bootstrap refresh; no fake published in API |
| Product optional | skipProduct true; step 4 “Lewati” |
| No DEMO store authority in API path | store id from progress/bootstrap only |
| No new maintenance UI | form error text only (UXE-008) |

## 6. Out of scope

- SEL-200+ seller workspace screens
- Product creation during onboarding (optional cards remain non-navigating as prototype)
- Multi-store switch UI
- Emergency-registration maintenance banner (no characterized composition)

## 7. Consumer notes

```ts
import {
 getOnboardingProgress,
 createOnboardingStore,
 patchOnboardingStore,
 completeOnboarding,
 checkSlugAvailability,
 uiStepFromServerState,
 mapCompletionDisplay,
} from "@/features/seller/onboarding";
```
