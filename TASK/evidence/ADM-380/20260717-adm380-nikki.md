# ADM-380 — Campaign/announcement backend dependency

**Status:** `[x]` completed (launch disposition: live-disabled)  
**Actor:** @nikki/opencode  
**When:** 2026-07-17  
**Depends on:** ADM-110, INT-025, UI-080 (`[x]`)

## 1. Product decision (authoritative)

| Choice | Decision |
| --- | --- |
| Launch scope | Admin campaign/announcement commands **not required for launch** |
| Disposition | **`DISABLED` / `OUT-OF-SCOPE`** on `/admin/campaigns` |
| Route gate | `getAdminPageMeta(["campaigns"]).disposition === "decision_pending"` — nav hidden; boundary denies even `*` |
| Mode gate | `getDomainSource("adminWrite") === "mock"` → prototype seed + local publish/test/pause may remain; **`api` / `disabled` → disabled controls + no seed fixtures + truthful copy** |
| Backend campaign | **Do not implement** list/create/publish/pause/test/ack routes for launch |
| Success UI | Never “Campaign published and audited” / test delivered on API path without durable backend acceptance |
| Re-open criteria | Explicit product re-open + campaign aggregate + OpenAPI + separate `campaigns.read`/`campaigns.publish` + `NumberedPageList` authority + MFA/reason/idempotency/audit |

Rationale: matrix row is `D` (no route mounted); OpenAPI/backend have zero `/v1/admin/campaign*` paths; only permission registry entry is unused `campaigns.publish`. Fake local publish is forbidden on API/live.

## 2. Existing UI inventory (no redesign)

| Surface | Control | Launch state |
| --- | --- | --- |
| Route `/admin/campaigns` | Nav + permission boundary | Always denied via `decision_pending` (ADM-110 preserved) |
| Create / Publish / Send test / Pause / Resume / Refresh health | Existing buttons | API/disabled: `disabled` + `CAMPAIGN_COMMANDS_DISABLED_TITLE`; mock may stay interactive prototype |
| Campaign seed table + metrics | Local `campaignSeed` | Empty list + `—` metrics on API/disabled; mock keeps seed |
| TablePagination | Client pagination | Rendered only when fixtures allowed (mock); no pagination claim without BE |
| Preview CTAs (banner + email) | Looked like buttons | `role="presentation"` noninteractive spans; never submit |

Disabled title (API/disabled only):  
`Campaign commands are out of scope for launch (ADM-380 deferred — no backend route)`

Domain key: `adminWrite` (mutations); route disposition independent of domain source.

## 3. Architecture enforcement

| Path | Role |
| --- | --- |
| `features/admin/config/routes.ts` | `campaigns.disposition = decision_pending` |
| `features/admin/commerce/campaigns/disposition.ts` | Domain-source helpers + truthful copy |
| `features/admin/commerce/campaigns/announcements.tsx` | Command/fixture gates |
| `features/admin/commerce/campaigns/preview.tsx` | Noninteractive preview CTAs |
| `tests/unit/adm-380-campaigns-disposition.test.ts` | Disposition unit coverage |
| `tests/unit/architecture-boundaries.test.ts` | `ADM-380: campaign commands are authoritatively disabled without BE` |

Gate asserts:

1. Route meta remains `decision_pending`.
2. Commands/fixtures only when source is `mock`.
3. Announcements never import `apiRequest` / `/v1/admin/campaign*`.
4. Preview has no `onClick` submit affordance.

## 4. Contract / matrix updates

| Doc | Update |
| --- | --- |
| `TASK/05-ADMIN-CONSOLE.md` §ADM-380 | Launch DISABLED checked; IMPLEMENT deferred |
| `TASK/10-ROUTE-AND-CONTROL-DISPOSITION.md` | `/admin/campaigns` + control rows → DISABLED |
| `TASK/06-ENDPOINT-CONTRACT-MATRIX.md` | Campaigns row → launch DISABLED note |
| `TASK/09-EXECUTION-STATUS.md` | ADM-380 → `[x]` |

## 5. Explicit non-goals (this task)

- No campaign backend or OpenAPI operations
- No new design / chrome redesign
- No fake-success or silent publish in API mode
- No enabling nav via `campaigns.publish` alone

## 6. Tests

```text
npm run test:run -- tests/unit/adm-380-campaigns-disposition.test.ts tests/unit/architecture-boundaries.test.ts tests/unit/adm-110-admin-permissions.test.ts
```

## 7. Rollback

Revert evidence + disposition module + announcements/preview gates + architecture test + status/matrix rows if product re-opens campaigns as IMPLEMENT; then wire exact admin campaign ops + approved UI-080 pagination contract.
