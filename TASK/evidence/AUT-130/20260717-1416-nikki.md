# AUT-130 — Google/OAuth button disposition

**Status:** `[x]` completed (launch disposition) 
**Actor:** @nikki/opencode 
**When:** 2026-07-17 14:16 WIB 
**Depends on:** INT-025, INT-120, INT-130 (`[x]`)

## 1. Product decision (authoritative)

| Choice | Decision |
| --- | --- |
| Launch scope | Seller Google / OAuth **not required for launch** |
| Disposition | **`DISABLED` / `OUT-OF-SCOPE`** on seller `AuthShell` Google control in API/live |
| Mode gate | `getDomainSource("auth") === "mock"` → prototype affordance may remain enabled; **`api` / `disabled` → `disabled` + authoritative title** |
| Buyer Google | **Never add** Google control to `BuyerLogin` (snapshot has none) |
| Backend OAuth | **Do not implement** start/callback/PKCE/provider token exchange for launch |
| Re-open criteria | Explicit product re-open + full OAuth contract (state/nonce/PKCE, surface binding, account linking, session/CSRF) |

Rationale: no OpenAPI OAuth operations; active no-op button is forbidden in API mode; implementing full OAuth would expand auth attack surface without launch need.

## 2. Existing UI inventory (no redesign)

| Surface | Control | Launch state |
| --- | --- | --- |
| `components/auth-shell.tsx` | “Lanjutkan dengan Google” button | Same geometry/copy; API/disabled → `disabled` + title; mock may stay interactive prototype |
| `components/buyer-login.tsx` | No Google control | Unchanged — must not gain one |

Disabled title (API/disabled only): 
`Google sign-in is out of scope for launch (AUT-130 deferred)`

## 3. Architecture enforcement

| Path | Role |
| --- | --- |
| `tests/unit/architecture-boundaries.test.ts` | `AUT-130: seller Google OAuth is authoritatively disabled in API mode` |

Gate asserts:

1. `AuthShell` reads `getDomainSource("auth")` and sets `disabled={!googleOAuthEnabled}` with launch title.
2. Shell has no OAuth transport (`/v1/auth/oauth`, callback, Google accounts URL, PKCE) and no click/navigation handler.
3. `BuyerLogin` has no Google control copy/wiring.
4. `features/auth/*` does not mount OAuth endpoints for launch.

## 4. Contract / matrix updates

| Doc | Update |
| --- | --- |
| `TASK/03-PUBLIC-AUTH-CHECKOUT-BUYER.md` §AUT-130 | Decision checked; IMPLEMENT path deferred |
| `TASK/10-ROUTE-AND-CONTROL-DISPOSITION.md` | `/register`/`/login` Google → DISABLED/OUT-OF-SCOPE; inventory row |
| `TASK/09-EXECUTION-STATUS.md` | AUT-130 → `[x]` |

## 5. Explicit non-goals (this task)

- No OAuth backend or OpenAPI operations
- No provider UI kit / redesign
- No Google button on buyer login
- No fake-success or silent no-op in API mode

## 6. Tests

```text
npm run test:run -- tests/unit/architecture-boundaries.test.ts
```

## 7. Rollback

Revert evidence + architecture test + disposition/status if product re-opens OAuth as IMPLEMENT; then replace with full secure OAuth claim.
