# INT-190 — Deterministic integration seed + first vertical-slice gate

- Status: **[x] complete**
- Owner: @nikki/opencode
- UI impact: **none** (no redesign; API probes only)
- Scope: Prove public + authenticated FE↔BE live paths using **QLT-110 seed only** on QLT-215 API harness

## 1. Deliverables

| Item | Path |
| --- | --- |
| Vertical-slice Playwright API probes | `tests/e2e/api/int-190-vertical-slice.spec.ts` |
| Stable seed ID helper (read-only) | `tests/e2e/api/helpers/seed.ts` |
| Live response samples | `TASK/evidence/INT-190/responses/*` |
| Playwright run log | `TASK/evidence/INT-190/playwright-int190.log` |
| Seed owner (unchanged) | QLT-110 `backend/cmd/seed` + `TASK/evidence/QLT-110/seed-ids.json` |
| Stack orchestration (unchanged) | QLT-215 `scripts/e2e-api-stack.sh` |

## 2. How to reproduce

```bash
# Disposable stack: migrate + QLT-110 seed + API ready
npm run test:e2e:api:stack

# Backend-only vertical slice (this evidence run)
E2E_API_SKIP_WEBSERVER=1 E2E_API_HAS_NEXT=0 \
  PLAYWRIGHT_API_BASE_URL=http://127.0.0.1:18080 \
  API_INTERNAL_URL=http://127.0.0.1:18080 \
  APP_ENV=local \
  npx playwright test -c playwright.api.config.ts \
    tests/e2e/api/int-190-vertical-slice.spec.ts --reporter=line

# Full API project (includes QLT-215 harness + INT-190)
# npm run test:e2e:api
```

## 3. Checklist → evidence

| Checklist item | Result | Proof |
| --- | --- | --- |
| Use QLT-110 seed only (no second seed command) | **PASS** | `helpers/seed.ts` mirrors `seed-ids.json`; stack still `make seed` |
| Migration + seed idempotent on disposable DB | **PASS** | QLT-215 stack; seed IDs stable in DB (`01HQ0SEED…`) |
| Public: featured → storefront → product | **PASS** | Live 200 envelopes; IDs `…042` / slug `seed-published-product` / store `seed-store-a` |
| Auth: login → session → merchant/store → seller read → logout | **PASS** | Seller owner A; bootstrap store `…031`; products list; logout → 401 |
| Negative: 401, CSRF, foreign store, unknown store, bad credentials | **PASS** | `responses/negatives.json` + Playwright negatives |
| No mock commerce fallback on API path | **PASS** | Probes hit Go API only; no DEMO_STORE / mock fixtures |

## 4. Stable seed IDs exercised

| Resource | ID / value |
| --- | --- |
| seller_owner_a | `01HQ0SEED00000000000000003` / `seller.owner.a@seed.fersaku.test` |
| buyer_a | `01HQ0SEED00000000000000001` / `buyer.a@seed.fersaku.test` |
| merchant A | `01HQ0SEED00000000000000021` |
| store A | `01HQ0SEED00000000000000031` / `seed-store-a` |
| store B (foreign) | `01HQ0SEED00000000000000032` |
| published product | `01HQ0SEED00000000000000042` / `seed-published-product` / price `50000` |
| nonprod password | `TestSeed1!` (documented seed only) |

## 5. Live path results (2026-07-17)

### Public slice

```text
GET /v1/public/products/featured?limit=50 → 200
  includes id=01HQ0SEED…042 storeSlug=seed-store-a
GET /v1/public/stores/seed-store-a → 200
  products include seed-published-product
GET /v1/public/products/seed-published-product?store=seed-store-a → 200
  id=01HQ0SEED…042 price=50000
```

Samples: `responses/public-*.json`

### Authenticated seller slice

```text
POST /v1/auth/login {seller.owner.a, TestSeed1!, SELLER} → 200 + fersaku_session
GET /v1/auth/session → 200 userId=…003 surface=SELLER sessionStatus=AUTHENTICATED
GET /v1/seller/me/merchant → 200
  merchantId=…021 currentStoreId=…031 onboardingCompleted=true
GET /v1/stores/…031/products → 200 includes published seed product
POST /v1/auth/logout + X-CSRF-Token (from GET /session) → 200
GET /v1/auth/session → 401 AUTH_REQUIRED
```

Samples: `responses/seller-*.json`

### Buyer path

```text
POST /v1/auth/magic-link/request {buyer.a@…} → 200 generic message (anti-enumeration)
POST /v1/auth/login buyer password path → 200 when not rate-limited
  (rate-limit soft-skips session assert; magic-link request remains required green)
```

### Negatives

| Case | HTTP | problem.code |
| --- | --- | --- |
| No cookie → `/v1/seller/me/merchant` | 401 | `AUTH_REQUIRED` |
| Logout without CSRF | 403 | `AUTH_CSRF_INVALID` |
| Seller A → store B products | 404 | `RESOURCE_NOT_FOUND` |
| Unknown public store | 404 | `RESOURCE_NOT_FOUND` |
| Bad password | 401/403 | problem present; no session cookie |

## 6. Playwright evidence

```text
npx playwright test -c playwright.api.config.ts tests/e2e/api/int-190-vertical-slice.spec.ts
# → 9 passed, 1 skipped (Next rewrite when E2E_API_HAS_NEXT=0)
# EC:0
```

Log: `playwright-int190.log`

## 7. Acceptance

| Criterion | Status |
| --- | --- |
| Same seed IDs stable | **PASS** |
| Public + auth path hit live backend | **PASS** |
| No mock commerce fallback on API domain flags | **PASS** (probe path is pure API; FE domains already wired PUB-100/AUT-100/SEL-100) |
| Evidence files present | **PASS** |

## 8. Residual (not blocking INT-190 gate)

1. **Buyer magic-link full consume E2E** — compose API default `MAIL_MODE=capture` (in-memory) does not deliver to Mailpit; full fragment consume needs `MAIL_MODE=smtp` + Mailpit (or capture dump endpoint). Magic-link *request* contract proven; password session proven when not rate-limited.
2. **Next same-origin + homepage SSR product success** — rewrite proven by QLT-215 when Next up; homepage SSR server-client residual noted in QLT-215.
3. **Visual / a11y / full domain canaries** — QLT-220/230 cells + capability matrix `foundation-pilot` / `public-catalog` / `seller-auth` / `seller-identity`.
4. **Featured list pollution** — disposable DB may retain non-seed published products from older integration fixtures; seed product still present and asserted by stable ID.
5. **QLT-105 required CI job** — enable `cross-stack-api-e2e` to run this suite on every PR (QLT-105).

## 9. Files touched

```text
tests/e2e/api/int-190-vertical-slice.spec.ts
tests/e2e/api/helpers/seed.ts
tests/e2e/api/helpers/index.ts
TASK/02-FOUNDATION-TRANSPORT-AUTH.md   # INT-190 checklist [x]
TASK/09-EXECUTION-STATUS.md            # INT-190 [x]
TASK/evidence/INT-190/**
```

## 10. Unlock

Domain workstreams may enable API flags per domain after this gate. Full-domain canaries remain QLT-220 cells; money/secret mutations still require their domain tasks + INT-180/185 where live.
