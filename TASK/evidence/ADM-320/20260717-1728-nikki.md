# ADM-320 — Admin inventory redaction/reveal + fulfillment operations

**Status:** `[x]` completed  
**Actor:** @nikki/opencode  
**When:** 2026-07-17  
**Depends on:** ADM-110, SEL-240, SEL-250, INT-140 (all `[x]`)  
**Branch:** main

## 1. Objective delivered

- Redacted inventory list adapters + privileged per-item reveal (`requireRecentMfa`, reason, no body MFA claim)
- Fulfillment list/detail adapters + force-fulfill/revoke on fulfillment screen only
- Wire inventory + fulfillment screens when admin domain is api
- Permission-gated via ADM-110 codes (`inventory.read` / `inventory.reveal` / `fulfillment.read` / `fulfillment.force`)
- Unit tests: redaction, reveal MFA, mock path, no secrets in query keys
- Evidence + mark ADM-320 `[x]`

## 2. Frontend modules

| Module | Role |
| --- | --- |
| `shared/api/schemas.ts` | Fulfillment DTO/list/detail; delivery grant command envelope; admin reveal reuses inventory reveal schema |
| `features/admin/data/contracts.ts` | `AdminFulfillment`, command result, statuses |
| `features/admin/data/mappers.ts` | Inventory redaction assert; reveal map; fulfillment status/DTO; grant command map |
| `features/admin/data/inventory.ts` | Redacted GET + reveal POST with `requireRecentMfa` (no `mock-recent-mfa`) |
| `features/admin/data/fulfillments.ts` | List/detail adapters; mock seed only on mock path |
| `features/admin/data/fulfillment-commands.ts` | Typed force-fulfill + revoke; invalidate fulfillment/order/audit |
| `features/admin/data/hooks.ts` | `useAdminInventoryRevealEnabled`, `useAdminFulfillments`, `useAdminFulfillmentForceEnabled` |
| `features/admin/screens/inventory.tsx` | Reveal only; component-local TTL + visibility clear; invalidate/delete stay disabled |
| `features/admin/screens/fulfillment.tsx` | Hook-backed rows; retry/revoke via typed commands + ControlDialog |

## 3. Backend contract (consumed)

| Endpoint | Permission (BE) | Use |
| --- | --- | --- |
| `GET /v1/admin/inventory` | `inventory.read` | Redacted snapshot; no secrets |
| `POST /v1/admin/inventory/items/{itemId}/reveal` | `inventory.reveal` + recent MFA | Expected admin facade (gap noted if unmounted); FE ready |
| `GET /v1/admin/fulfillments` | `fulfillment.read` | Delivery grant list |
| `GET /v1/admin/fulfillments/{deliveryId}` | `fulfillment.read` | Detail |
| `POST /v1/admin/orders/{orderId}/delivery/force-fulfill` | `fulfillment.force` | Fulfillment retry control only |
| `POST /v1/admin/orders/{orderId}/delivery/revoke` | `fulfillment.force` | Fulfillment revoke control only |

Domain sources: `adminRead` (reads), `adminWrite` (reveal/force/revoke).

**BE note:** admin reveal route remains matrix status `G` (store-scoped reveal exists; admin facade still expected). FE uses the documented admin path and never seller store-scoped reveal.

## 4. Behavior

| Path | Behavior |
| --- | --- |
| Mock | Fixtures only; no transport |
| API + claims | Schema-mapped list; reveal/force/revoke typed POST |
| API without claim | Query/action disabled fail-closed |
| List inventory | Field-name `schemaPreview` only; assert rejects secret bags |
| Reveal | Component-local state; TTL from `expiresAt`; clear on hide/unmount/visibility; never React Query |
| Fulfillment retry | Maps to force-fulfill on **order id** from row; MFA on API path |
| Invalidate/delete inventory | Remain disabled |
| Order detail | Still no force/revoke (ADM-300 disposition) |

## 5. Tests

```text
npm run test:run -- tests/unit/adm-320-admin-inventory-fulfillment.test.ts
# 14 passed
npx tsc --noEmit
# clean
```

Coverage:

- Redacted list never has values/secrets
- assertNoSecrets throws on values bag
- Reveal DTO → component-local secret + TTL
- Permission deny matrix for inventory/fulfillment codes
- Mock path never hits transport
- API inventory GET path
- Reveal: `requireRecentMfa`, reason body only, no `mfaVerified`, no secrets in query keys
- Short reason rejected without transport
- Fulfillment status mapping + list GET
- Force-fulfill / revoke typed routes + MFA
- Mock force/revoke no transport

## 6. Acceptance

| Criterion | Evidence |
| --- | --- |
| Redacted list adapters | `inventory.ts` + `assertNoSecretsInAdminInventory` |
| Privileged reveal | `revealInventoryItem` + inventory screen |
| Fulfillment hooks | `fulfillments.ts` + `useAdminFulfillments` |
| Force/revoke ownership | fulfillment screen only |
| No secrets in keys | unit test on `queryKeys.admin.inventory/fulfillment` |
| No UI redesign | existing tables/dialogs/metrics preserved |

## 7. Out of scope / follow-ups

- BE mount of `POST /v1/admin/inventory/items/{itemId}/reveal` if still missing
- Authoritative NumberedPageList page meta when BE exposes it
- Invalidate/delete inventory mutations (remain disabled)
- Seller delivery retry (store-scoped) vs admin force-fulfill distinction already by route
