# INT-000 — Fix and freeze OpenAPI as wire contract (G1)

- Status: **complete**
- Owner: @nikki/opencode
- Base: `76e0456` (pre-task)
- UI impact: **none** (no FE screens/components)
- Scope: OpenAPI 3.0.3 validity, security schemes, envelope schemas, router drift gate, lint tooling

## 1. Environment

```text
date: 2026-07-17 12:00 WIB (Asia/Jakarta)
go: go1.24.5 ($HOME/.local/go/bin/go)
node: used via npx @redocly/cli@1.34.3
os: Linux
```

## 2. Spec quality (acceptance)

### Parse / lint (exit 0)

```bash
# YAML parse
python3 -c "import yaml; yaml.safe_load(open('backend/api/openapi.yaml')); print('OK', '3.0.3')"

# Redocly OpenAPI 3.0.3 lint (structural errors must be zero)
npm run api:lint
# equivalent: npx --yes @redocly/cli@1.34.3 lint backend/api/openapi.yaml --config=redocly.yaml
# → exit 0 — "Woohoo! Your API description is valid."
# (warnings only: operation-4xx-response, no-unused-components, etc.)

# Makefile alias from backend/
make -C backend openapi-lint
```

Result: **exit 0**. Declared `openapi: 3.0.3` (no silent upgrade to 3.1).

### Security schemes (standardized)

| Scheme | Type | Purpose |
| --- | --- | --- |
| `sessionCookie` | apiKey cookie `fersaku_session` | Browser/SSR session (replaces all `cookieAuth` aliases) |
| `merchantApiKey` | http bearer `fsk_*` | Gateway `/v1/gateway/*` + legacy `/v1/qris/*` |
| `xenditCallbackToken` | apiKey header `X-Callback-Token` | Inbound Xendit webhooks |
| `capabilityToken` | apiKey header `X-Capability-Token` | Documented capability/public-adjacent token |

Root `security: []` default; operations set explicit schemes. Public ops use `security: []`.

### Envelope / list / money / time

Schemas present and tested:

- `SuccessEnvelope`, `ProblemEnvelope`, `FieldViolation`
- `CursorListMeta`, `NumberedPageListMeta` (+ envelope wrappers)
- `MoneyIdr` (int64 whole IDR), `Rfc3339Timestamp`
- Reusable headers/params: `X-Request-ID`, `X-CSRF-Token`, `Idempotency-Key`, `X-Audit-Reason`, `session authentication`, `If-Match`, `Retry-After`, `X-Callback-Token`

### Internal / non-production ops

Marked `x-internal: true` + tag `internal` / disposition:

- `POST /v1/_scaffold/echo` (local/test env-gated in router)
- `POST /v1/_test/paid-orders`
- `POST /v1/checkout/simulate-payment`
- Gateway/QRIS product/catalog/upload **explicit rejections**
- Fee publish/mutate **explicit rejections**
- KYC browser presign **explicit rejections**

### Gateway / QRIS / Xendit coverage

Documented and inventory-covered:

- `/v1/gateway/payment-intents*` + events
- Legacy `/v1/qris/*` (same use case; deprecation surface)
- `POST /v1/webhooks/xendit`, `/sandbox`, `/live`, `/disbursement`

## 3. Router inventory + drift gate

### Inventory

- Source of truth for mounts: `backend/internal/adapters/http/router.go`
- Allowlist artefact: `backend/test/contract/router_inventory.txt` (~299 METHOD path lines)
- Notes:
 - Notification mounts via `mount()` expanded to four bases
 - Chi `Get("/")` listed with trailing-slash and non-slash variants (canon compare strips trailing slash)
 - `HandleFunc` reject stubs listed as POST (matches OpenAPI)

### Contract tests

```bash
cd backend && $HOME/.local/go/bin/go test -count=1 ./test/contract/
# ok github.com/dasepmoch/fersaku-new/backend/test/contract
```

Also: `make -C backend test-contract`

Tests:

| Test | Purpose |
| --- | --- |
| `TestOpenAPIVersion303` | Freeze 3.0.3 |
| `TestOperationIDsUniqueAndPresent` | Unique operationId |
| `TestEnvelopeAndSecuritySchemasPresent` | Golden schemas/schemes/headers |
| `TestNoCookieAuthAlias` | No `cookieAuth` drift |
| `TestRouterOpenAPIDrift` | Router ↔ OpenAPI allowlist both directions (x-internal rejection/test-only excluded from reverse) |
| `TestGoldenEnvelopeShapes` | required fields on envelopes/list meta |
| `TestOpenAPIDeclaresGatewayAndXenditSurface` | Critical surface presence |
| `TestInventoryCoversAbsoluteRouterMounts` | Inventory includes absolute `/v1|health|metrics` mounts from router.go |

## 4. Lint tooling

| Command | Location |
| --- | --- |
| `npm run api:lint` | root `package.json` |
| `npm run api:bundle` | root `package.json` (optional bundle to `backend/api/openapi.bundle.yaml`) |
| `make -C backend openapi-lint` | `backend/Makefile` |
| `make -C backend openapi-bundle` | `backend/Makefile` |
| `make -C backend test-contract` | Go drift gate |

Config: `redocly.yaml` (repo root) — structural rules error; style noise as warn/off.

## 5. Files touched

```text
backend/api/openapi.yaml # parse fixes, schemes, envelopes, missing mounts, x-internal
backend/test/contract/openapi_contract_test.go
backend/test/contract/router_inventory.txt
backend/go.mod # gopkg.in/yaml.v3
backend/Makefile # openapi-lint, openapi-bundle, test-contract
package.json # api:lint, api:bundle
redocly.yaml
TASK/09-EXECUTION-STATUS.md # INT-000 [x]
TASK/evidence/INT-000/20260717-1200-nikki.md
```

## 6. Known remaining gaps (documented, not fake-implemented)

Aligned with `TASK/06-ENDPOINT-CONTRACT-MATRIX.md` dispositions — **not** claimed as available product completeness:

| Gap | Disposition | Owner |
| --- | --- | --- |
| Seller order list/customer/review CRUD surfaces still incomplete vs old claims | G/U in matrix | SEL-* domain tasks |
| Admin inventory reveal/campaign product surfaces | G/U | ADM-* |
| Buyer download/protected-link exchange | G | CHK-140 / INT-000 freeze later if product chooses path |
| Contact form public submit | D | PUB-200 |
| Personal profile media user-scoped upload | D | INT-175 |
| Recent authentication step-up exact path | D | INT-140 |
| Admin auth login | D | ADM-100/INT-140 |
| Many routes status **M/U** (auth/CSRF/pagination/DTO) | wiring tasks INT-010+ | domain lanes |
| OpenAPI op detail depth uneven (some 200-only stubs) | acceptable for G1 freeze; domain INT-* deepen | INT-010 codegen consumers |
| Redocly warnings (4xx coverage, unused components) | non-blocking | QLT-105/INT-010 |

**INT-000 does not implement INT-010+ domain wiring or FE UI.**

## 7. Acceptance checklist

- [x] Spec valid OpenAPI 3.0.3, no parse errors
- [x] Does not claim unmounted production routes as available (drift test green; extras only x-internal rejection)
- [x] Contract/router drift tests green
- [x] FE can later generate transport types without invalid YAML blocking INT-010
- [x] Evidence path real
