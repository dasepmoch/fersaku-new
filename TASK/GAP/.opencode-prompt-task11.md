# Task 11 — P1: Verifikasi backup/PITR dan object/data restore end-to-end

## READ THESE FILES FIRST:
1. `TASK/GAP/11-P1-DR-BACKUP-E2E.md` — the full task spec
2. `TASK/GAP/00-AUDIT-REPORT.md` — audit context
3. `TASK/PROD/11-AGENT-RUNBOOK.md` — agent execution rules
4. `backend/docs/runbooks/backup-restore-integrity.md` — existing runbook
5. `TASK/PROD/evidence/KEY-11*` — existing evidence (local logical restore)
6. `backend/internal/adapters/postgres/` — postgres adapters
7. `backend/internal/adapters/storage/` or `backend/internal/adapters/r2/` — object storage adapter

## STRICT CONSTRAINTS:
- DO NOT change any UI. Use existing components only.
- All new/changed code MUST be: scalable, modular, clean, best practice.
- Follow existing patterns in the codebase. Do NOT revert pre-existing changes.
- Jangan commit credentials, token, cookie, payment body, atau URL secret.
- No live production data — use sanitized test data only.
- Secrets supplied externally, not in dump.

## ENVIRONMENT:
- Go 1.25.x available on host at ~/.local/go/bin/go
- Docker available with sudo
- Postgres available via Docker
- No managed HA/PITR — implement local restore drill + scripts
- Mark anything requiring managed HA as BLOCKED

## TASK DETAILS (from 11-P1-DR-BACKUP-E2E.md):

### Key findings to fix:
1. Runbook + KEY-11 evidence prove local logical restore, but managed HA/PITR provisioning and managed restore drill still owner-pending
2. Database stores object refs, grants, ledger, outbox, KYC metadata; R2/object bytes at provider durability boundary must be restored together

### Implementation steps:
1. Owner provision managed HA Postgres, continuous WAL/PITR, encryption, retention, monitoring, access separation; record RPO/RTO decision. (BLOCKED — owner)
2. Restore snapshot/PITR to new instance without touching production. Pair DB restore with object inventory/checksum/retention validation for public/private buckets.
3. Run migrations/version check, integrity hash/ledger/outbox checks, scanner/object access checks, synthetic checkout/read/download (non-money), admin read-only smoke.
4. Measure restore start/end, data loss point, queue replay, webhook dedupe, application cutover/repoint procedure. Verify secrets supplied externally.
5. Schedule quarterly drill and incident runbook with owner/on-call, escalation, communication, rollback to original instance.

### Acceptance criteria:
- Managed restore drill meets approved RPO/RTO and has sanitized evidence.
- Ledger/audit/integrity chain, outbox, object refs and actual bytes reconcile; missing object fails drill.
- App can boot against restored clone, readiness passes, no duplicate money side effect when outbox replayed.
- Next drill date, owner, alert, evidence location recorded.

## AFTER COMPLETING, print a summary:
- What was implemented vs blocked
- Files changed/created
- Test commands run and results
- Any issues found
