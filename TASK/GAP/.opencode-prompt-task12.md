# Task 12 — P1: Reconcile readiness board dan lengkapi staging/canary evidence

## READ THESE FILES FIRST:
1. `TASK/GAP/12-P1-READINESS-EVIDENCE.md` — the full task spec
2. `TASK/GAP/00-AUDIT-REPORT.md` — audit context
3. `TASK/PROD/11-AGENT-RUNBOOK.md` — agent execution rules
4. `TASK/PROD/09-EXECUTION-STATUS.md` — execution status board
5. `TASK/PROD/13-KEYSWAP-STATUS.md` — keyswap status (pre-live NOT READY)
6. `backend/docs/security/residual-risks.md` — residual risks doc
7. All `TASK/GAP/evidence/` files from tasks 01-11
8. `TASK/PROD/evidence/KEY-51*` — launch subset evidence

## STRICT CONSTRAINTS:
- DO NOT change any UI. Use existing components only.
- All new/changed code MUST be: scalable, modular, clean, best practice.
- Follow existing patterns in the codebase. Do NOT revert pre-existing changes.
- Jangan menutup gap hanya dengan mengedit status. "Done" means evidence exists.
- Full deferred feature implementation tetap out of scope.
- Domain yang akan diaktifkan harus punya acceptance evidence.

## ENVIRONMENT:
- Go 1.25.x available on host at ~/.local/go/bin/go
- Node.js v24.x available via nvm
- Docker available with sudo

## TASK DETAILS (from 12-P1-READINESS-EVIDENCE.md):

### Key findings to fix:
1. `TASK/PROD/13-KEYSWAP-STATUS.md` — pre-live NOT READY: staging parity, headed E2E, live canary, owner operations pending
2. `TASK/PROD/09-EXECUTION-STATUS.md` and `residual-risks.md` have "done/pass" claims contradicting test failures, scanner absence, stale image/callback 404 evidence, managed infra pending
3. `TASK/PROD/evidence/KEY-51*` selects launch subset and explicitly defers full catalog/storefront/admin matrix

### Implementation steps:
1. Create one release matrix keyed by commit/image digest with columns: owner, severity, prerequisite, command, result, timestamp UTC, expiry, evidence link.
2. Re-run all P0 gates after fixes on clean checkout and disposable stack. Mark old evidence stale; don't reuse host/demo output for production claim.
3. Staging parity: same artifact/config schema/route surface as production, provider sandbox mode explicit, scanner real/stub equivalent, LB trusted proxy, Redis, storage, mail, worker topology.
4. Execute headed critical journeys for enabled domains: register/verify/login/MFA, seller onboarding/KYC, product upload/scan, storefront publish, QRIS pending/paid/expired/unknown, callback replay, delivery, withdrawal lifecycle, admin audit/impersonation guard.
5. Run canary with safe cohort/amount and rollback rehearsal. Record SLO/error budget, provider dashboard callback receipt, queue age, ledger reconciliation, no-money-anomaly signoff.
6. Reconcile docs so "done" means evidence exists; keep human owner tasks clearly labelled OWNER/PENDING and block go-live on P0/P1 critical rows.

### Acceptance criteria:
- One release candidate has a signed readiness matrix with no unresolved P0 and explicit approved P1 waivers (if any).
- Staging artifact digest equals candidate promoted to canary; all enabled-domain E2E pass without skip-only claims.
- Canary and rollback evidence is recent, sanitized, and points to actual runtime routes/images.
- Residual-risk document includes scanner, provider contract, proxy/rate-limit, observability, capacity, legal, supply-chain, and DR status.

## AFTER COMPLETING, print a summary:
- What was implemented vs blocked
- Files changed/created
- Test commands run and results
- Any issues found
