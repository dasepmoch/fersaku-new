# Task 08 — P1: Pastikan mode live tidak menampilkan fixture atau affordance palsu

## READ THESE FILES FIRST:
1. `TASK/GAP/08-P1-LIVE-DATA-TRUTH.md` — the full task spec
2. `TASK/GAP/00-AUDIT-REPORT.md` — audit context
3. `TASK/PROD/11-AGENT-RUNBOOK.md` — agent execution rules
4. `frontend/features/admin/screens/access/users.tsx` — Users screen (lines ~25-30)
5. `frontend/features/admin/data/access.ts` — API hooks for admin users
6. `frontend/shared/` — domain source / mock mode helpers
7. Any files containing `demoSellerUsers`, `mock-admin-session`, hardcoded metrics, fake IDs

## STRICT CONSTRAINTS:
- DO NOT change any UI. Use existing components only.
- All new/changed code MUST be: scalable, modular, clean, best practice.
- Follow existing patterns in the codebase. Do NOT revert pre-existing changes.
- Jangan implement fitur deferred. Hanya pastikan affordance-nya disabled/hidden/jujur.
- Static guard yang scoped agar false positive tidak mengulang masalah INT-170.

## ENVIRONMENT:
- Node.js v24.x available via nvm
- Frontend uses Next.js, Playwright, vitest
- No live API needed — test with mock and API mode assertions

## TASK DETAILS (from 08-P1-LIVE-DATA-TRUTH.md):

### Key findings to fix:
1. `users.tsx:25-30` — identical branch `isMock ? demoSellerUsers() : demoSellerUsers()`. API mode shows seller demo data, email, ID, and "Open as user" button
2. Same screen uses `useAdminStaffDirectory` for admin, but doesn't use API `listAdminUsers`/`useAdminUsers` available in `features/admin/data/access.ts`
3. Many surfaces have deferred/disabled affordance that must remain honest; static search shows "mock", "placeholder", "out of scope" on public/admin pages

### Implementation steps:
1. Trace every privileged/public screen to domain source snapshot. API/live must only use server data or show explicit empty/degraded state.
2. Connect Users screen to users lookup endpoint already available; create loading, empty, permission denied, stale, API error states. Impersonation only via server-issued short-lived session + audit evidence.
3. Audit all `demo*`, `mock*`, hardcoded metrics, fake IDs, "mock-admin-session", Date.now-generated mock values on live mode. Add scoped static guard.
4. Ensure deferred features don't have buttons that look active, tooltip that only explains after hover, or copy claiming real data. Don't implement deferred features.
5. Add Playwright live-source smoke for admin users, dashboard, payments, KYC, storefront, buyer library, contact/deferred pages.

### Acceptance criteria:
- No fixture row/metric/ID/email in rendered live/API output for all declared api/disabled domains.
- Users screen shows only server lookup and valid impersonation guard/audit; no fake target.
- Domain disabled produces clear state, not network request or fake success.
- Static and E2E checks can distinguish allowed mock fixtures on mock-only tests from production bundles.

## AFTER COMPLETING, print a summary:
- What was implemented vs blocked
- Files changed/created
- Test commands run and results
- Any issues found
