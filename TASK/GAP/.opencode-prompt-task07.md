# Task 07 — P1: Wire observability nyata dan alert yang dapat ditindaklanjuti

## READ THESE FILES FIRST:
1. `TASK/GAP/07-P1-OBSERVABILITY.md` — the full task spec
2. `TASK/GAP/00-AUDIT-REPORT.md` — audit context
3. `TASK/PROD/11-AGENT-RUNBOOK.md` — agent execution rules
4. `backend/internal/config/config.go` — OTEL config (lines ~123-125, ~177)
5. `frontend/shared/observability/reporter.ts` — frontend reporter (lines ~45-65)
6. `backend/internal/adapters/http/middleware/` — existing middleware
7. `backend/internal/platform/metrics/` — metrics package

## STRICT CONSTRAINTS:
- DO NOT change any UI. Use existing components only.
- All new/changed code MUST be: scalable, modular, clean, best practice.
- Follow existing patterns in the codebase. Do NOT revert pre-existing changes.
- Jangan commit credentials, token, cookie, payment body, atau URL secret.
- No secret/PII leakage in sampled logs. No high-cardinality raw IDs, emails, headers, signatures, account numbers, KYC data.
- Telemetry shutdown flush and exporter outage must not block money mutation indefinitely.

## ENVIRONMENT:
- Go 1.25.x available on host at ~/.local/go/bin/go
- Node.js v24.x available via nvm
- No live OTEL collector — implement wiring + stub/sink for testing
- Mark anything requiring live OTEL collector/Grafana as BLOCKED

## TASK DETAILS (from 07-P1-OBSERVABILITY.md):

### Key findings to fix:
1. `config.go:123-125,177` — reads `OTEL_EXPORTER_OTLP_ENDPOINT` but no SDK/exporter/span wiring; trace middleware only creates/echo trace context
2. `frontend/shared/observability/reporter.ts:45-65` — default reporter is no-op, no production `setObservabilityReporter`
3. Error boundaries call `reportError`, so errors can disappear without sink
4. Metrics endpoint only mentioned in docs as needing restriction, not deployment-enforced policy

### Implementation steps:
1. Pick backend telemetry and wire OTEL HTTP/DB/provider/outbox/job spans with sampling, timeout, batch, queue, shutdown flush.
2. Propagate request ID/trace ID from frontend → API → provider/job without raw payment/KYC payload. Standardize service, env, release, route template, status, error class.
3. Install frontend reporter at bootstrap/error boundary with real sink; no-op only local/test or explicit disabled mode visible in diagnostics.
4. Add RED metrics and SLO alerts: 5xx, latency, provider auth/timeout, callback backlog/replay, upload scanner quarantine, queue lag, DB pool saturation, Redis failures, delivery DLQ, payout mismatch.
5. Protect `/metrics` with network/auth policy; verify no high-cardinality raw IDs, emails, headers, signatures, account numbers, KYC data.
6. Add synthetic dashboard/runbook links and test alert routing in staging without paging real users.

### Acceptance criteria:
- Trace appears across at least one checkout, callback, upload scan, and worker job in staging with shared correlation ID.
- Frontend error boundary produces redacted event in configured sink; reporter absence fails readiness or is explicit non-prod mode.
- Alerts have owner, threshold, window, severity, runbook, recovery test. No secret/PII leakage.
- Telemetry shutdown flush and exporter outage do not block money mutation indefinitely.

## AFTER COMPLETING, print a summary:
- What was implemented vs blocked
- Files changed/created
- Test commands run and results
- Any issues found
