# GAP-01 P0 Duitku contract — evidence

- Date: 2026-07-20
- Agent: opencode
- Scope: HMAC-SHA256 signatures, merchantOrderId vs reference, env fail-closed, callback SUCCESS ack, Xendit v1 fallback guard

## Contract freeze

- Doc: https://docs.duitku.com/api/id/
- Verified: 2026-07-20
- Production base: `https://passport.duitku.com`
- Sandbox base: `https://sandbox.duitku.com`
- Signatures: HMAC-SHA256 lowercase hex (MD5 obsolete on live path)
- Callback ack body: `SUCCESS` with HTTP 200
- Status lookup key: `merchantOrderId` (= `payment_intents.external_id`)

## Commands run

```text
go test ./internal/adapters/duitku/... ./internal/adapters/xendit/... ./internal/application/... ./internal/adapters/http/handlers/... ./internal/config/... ./internal/app/... -count=1
```

## Results

pass (all packages above)

## Secrets check

- [x] no keys in this file
- [x] no live credentials used (httptest + fixtures only)

## Migration

- `000034_duitku_merchant_order_repair` — audit/report table only; no blind ID rewrite

## Blocked

- Live provider credentialed E2E against passport/sandbox: BLOCKED (no live payment credentials in environment)
