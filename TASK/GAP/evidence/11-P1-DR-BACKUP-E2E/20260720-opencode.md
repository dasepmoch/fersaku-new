# 11-P1-DR-BACKUP-E2E — evidence

- Date: 2026-07-20 (drill stamps 2026-07-19 UTC host clock)
- Agent: opencode
- Scope: Local E2E backup/restore + object inventory pairing; managed HA/PITR **BLOCKED owner**

## Implemented vs blocked

| Item | Status |
| ---- | ------ |
| E2E local logical dump → isolated clone | **Done** (`scripts/dr_restore_e2e.sh`) |
| Migration head parity | **Done** (origin=clone=33) |
| Ledger / outbox / audit fidelity on clone | **Done** |
| Object inventory + checksum/size; missing object fails | **Done** (positive + negative path) |
| Cutover/repoint procedure + secrets external | **Documented** |
| RPO/RTO decision + quarterly schedule + owner/on-call | **Recorded** |
| Live `/health/ready` on origin stack | **Pass** (200) |
| Managed multi-AZ PG + continuous WAL/PITR | **BLOCKED** — owner |
| Managed restore drill meeting RPO ≤5m / RTO ≤60m | **BLOCKED** — owner |
| App boot with `DATABASE_URL` pointed at managed clone | **BLOCKED** — no managed instance |

## Files changed/created

| Path | Role |
| ---- | ---- |
| `backend/scripts/dr_restore_e2e.sh` | **New** — full E2E DR drill |
| `backend/docs/runbooks/backup-restore-integrity.md` | Expanded E2E + quarterly + residual |
| `TASK/PROD/ops/02-postgres-pitr-runbook.md` | Link E2E script; mark managed BLOCKED |
| `TASK/PROD/ops/04-r2-object-storage-runbook.md` | DR object pairing section |
| `TASK/PROD/ops/README.md` | GAP-11 pointer |
| `TASK/GAP/evidence/11-P1-DR-BACKUP-E2E/*` | This evidence + raw report copy |
| `TASK/PROD/evidence/KEY-11/20260720-gap11-opencode.md` | KEY-11 cross-link |

## Commands run and results

```bash
# Positive path
EVIDENCE_DIR=TASK/GAP/evidence/11-P1-DR-BACKUP-E2E \
  ./backend/scripts/dr_restore_e2e.sh
# → PASS: all selected DR restore E2E checks OK
# local_rto_seconds=4; migration head 33; parity money tables OK
# object inventory 1/1; buckets private+public OK; ready=200

# Negative path (missing object must fail)
./backend/scripts/dr_restore_e2e.sh --fail-missing-object
# → PASS: negative path confirmed — missing object fails drill
```

### Positive drill snapshot (sanitized)

| Metric | Value |
| ------ | ----- |
| Stamp | `20260719T200803Z` |
| Dump size | ~535 KiB |
| Migration head | 33 = 33 |
| payment_intents / orders / withdrawals | 20/20 · 27/27 · 8/8 |
| outbox / ledger / audit | 52/52 · 48/48 · 13/13 |
| Outbox dedupe_key duplicates | 0 |
| Provider event canonical key dups | 0 |
| Object inventory | 1 ok / 0 missing |
| Local RTO | 4 s |
| Next drill date | **2026-10-17** |
| Owner | platform-lead |
| Alert | `fersaku_audit_chain_status_total{result="broken"}` |
| Report | `dr-e2e-20260719T200803Z.txt` (same dir) |

### Notes found during drill

- Origin `audit_events` has **1 pre-existing sequence gap** (seq 3→5); restore preserves it (fidelity pass). Not introduced by restore.
- `payment_provider_events` unique key is `(provider, account_scope, payment_mode, provider_event_id)` — same `provider_event_id` may appear across modes (demo data).

## Acceptance mapping

| Criterion | Met? |
| --------- | ---- |
| Managed restore drill meets RPO/RTO + sanitized evidence | **Partial** — local evidence yes; managed **BLOCKED** |
| Ledger/audit/outbox + object refs/bytes reconcile; missing object fails | **Yes** (local) |
| App boot against restored clone, ready, no duplicate money on outbox replay | **Partial** — clone SQL + essential tables + live ready on origin; outbox dedupe proven; full API cutover to clone not run (would dual-bind compose) |
| Next drill date, owner, alert, evidence location recorded | **Yes** |

## Secrets check

- [x] no keys / DSN passwords in this file
- [x] no dump binary committed
- [x] probe object used sanitized non-secret body only

## Rollback

- Drop clone: `DROP DATABASE fersaku_restore_drill;`
- Remove script/docs if needed; no production cutover performed
