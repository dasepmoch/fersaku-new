# 06-P1-CAPACITY-MIGRATION — evidence

- Date: 2026-07-20
- Agent: opencode
- Scope: Connection budget single source + role pool defaults + production-safe migrate/bootstrap

## Implemented

| Item | Status |
| ---- | ------ |
| Capacity worksheet (`config.CapacityWorksheet`) | Done |
| Role pool defaults API 20 / worker 10 | Done |
| Fail closed staging/production over budget | Done |
| Env knobs PG_* pool + statement timeout | Done |
| `app.go` uses cfg.Pool (not Default 20 for all) | Done |
| migrate.sh destructive guard + audit + no local URL on prod | Done |
| migrate-job.sh backup checkpoint + EXPECTED_HEAD | Done |
| launch_bootstrap production no seed / no local URL | Done |
| Docs topology/pool-tuning/baseline/.env.example | Done |
| Unit tests budget + role + deploy contract | Done |
| Live concurrent migrate / pool exhaustion under load | **Not run** (no prod DB; unit + script refuse paths only) |
| Managed PITR checkpoint automation | **BLOCKED** (operator sets `MIGRATE_BACKUP_CHECKPOINT`) |

## Commands run

```bash
go test ./internal/config/ ./internal/adapters/postgres/ -count=1
# ok

# migrate guards
APP_ENV=production ./scripts/migrate.sh up                    # rc=2 no DATABASE_URL
APP_ENV=production DATABASE_URL=...@db.example... down        # rc=2 refused
APP_ENV=production ... force 1                                # rc=2 refused
CI=true + break-glass tokens drop                             # rc=2 still refused
APP_ENV=production DATABASE_URL=...fersaku_local... version   # rc=2 local URL refused
APP_ENV=staging ... down                                      # rc=2 refused
APP_ENV=production launch_bootstrap (no URL)                  # rc=2
APP_ENV=production migrate-job.sh down                        # rc=2
```

## Capacity worksheet (defaults)

```
api=2×20 worker=2×10 app_total=60 usable=80/100 headroom_band=20 migrate=4 admin=4
```

## Secrets check

- [x] no keys in this file
- [x] no keys in commit paths
