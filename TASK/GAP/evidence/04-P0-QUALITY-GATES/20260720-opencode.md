# 04-P0-QUALITY-GATES — evidence

- Date: 2026-07-20
- Agent: opencode
- Commit (at run): d109355
- Scope: Restore all quality gates (gofmt, architecture boundary, prettier, eslint, typecheck, coverage, build, bundle)

## Commands run

### Backend
```
gofmt -l .                    # empty
go vet ./...                  # pass
go test ./... -count=1        # pass (41 ok packages)
go test -race ./internal/... ./test/contract/... -count=1  # pass
```

### Frontend
```
npm run format:check          # pass (with .prettierignore)
npx eslint app components features shared lib tests --max-warnings=0  # pass
npm run typecheck             # pass
npm run test:coverage         # 92 files, 1167 tests pass
npm run build                 # pass
npm run check:bundle          # Bundle budget OK: 161 JS chunks
```

## Runtime versions
- Go: go1.24.5 linux/amd64
- Node: v24.18.0
- npm: 11.16.0

## Results
pass

## Secrets check
- [x] no keys in this file
- [x] no keys in commit

## Notes
- Architecture boundary: replaced `xendit.Fake` import in application test with in-package `fakeDisburse` port double.
- Prettier: added `.prettierignore` for `.next*`, coverage, reports, generated.
- ESLint: renamed non-hook `useMock*` predicates; fixed setState-in-effect / refs / Date.now purity without global rule disable.
- Typecheck: fixed `repoPath` scope in qlt-220 parent framework spec.
- Coverage tests: injectable clock on session mapper; scoped PUB-230 regex; brace-balanced INT-170 catch scan (assertions retained).
